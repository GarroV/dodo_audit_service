"""Блок `mcp`. Контракт — `docs/forge/blocks/mcp.md`.

Чтение накопленных проверок через Claude: список, история точки, сводка по
сети. Только чтение (T095) — пишущие инструменты это отдельная задача T098.

Блок ничего не считает и в базу не ходит сам: проценты, буквы и разбивки
приходят из `db` такими, какими их положил движок при завершении проверки
(жёсткое требование проекта «оценку не считать заново»), а запросы к Postgres
живут в `src/db/queries.py` — здесь их нет и быть не должно.

Арендатор берётся из личного токена (`src/mcp/config.py`), а не из аргумента
вызова: иначе агент партнёра назвал бы код соседа и получил его историю.
"""

from __future__ import annotations

from .errors import AuthError, McpConfigError, McpError, ToolError

__all__ = ["AuthError", "McpConfigError", "McpError", "ToolError"]
