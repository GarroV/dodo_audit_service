# Dodo Audit Service

Здесь собрано всё, что нужно, чтобы начать строить сервис выездных проверок пиццерий: методика, рабочий движок, правила, выведенные на двух боевых проверках, и границы первого шага.

Материал не гипотетический. В августе 2026 через этот движок прошли две реальные проверки в Белграде — 82 фотографии, два отчёта, оба ушли партнёрам. Всё, что здесь написано про правила и грабли, вылезло на этих прогонах.

## Что уже работает

Это не пакет для старта — сервис построен и проверяется прогоном. Ниже то, что
есть сегодня; порядок чтения документов — следующим разделом.

| Поверхность | Чем поднимается | Что делает |
|---|---|---|
| Телеграм-бот | `docker compose up -d` | Проверка целиком: старт, фиксация с кадра или словами, правка в чате, завершение с PDF и письмом. Завершённая проверка и её кадры уходят в историю сами |
| Движок оценки | `engine/audit.py`, `engine/report.py` | Считает процент, букву и разбивку по зонам; собирает PDF и письмо. Оценку не считает больше никто |
| База проверок | `make db-up`, `make migrate` | Завершённые проверки, находки, кадры, справочник точек. Изоляция партнёров ключами схемы; завершённое от правки закрыто политиками |
| MCP-сервер | `make mcp` | Чтение истории через Claude и правка методики версиями, доступ личным токеном, только петля |
| Демо | `docker compose --profile demo up -d` | Синтетический англоязычный набор, боевых данных не касается |

**Главный сквозной сценарий:** аудитор идёт по точке → присылает кадр с комментарием
или пишет словами → бот предлагает пункт, класс и зону → аудитор подтверждает
нажатием → в конце «Завершить» → PDF и текст письма партнёру, а следом сама
проверка и её кадры ложатся в историю. Оценка в отчёте совпадает с тем, что
выдаёт движок на тех же данных, — это проверяется прогоном, а не глазами; база
без настроек — законная конфигурация, и тогда бот работает без истории.

**Чего пока нет:** продукт не раскатан на площадку (это отдельная команда и
отдельное решение владельца), режим проверки по чек-листу отложен до первой
версии, справочника точек в интерфейсе нет — название вводится текстом.

## Что читать и в каком порядке

| Файл | О чём |
|---|---|
| `docs/01-context.md` | Зачем система, кто ей пользуется, что есть сегодня |
| `docs/02-domain.md` | Методика: классы D1/D2/D3, зоны, расчёт оценки, буквы |
| `docs/03-recording-rules.md` | Как формулировать нарушение. Главный документ по качеству |
| `docs/04-engine.md` | Что уже написано и работает, что в нём сломано |
| `docs/05-calibration.md` | Результаты двух боевых проверок и разбор ошибок модели |
| `docs/06-mvp-bot.md` | Задание на первый шаг — телеграм-бот |
| `docs/07-roadmap.md` | Что после MVP: многоязычность, чек-листы, админка |
| `docs/08-deploy.md` | Подъём на сервер: что уезжает, порядок, смоук, откат |

## Что лежит рядом

- `engine/` — рабочие скрипты. Это не черновик, это то, чем собраны оба отчёта.
- `docs/_legacy-skill.md` — `SKILL.md` исходного плагина. Устарел и противоречит `docs/`, оставлен как след происхождения.
- `data/` — данные предметной области: чек-лист на 136 пунктов, критерии, зоны, ставки вычетов.
- `examples/` — две настоящие проверки: `inspection.json`, фотографии и собранный PDF. Эталон **формата**, но не формулировок — см. `examples/README.md`.
- `src/db/` — где живут завершённые проверки: Postgres по `DATABASE_URL` и кадры в S3-совместимом хранилище по `S3_*`. Схема правится только миграциями (`src/db/migrations/`). Поднять базу рядом с ботом и накатить схему одной командой — `make db-up` (снести — `make db-down`); локальное хранилище для смоука — `make storage-up` / `make storage-down`. Имена переменных и что они значат — `.env.example`.
- `tests/methodology/` — синтетическая методика, **по которой идёт набор тестов**. Боевая лежит вне git (D002) и её правит управляющая компания: пока тесты читали её, добавленное в данные слово красило нашу сборку, а формулировки и названия зон утекали в публичный репозиторий через исходники тестов (T141, T146). Совпадают с боевой только идентификаторы — коды пунктов и зон, классы, ставки; формулировки, названия зон, критерии и подсказки выдуманы. Подробности — `tests/methodology/README.md`. Тесту, которому нужна именно боевая методика (боевой якорь на `examples/`, полнота каталога управляющей компании), её дают фикстуры `live_data_env` / `live_data_copy` под меткой `requires_data`.
- `demo/` — синтетический **англоязычный** набор для показов: вымышленная точка, свой чек-лист на 10 пунктов, свои ставки вычетов и своя карта кадров (`demo/data/photo-cues.md`). Боевых данных не касается. Наполняется `make demo`; демо-стенд в контейнере — `docker compose --profile demo up -d`. Английский там не только в данных: профиль ставит демо-боту `BOT_UI_LANG=en`, поэтому по-английски он и здоровается, и ведёт мастер начала проверки. Карта кадров нужна показу не для полноты: без неё в демо не работает быстрый путь (D063), и на первом же комментарии показ упирается в модель — то есть в чужой ключ.

## С чего начать

1. Прочитать `docs/02-domain.md` и `docs/03-recording-rules.md` — без этого код будет правильным, а отчёты неверными.
2. Запустить движок на готовых данных и убедиться, что цифры сходятся:

```bash
cd examples/belgrade-1
python3 ../../engine/audit.py score      # ожидается 97.5%, оценка A
python3 ../../engine/report.py pdf       # соберётся в examples/belgrade-1/reports/
```

Имя PDF детерминировано (пиццерия + аудитор + дата), но сборка кладёт отчёт в `reports/`
рядом с проверкой и приложенный эталон не трогает. Сверять — с ним; `--out` пишет ровно
по указанному пути, поэтому эталон им не перекрывать.

3. Только после этого браться за `docs/06-mvp-bot.md`.

## Команды проекта

Единственный список: команды живут в `Makefile`, здесь их назначение. Добавили
цель — впишите сюда тем же изменением, иначе через месяц о ней никто не узнает.

| Команда | Что делает |
|---|---|
| `make check` | Все гейты качества разом: формат, линт, типы, тесты с покрытием, мёртвый код, границы модулей. Это то, что обязано быть зелёным до коммита. |
| `make test` | Только тесты с покрытием. Часть `check`. |
| `make test-honest` | Тот же прогон, но с отключённым кэшем байткода — для проверки тестов порчей. Отдельной целью: в обычном прогоне это замедляет набор в разы, потому что каждый подпроцесс движка компилируется заново. |
| `make regress` | Движок на эталонных данных. Ожидается дословно: `belgrade-1` → 97.5%, A, 5×D1; `belgrade-2` → 97.0%, A, 6×D1. Разошлось — регрессия. |
| `make demo` | Наполнить синтетический англоязычный демо-набор. Боевых данных не касается. |
| `make demo-down` | Снести **только** демо-стенд. Существует потому, что напрашивающаяся `docker compose --profile demo down -v` сносит вместе с демо боевой бот и удаляет том с состоянием всех идущих проверок. |
| `make loadcheck` | Замер потолка нагрузки: параллельные проверки и альбом одновременных записей. Проверка способна упасть — со снятой блокировкой движка теряет записи. |
| `make loadcheck-live` | Тот же замер, но с **настоящими** вызовами модели. **Платный прогон**: без `--yes` только показывает план и цену. Замерено 03.09: разбор кадра 5.3 с медиана, под очередью из восьми одновременных 7.8 с, отказов нет. |
| `make fastpath` | Замер быстрого пути разбора: сколько фиксаций закрывается по словам, без вызова модели. Расход нулевой — проверка детерминированная. Зону берёт так же, как бот (из слов комментария, иначе память о прошлой записи), поэтому число боевое: на 05.09 — **7 из 17 (41%) при нуле неверных**. С эталонной зоной, которой у бота нет, те же данные дают 53% — это верхняя граница, а не результат. Числа привязаны к версии карты слов, её отпечаток замер печатает сам. |
| `make processhint` | Замер признака «<описание>, это <процесс>» (T166): сколько раз он срабатывает там, где указания словарю не было. Признак ненадёжен по устройству, поэтому меряется только это. На 05.09 — **0 срабатываний на 1027 текстах** методики и боевых проверок, тогда как одно имя процесса без связки сработало бы 24 раза. Корпус, на котором доля считается по-настоящему, — сырые слова аудитора (T183); их пока накоплено ноль, и замер говорит это прямо. Расход нулевой. Каталог состояния не подставляет: пустой каталог по умолчанию дал бы успокоительный ноль вместо «мерить нечего». |
| `make mcp` | Поднять MCP-сервер поверх базы проверок: двадцать два инструмента — шесть чтения истории, один снятия проверки из истории и пятнадцать для правки методики (счёт сверен с `src/mcp/catalogue.py: TOOLS` 06.09.2026; перечень с описаниями — в контракте блока `docs/forge/blocks/mcp.md`, здесь только число), доступ личным токеном, только петля. Подключение к Claude — мостом `tools/mcp_bridge.sh`. |
| `make migrate` | Накат схемы базы. Идемпотентно: на накатанной базе печатает «нечего накатывать». |
| `make cov-engine` | Покрытие движка, который вызывается подпроцессом и в общую метрику не попадает. |

Тесты с живой базой требуют `DATABASE_URL`; без неё они уходят в пропуск, и
покрытие выходит ниже — это не регрессия, а неполный прогон.

## Два правила, которые не обсуждаются

**Оценку не считать заново.** Проценты, буква, разбивка по зонам, сроки — только через `audit.py score`. Один отчёт уже ушёл партнёру с буквой, которую модель поставила «на глаз», а не посчитала.

**Модель предлагает, фиксирует человек.** Никакая запись не попадает в отчёт без подтверждения аудитора. Причины — в `docs/05-calibration.md`.
