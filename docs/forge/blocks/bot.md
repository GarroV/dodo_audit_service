# Блок: bot

## Назначение

Весь разговор с аудитором: начать проверку, принять материал, показать предложение, зафиксировать подтверждённое, дать поправить, завершить и отдать отчёт.

## API-контракт

Наружу — только Telegram. Внутрь — вызовы `domain`, `recognize`, `report`.

Состояния диалога: `idle` → `starting` (пиццерия, вид, язык) → `working` (приём материала) → `confirming` (ждём подтверждения записи) → `finishing`.

Через `confirming` проходит не всякая запись: фиксация словами (T121, D064) подтверждения не ждёт — запись появляется сразу, разговор остаётся в `working`.

### Что блок предоставляет

```python
# src/bot/config.py
load_bot_settings(env: Mapping[str, str] | None = None) -> BotSettings
# BotSettings: token, allowed_ids: frozenset[int], mode, ui_lang,
#              auditor_names: Mapping[int, str]
# ui_lang — язык стенда до начала проверки (T131), из BOT_UI_LANG.
# Неизвестный язык — BotConfigError на старте, а не молчаливый русский

# src/bot/access.py
is_allowed(user_id: int | None, allowed_ids: frozenset[int]) -> bool
AccessMiddleware(allowed_ids)          # внешняя мидлварь на message и callback_query

# src/bot/auditor.py
auditor_name(user_id, full_name, names) -> str   # T063: проверяющий по Telegram ID

# src/bot/texts.py
t(key: str, lang: str, **params: object) -> str  # каталог ru/en, неизвестный язык — отказ
default_ui_lang(env=None) -> str                 # язык стенда: BOT_UI_LANG, иначе ru (T131)
ui_lang_or_default(lang: str | None) -> str      # язык проверки, иначе язык стенда

# src/bot/keyboards.py
kind_title(code: str, lang: str) -> str          # вид проверки словами (T131, T152).
KIND_TITLES                                      # = domain.INSPECTION_KINDS, своей копии нет
# Язык тут не всегда язык интерфейса: на кнопке — интерфейса, в шапке отчёта —
# отчёта. Клавиатуры мастера (`new_inspection_keyboard`, `kind_keyboard`,
# `resume_keyboard`) с T131 принимают язык параметром.
# Сама таблица с T152 живёт в предметной области (`src/domain/kinds.py`): вид
# проверки нужен ещё и базе, и письму по записанной проверке. Здесь остался
# тонкий переходник, приводящий отказ к `BotTextError`. В проверку, в базу и в
# отпечаток уходит КОД (`start_inspection(kind="planned")`), слово подставляется
# при печати — и в чате, и в шапке для движка

# src/bot/material.py                            # связывание кадров с комментарием
PhotoGroup, Comment, Material, ChatMaterialQueue, MaterialStore
# T229: очередей в `ChatMaterialQueue` две и они симметричны — кадры, ждущие
# комментария, и комментарии, ждущие кадра (`hold_comment`). T241: очередь слов
# читается снимком (`held_comments() -> tuple[Comment, ...]`, в порядке прихода,
# при чтении не пустеет) — при завершении бот называет не только кадры без
# записи, но и слова, кадра не дождавшиеся. Слова, сказанные
# ДО кадра, придерживаются и собираются в запись, когда кадр придёт (D090);
# отказа «комментарий без кадра» больше нет, а фотофиксация остаётся
# обязательной (D078): без кадра записи по-прежнему не появляется. Подпись к
# кадру сильнее придержанных слов, обе очереди живут в памяти процесса

# src/bot/albums.py                              # склейка альбома по media_group_id
Frame, AlbumBuffer

# src/bot/sidecar.py                             # заметки бота рядом с проверкой, файлом
SeenFrame, RecordMessage, Notes
read(chat_id) / reset / remember_frames / remember_zone / unclaimed
remember_record(chat_id, message_id, n) / record_of(chat_id, message_id)
# T204: карта «сообщение бота → запись». Аудитор правит запись ОТВЕТОМ на
# сообщение о ней, и адресовать правку больше нечем: в `callback_data` кнопки
# номер едет, а в ответе — только номер исходного сообщения. Живёт на диске, а
# не в памяти: сообщения в переписке переживают перезапуск бота, и правка
# ответом обязана пережить его вместе с ними
remember_origin(chat_id, message_id, n) / origin_of(chat_id, message_id)
# T205: вторая карта — «сообщение АУДИТОРА → запись». Кадр он присылает
# ответом на свои же слова, и попасть тот кадр обязан в ту же запись. Карт
# две, а не одна с признаком: `records` — сообщения БОТА, ответ на такое
# ПРАВИТ запись; `origins` — сообщения аудитора, ответ на такое КАДРОМ
# добавляет кадр в ту же запись. Слей их — и ответ словами на своё же
# голосовое стал бы правкой
# все присланные кадры (T068) и последняя названная зона (D048)
# источник записи (D044) отсюда уехал в саму проверку (T108): domain.Finding.source

# src/bot/sealed.py                              # сданная проверка не правится (T201, D080)
is_sealed(chat_id) -> bool                       # отчёт по этой проверке отдан
refuse(message, lang) -> None                    # один отказ на все входы, с обоими выходами
# Проверка стоит там, где проверка МЕНЯЕТСЯ (приём материала, `_save`, правки,
# правка ответом, `/undo`), а не только на входе в разговор: закрытая кнопка при
# открытом кадре была бы не запретом, а его видимостью

# src/bot/shown.py                     # карты «сообщение → запись»: показ и исток (T204, T205)
remember(chat_id, sent, n)                       # зовётся из КАЖДОГО показа записи
remember_origin(chat_id, message_id, n)          # T205: сообщение аудитора, из которого выросла запись
# Отдельным модулем, а не помощником роутера: показов три (фиксация, правка
# кнопкой, правка ответом), правило одно, и роутер правки не должен зависеть от
# роутера разбора ради одной строки
# Правило одно на обе функции: отказ заметок гасится в журнал, показ записи
# он не роняет

# src/bot/pending.py                             # что ждёт нажатия кнопки, только в памяти
Offer, Proposal, PendingStore
next_slot() -> int                          # номер нового разговора о материале, сквозной по чатам
propose(chat_id, proposal, *, at=None)      # `at` — сообщение бота, под которым стоят кнопки
proposal(chat_id, *, at=None) -> Proposal | None       # предложение под этим сообщением, не забирая
take_proposal(chat_id, *, at=None) -> Proposal | None  # забрать: запись по нему уже сделана
# Proposal.correcting — номер записи, которую этот разбор ПРАВИТ (T204). Пусто —
# обычный разбор нового материала. Адресат назван в момент ответа аудитора, а не
# в момент нажатия: между показом кандидатов и кнопкой аудитор успевает прислать
# ещё кадр, и «последняя запись» к тому времени уже другая
# Proposal.fast — пункт, которым сверка уже сделала запись (T117, T121). Живёт
# только ради кнопки «Разобрать моделью» под этой записью; пусто, если сверка
# не сошлась или запись не легла
# Proposal.slot — номер этого разговора о материале в хранилище (T206):
# кандидаты, вопрос о зоне, страницы ручного перечня — один разговор, разные
# сообщения, и по номеру он опознаётся целиком
# Proposal.batch — какой это кадр пачки и сколько их всего, или ничего для
# одиночного материала (T206, D081). Пачка — единственный случай, когда живых
# предложений в чате несколько разом
# Proposal.origin — сообщение АУДИТОРА, из которого вырос этот материал (T205);
# ответом на него аудитор досылает кадр в получившуюся запись
# T206: предложений в чате бывает несколько разом (пачка кадров без
# комментария разбирается по кадру, а не одной записью на всю пачку).
# Нажатие адресует то предложение, под сообщением которого стоит кнопка
# (`at`); неадресованное нажатие при нескольких живых предложениях получает
# отказ «предложение устарело», а не догадку по последнему показанному —
# догадка записала бы не тот кадр молча

# src/bot/photos.py                              # токен телеграма есть только у бота
fetch_bytes(bot, file_id) -> bytes | None
download_all(bot, file_ids, dest) -> dict[str, str]   # карта для report.build_pdf

# src/bot/view.py                                # строки, которые читает аудитор
# ВСЁ, что показывает методику, требует `chat_id` и умолчания не имеет (T225):
# справочники читаются из ИЗДАНИЯ той проверки (T169), а не из действующей
# методики. Умолчание было бы молчаливым откатом на действующую — ровно тем
# дефектом, ради снятия которого чат сюда и заведён
zone_titles(lang, *, chat_id) / zone_title(code, lang, titles=None, *, chat_id)
confirm_line(finding, lang, *, chat_id) / changed_line(finding, lang, *, chat_id)
candidate_lines(candidates, lang, taken=None, *, chat_id) / counts_line(counts)
corrected_block(finding, lang, *, title, chat_id, cue="", zone_guessed=False)  # правка ответом (T204)
# Процента в строках записи и правки больше нет (T162, D072): по ходу обхода
# оценка не показывается, `pct` из их сигнатур убран вместе с показом
stored_headline(lang)                            # отбивка о сохранении, одна на все пути (T230)
# Пути добавления записи три — сверка по словам, кнопка кандидата, ручной
# перечень, — и о сохранении они говорят одними и теми же словами (D090).
# Правка записи (`corrected_block`) этой отбивки не несёт: записи там не
# прибавилось. Вычета в отбивке нет — оценка по ходу обхода не показывается (D072)
fixed_block(finding, lang, *, title, cue, chat_id, zone_guessed=False)  # запись без подтверждения (T121)
confirmed_block(finding, lang, *, title, chat_id, zone_guessed=False)   # запись по кнопке (T135, T156)
# `zone_guessed` теперь у обоих показов и тем же текстом (T156): зона из памяти
# называется догадкой на всех путях записи, а не только на быстром
record_lines(findings, lang, *, chat_id)         # пометку догадки несёт сама запись (T108)
zone_unusual_mark(finding, lang)                 # «зона не из списка пункта» (T147)
manual_taken_line(codes, zone, lang, taken)      # занятые пары на странице ручного перечня (T173)
# Пометка занятости у ДВУХ перечней разная по месту и одинаковая по словам: в
# перечне модели она в строке пункта (`candidate_lines`), в ручном — в тексте
# над клавиатурой, потому что строк у него нет вовсе, а кнопка занята кодом и
# формулировкой во всю ширину ряда (T217)
# Одно правило на все три строки записи: фиксацию, правку и список перед
# сборкой. Раздельные условия и были причиной, по которой пометка стояла
# только в фиксации

# src/bot/info.py                                # информационная часть в конце (T158)
FIELDS / InfoField(code, kind)                   # семь полей: текст, да/нет, даты
question(field, lang, *, chat_id) / fields_to_ask(lang, *, chat_id)
                                                 # вопрос из ИЗДАНИЯ проверки (T225);
                                                 # нет пункта — не спрашиваем
parse_date(text) -> str | None                   # «14.09.2026 18:30» или ничего

# src/bot/zones.py                               # зона из слов аудитора (T124)
zone_from_words(note: str, *, chat_id: int) -> str | None
                                                 # имена зон из ИЗДАНИЯ проверки (T225),
                                                 # произносимые формы — в коде

# src/bot/inspection.py                          # чтение проверки без немоты (T126)
state_path(chat_id) -> Path
read_inspection(chat_id) -> Inspection | None    # любая неудача чтения — DomainError
# Весь блок читает проверку отсюда, а не через `domain.get_state` напрямую:
# порча приходит разными исключениями (`OSError`, `UnicodeDecodeError`,
# `TypeError`), а две формы не бросают ничего — чужой JSON и каталог на месте
# файла. Здесь всё это приводится к одному отказу

# src/bot/lang.py                                # языки чата, на которых нельзя упасть (T126)
chat_ui_lang(chat_id) -> str / chat_langs(chat_id) -> tuple[str, str]

# src/bot/refusal.py                             # отказ движка человеческими словами (T127)
Refusal(text, clash)
item_title(code, lang, *, chat_id)               # вопрос пункта из ИЗДАНИЯ проверки (T225)
not_recorded(chat_id, *, code, zone, lang, exc) -> Refusal
not_changed(chat_id, n, *, code, zone, lang, exc) -> Refusal

# src/bot/shown.py                               # карта «сообщение → запись» (T204, T205)
remember(chat_id, sent, n) / remember_origin(chat_id, message_id, n)
tell_refusal(message, chat_id, told, lang) -> Message
# Отказ, назвавший запись, — её показ (T227): отправка отказа живёт здесь, и
# связь кладётся тем же движением. Отказ без записи в карту не попадает

# src/bot/routers/info.py                       # кадр в информационной части (T179)
# Кадр, присланный к вопросу, ждёт ОТВЕТА на этот же вопрос и уезжает в движок
# вместе с ним: `domain.set_info(..., photos=[...])`. Печатается он под текстом
# своего поля, поэтому кадра без ответа в отчёте не существует. Неиспользованный
# кадр на следующий вопрос не переезжает, а его потеря называется вслух

build_start_router(settings, pending=None) -> Router  # T050, T051, T052, T063
build_material_router(*, store, albums, on_material, on_waiting=..., on_frame=None, album_window=...) -> Router
# T202: кадр с комментарием в модель не уходит и даже не скачивается у
# телеграма — разбирается комментарий (D081). Кадр без комментария
# разбирается кадром, как и раньше, по кнопке «Разобрать». Решает не бот:
# правило живёт одним местом в `recognize.needs_photo(note)`
# T205: `on_frame` — обработчик кадра, присланного ответом на своё же
# сообщение (`FrameHandler`, см. `make_frame_handler` ниже). Умолчание `None`
# значит «не подхватывать»: без обработчика такой кадр идёт обычной дорогой, с
# вопросом «Разобрать?», как было до T205
build_record_router(*, store, pending) -> Router  # T055, T057, T067, T181, T183
# T183: вместе с записью в домен уходят СЫРЫЕ СЛОВА аудитора — `add_finding(...,
# words=...)`. Словами считается весь его материал об этом кадре, каким он
# пришёл: подпись, отдельное сообщение и расшифровка голоса приходят одной
# строкой (`Proposal.note`). Текстом записи они не становятся (в отчёт идёт
# формулировка), правкой не переписываются и не дописываются: разница между
# сказанным и записанным и есть сигнал. Кадр без комментария оставляет пусто
# T181: при фиксации в домен уходит ПРЕДЛОЖЕНИЕ системы — `add_finding(...,
# suggested=domain.Suggestion(code, level, zone, confidence))`. Предложением
# считается ПЕРВЫЙ кандидат модели, а не нажатая кнопка: выбор второго и есть
# правка, ради которой сигнал собирается (D077). Быстрый путь кладёт свой пункт
# без уверенности — сверка её не считает. Ручной перечень не кладёт ничего:
# модель там не отвечала, и сравнивать не с чем
build_edit_router() -> Router                     # T056
build_correct_router(*, pending) -> Router        # T204: правка ответом на сообщение бота
# T231: ответ, назвавший ТОЛЬКО зону, переставляет зону записи и не трогает
# пункт (D090). Признак «объекта в словах нет» — это `NO_CUE` самой сверки
# (`recognize.fastpath`), а не догадка бота: при любом другом её отказе объект
# назван, и пункт ищется заново моделью. Модель за такую правку не зовётся
# Стоит ДО приёма материала: иначе ответ уедет комментарием к ждущему кадру, и
# вместо правки записи появится вторая. Ответ не про запись роутер пропускает
# дальше нетронутым (`SkipHandler`), и прежнее связывание работает как работало
build_info_router() -> Router                     # T158: информационная часть перед отчётом
start_info(message, state, chat_id, lang)         # зовётся из `finish.on_build`
build_records_router(store) -> Router             # T139: `/records`, показ записанного
UNCLAIMED_SHOWN_LIMIT                             # T138: кадров без записи за раз
HELD_SHOWN_LIMIT                                  # T241: слов без кадра за раз
# T138: кадр без записи возвращается в чат САМИМ кадром, ответом на сообщение,
# которым он пришёл. Номера сообщения аудитору не называются: в телеграме он их
# не видит, и `view.unclaimed_lines` вместе с текстом `finish.unclaimed_line`
# снят. Ответ мягкий: удалённое исходное сообщение показ не отменяет
show_records(message, chat_id, lang, *, store)    # первая часть итога, общая с `/finish`
# T241: та же функция называет и слова, не дождавшиеся кадра, — текстом сам
# текст, голосовое самим голосовым (расшифровки у придержанного голоса ещё нет).
# Очередь ожидания живёт в памяти процесса, поэтому приходит параметром
# (`store`), а не читается показом: второе хранилище разошлось бы с тем, из
# которого собираются записи
build_finish_router(store) -> Router              # T058, T068, T167, T182, T241
# T167: расхождение версии методики (`domain.ChecklistVersionMismatch`) в чате
# называется ОБЕИМИ версиями и двумя кнопками — перевести проверку на
# действующую методику (`domain.sync_checklist_version`, след остаётся в самой
# проверке) или оставить как есть и вернуть прежнюю версию на диск. За аудитора
# бот не выбирает: молчаливый подсчёт по новой методике запрещает D033
archive(message, chat_id, photos, lang, *, allow_missing)   # слив завершённой в историю (T123)
# T182: исходов слива четыре, а не три. `db.VersionMismatchError` разбирается
# отдельной веткой ДО общего `db.DbError` (он её предок, иначе недостижима):
# база в порядке, а методику переиздали между итогом и сливом. Текст и
# клавиатура — те же, что на подсчёте (T167)
make_material_handler(pending) / make_waiting_handler(pending) / make_frame_handler()
# T205: `make_frame_handler` кладёт кадр, присланный ответом на своё же
# сообщение, в ту же запись. Разбора здесь нет: пункт уже найден по словам
# аудитора, кадр их только подтверждает
# T206: пачка кадров без комментария разбирается по кадру (`_analyze_frames` в
# `record.py`), а не одной записью на всю пачку — иначе второй и третий кадр
# уезжали в отчёт прикреплёнными к чужому пункту, никем не разобранные
# T232: разбирается не больше `record.BATCH_LIMIT` кадров (пять, D091), про
# остальные бот ГОВОРИТ, что не разбирал их и что прислать их надо отдельной
# пачкой (D093). Предел стоит у разбора, а не на приёме: кадры принимаются все
# и попадают в список присланного (T068), платит пачка вызовами модели

# src/bot/app.py
build_dispatcher(settings, *, album_window=..., on_material=None) -> Dispatcher
on_unexpected_error(event) -> bool                # последний рубеж: сбой доходит до аудитора (T126)
create_bot(settings) -> Bot
announce_commands(bot) -> None                   # команды в меню телеграма (T139)
MENU_COMMANDS: tuple[tuple[str, str], ...]       # состав меню: /start, /records, /undo,
                                                 # /finish, /mcp (T209, D087), /version (T246).
                                                 # Снятия сданной проверки здесь нет и не
                                                 # будет — это операция УК через MCP (D086)

# src/bot/routers/mcp.py
MCP_COMMAND: str                                 # «mcp»
MCP_URL_VAR: str                                 # «BOT_MCP_URL» — адрес, который бот ПЕЧАТАЕТ
setup_url(lang) -> str                           # адрес из окружения или заглушка на языке
start_polling() -> None                          # long polling; методика проверяется до первого сообщения
main() -> None                                   # точка входа `python -m src.bot`
```

### Команды и кнопки

| Что | Как вызывается |
|---|---|
| Начать проверку | `/start`, дальше кнопки мастера |
| Разобрать кадр | кнопка «Разобрать» под принятым кадром (T067) |
| Записать пункт по словам | ничем: запись появляется сразу, без нажатия (T121, D064) |
| Отправить тот же материал модели | кнопка «Разобрать моделью» под такой записью (T117, T121) |
| Зафиксировать запись | кнопка с номером кандидата (T055) |
| Выбрать пункт вручную | кнопка «Выбрать пункт», страницами по 8 (T034) |
| Назвать зону, когда в словах её нет | кнопки зон справочника, ПО ОДНОЙ В РЯД (T140) |
| Поправить запись | кнопки «Зона», «Класс», «Формулировка», «Удалить» под любой записью (T056) |
| Поправить ПУНКТ записи | ответ (reply) на сообщение бота с этой записью, словами или голосом (T204) |
| Посмотреть, что уже записано | `/records`, она же в меню телеграма (T139) |
| Снять последнюю запись | `/undo` |
| Завершить проверку | `/finish`, дальше кнопки итога (T058) |
| Поставить MCP у себя | `/mcp`, она же пункт меню «Установка MCP» (T209, D087). Токена не печатает — см. решения блока |
| Спросить версию сборки | `/version`, она же пункт меню «Версия сборки» (T246). В меню объявлена вместе с остальными (T209) |
| Убрать сданную проверку из чата | кнопка «Убрать из чата» рядом с любым отказом сданной (T201) |
| Ответить на информационную часть | кнопки «Да»/«Нет», «Пропустить», «Дальше к отчёту» плюс текст, голос и подпись к кадру (T158) |

## Зависимости

`domain`, `recognize`, `report`.

## Definition of Done блока

Пункты второй очереди (`recognize` не принят на момент первой) помечены отдельно.

**Первая очередь — T050, T051, T052, T053, T054, T059, T063**

- [x] Отвечает только Telegram ID из списка разрешённых; чужому не отвечает ничего осмысленного.
- [x] «Новая проверка» спрашивает пиццерию текстом (D051), вид и язык отчёта; проверяющий подставляется по Telegram ID, дата — сегодняшняя, руками не вводится (D032, T063).
- [x] Незавершённая проверка не перезаписывается молча: показывается, чья она и от какого числа, с выбором «Продолжить» или «Начать новую».
- [x] СДАННАЯ проверка не правится вовсе (T201, D080): «Продолжить» у неё нет, а кадр, комментарий, кнопка правки, ответ на сообщение бота и `/undo` упираются в отказ с причиной и двумя выходами — начать новую или убрать сданную из чата.
- [x] Комментарий связывается с фотографией тремя способами: подпись, сообщение следом, ответ на любое из ранее отправленных фото.
- [x] Альбом склеивается по `media_group_id` с окном 1–2 секунды в одну запись с несколькими фотографиями.
- [x] Пачка сообщений, пришедшая с задержкой после потери связи, обрабатывается без путаницы в привязке комментариев.
- [x] Прерванная проверка продолжается после перезапуска бота.

**Вторая очередь — T055-T058, T067, T068**

- [x] Предложения модели показываются кнопками; запись по ним появляется только после подтверждения. С фиксации словами подтверждение снято позже, решением D064 — см. пятую очередь.
- [x] Подтверждение — одна строка: пункт, класс, зона, накопленный процент. Без таблиц после каждого кадра.
- [x] Правки прямо в чате: удалить последнее, сменить зону, класс, переписать формулировку. Процент пересчитывается.
- [x] Замеры пишутся записями `D0` (`INF09`, `INF10`, `INF11`) среди находок; блок `info` не используется.
- [x] «Завершить» показывает итог и список зафиксированного, даёт поправить, затем отдаёт PDF файлом и письмо сообщением.
- [x] На кадр без комментария бот спрашивает «Разобрать?» кнопкой; без нажатия ни один кадр в модель не уходит (D043, D046).
- [x] Кадры, оставшиеся без записи, показываются при завершении проверки и не теряются молча (T068). Показываются самим кадром, ответом на своё сообщение (T138): номером сообщения аудитор кадр найти не мог — в телеграме номер не показывается.

**Четвёртая очередь — T117**

- [x] `fast_path` зовётся ДО `classify`. Сработал — модель не зовётся вовсе. Не сработал — как раньше, без изменений.
- [x] Рядом с ответом видны слова аудитора **целиком** и сработавшая строка карты (`item.cue`).
- [x] Кнопка «Разобрать моделью» отдаёт модели тот же материал — те же слова и тот же кадр — и сверку в этот раз пропускает.
- [x] Фиксация идёт словами аудитора, источник — «со слов аудитора» (`SOURCE_COMMENT`).
- [x] `reason` человеку не показывается никогда: он уходит в журнал.

**Изменено пятой очередью:** пункт больше не показывается кнопкой подтверждения — по нему сразу делается запись (T121, D064).

**Пятая очередь — T121 (решение D064)**

- [x] Слова аудитора, однозначно легшие на карту, дают запись **сразу, без нажатия**.
- [x] Фиксация по кадру не изменилась: «Разобрать?» → кандидаты → запись только по нажатию.
- [x] Показ записи виден: строка записи, **вопрос пункта словами**, слова аудитора целиком, строка карты.
- [x] Под записью — четыре кнопки правки и «Разобрать моделью»: правка не меняет код пункта, и без выхода к модели неверный код чинился бы по кругу.
- [x] Отказ движка (пара «пункт + зона» занята) тупиком не заканчивается: причина названа, материал уходит модели.
- [x] Быстрый путь и разбор моделью в остальном не тронуты; `SOURCE_COMMENT` доезжает до записи как прежде.

**Шестая очередь — T123, T124, T126, T127, T128 (расхождения, найденные сверкой со спекой)**

- [x] Завершённая проверка уходит в историю сама: `push_inspection`, следом `upload_photos` теми же кадрами, что скачаны для отчёта (T123). Отказ базы отчёт не отменяет и молчанием не остаётся; незаданный `DATABASE_URL` — законная работа без базы и сбоем не выглядит.
- [x] Зона берётся из слов **текущего** комментария, а память подставляется только когда о зоне в них не сказано (T124). Названы две зоны — разбор не выбирает за человека. Зона из памяти в записи без подтверждения названа догадкой вслух.
- [x] Испорченное состояние не делает бота немым: у диспетчера есть глобальный обработчик, `/start` работает и на нечитаемом файле — это выход (T126). Аудитору уходит причина и что делать, разбор с путями — в журнал.
- [x] Отказ движка разбирается, а не пересказывается: пункт и зона названы по-человечески на языке интерфейса, занятая пара «пункт + зона» приводит к кнопкам занявшей записи (T127). Командной строки в чате нет.
- [x] Причина не называется наугад: «предложение устарело» больше не винит перезапуск, которого не было (T128).
- [x] Язык интерфейса и язык речи задаются из мастера, а не остаются умолчанием: выбор английского делает английским и разговор (T128).
- [x] Название точки длиннее замеренного предела не принимается на вводе, а не роняет сборку отчёта в конце проверки (T128).

**Седьмая очередь — T131 (язык интерфейса до начала проверки)**

- [x] Язык, которым бот говорит ДО начала проверки, — параметр развёртывания `BOT_UI_LANG`, а не константа в коде.
- [x] Демо-стенд поднимается английским целиком: приветствие, кнопка входа, вопросы мастера и надписи его кнопок. Проверено запуском демо-образа с окружением демо-профиля, а не чтением кода.
- [x] Боевой стенд не тронут: переменная не задана — русский, как с первого дня.
- [x] Неизвестный язык останавливает бота на старте (`BotConfigError`), молча русским не становится.
- [x] После начала проверки язык берётся из её собственного `ui_lang`, а переменная больше не спрашивается.
- [x] Вид проверки на кнопке — на языке интерфейса, а в шапке отчёта партнёру — на языке отчёта: два разных языка проекта, а не один.

**Восьмая очередь — T126 переоткрыта, T147, T151, T153**

- [x] Испорченное состояние не делает бота немым **ни на одной форме порчи**: пустой файл, обрезанный JSON, список вместо объекта, двоичный мусор, `findings` не список, нет прав на чтение, чужая структура JSON, каталог вместо файла — на всех восьми входах (`/start`, `/finish`, `/undo`, кадр, голос, текст, правка, сборка). `/start` на каждой из них даёт выход: повреждение названо вслух и под ним кнопка «Новая проверка».
- [x] Чужой JSON больше не выдаётся за незавершённую проверку, а каталог на месте файла — за непочатую: обе формы теряли содержимое молча при следующей «Новой проверке».
- [x] Последний рубеж отвечает, даже если язык чата не выбрался: у `on_unexpected_error` своя защита, потому что за ним обработчика нет.
- [x] Пометка «зона не из списка пункта» видна везде, где видна запись: при фиксации, после правки зоны и в списке перед сборкой отчёта. Правка — самый частый способ увести запись туда, где пункта нет, и раньше она об этом молчала.
- [x] Отказ сборки отчёта разбирается, а не пересказывается (тот же запрет, что в T127): ни удвоенного служебного префикса, ни абсолютных путей, ни инструкции ставить системные библиотеки. То же на входе в проверку — `start.failed` больше не отдаёт стек движка. Неудача письма получила свой текст: отчёт к этому моменту уже у аудитора.
- [x] Сданная проверка не называется незавершённой и не пугает стиранием: `/start` различает проверку в работе и проверку, по которой отчёт уже собран и отдан. Признака «завершена» у движка нет — бот опирается на то, что сделал сам, и хранит это в своих заметках.

**Девятая очередь — T162, T139, T128 (переоткрыта), T158**

- [x] Процент по ходу обхода не показывается — только в итоге при завершении (D072). Вместе с показом снят и расчёт: подпроцесс движка не зовётся ради числа, которое выбрасывается.
- [x] Посмотреть записанное можно, не завершая проверку: команда `/records` с тем же содержимым, что первая часть `/finish`, и она объявлена в меню телеграма — команда, о которой аудитор не знает, спрятана так же, как была спрятана функция.
- [x] Имя файла отчёта не вылезает за 255 байт ни на каком достижимом входе: название точки ограничено и знаками, и байтами, имя аудитора из профиля телеграма обрезается, обрезка видна в имени и названа аудитору.
- [x] Информационная часть спрашивается после подтверждения завершения и ДО сборки отчёта (D069, D070): семь полей, все пропускаемые, ответ текстом, голосом (расшифровка показывается до записи и правится) или подписью к кадру, «да/нет» кнопками на языке отчёта. Вид проверки и фотофиксация в неё не входят.
- [x] **Закрыто двенадцатой очередью (T179):** кадр, приложенный к полю информационной части, доезжает до страницы отчёта. На момент девятой очереди движок хранил в `info` строку, и места под снимок не было.

**Десятая очередь — T160, T134, T135, T136, T137**

- [x] Фотофиксация обязательна всегда (D078, отменивший вчерашний D071), и аудитор понимает это ДО первой ошибки, а не по факту отказа: правило звучит первым сообщением проверки, повторяется при её продолжении и стоит абзацем под ожиданием кадра к уже сказанным словам (отказ на этом месте снят задачей T229, решение D090). Формулировка одна на все три места (`material.photo_required`, сборка `with_photo_rule`) — скопированная, она разъехалась бы молча. Признак «фото обязательно по пунктам» из D071 не заводится: обязательно везде.
- [x] Кнопка, которую некому обработать, получает ответ, а не тишину до таймаута телеграма (T134): последний рубеж без фильтра данных, включённый в диспетчер ПОСЛЕДНИМ. Нажатие старше 48 часов приходит без сообщения — тогда единственный канал ответа всплывающее окно, и текст держится в пределе `ALERT_TEXT_LIMIT` (200 знаков, режет Telegram молча).
- [x] Кнопка выбора кандидата подписана действием, а не голой цифрой (T136): ряд «1 // Выбрать пункт // Не записывать» читался номером рядом с двумя глаголами. Номер остаётся только там, где связывает кнопку со строкой перечня, — при нескольких кандидатах; при одном его нет.
- [x] Подтверждённая запись показана не скупее автоматической (T135): к строке добавлены вопрос пункта словами и формулировка, которая уйдёт в отчёт партнёру. Требование спеки «одна строка» снято тем же изменением (`docs/06-mvp-bot.md`, шаг 5) — оно опиралось на пункт, «прочитанный на кнопке», где до T136 стояла цифра. Таблицы после каждого кадра и процента по ходу обхода по-прежнему нет.

- [x] Занятый пункт в перечне предложений помечен и называет номер занявшей записи (T137): бот сказал «уже зафиксировано», отдал материал модели — это задумано, на кадре бывает второе нарушение, — и она предлагает тот же пункт. Непомеченным он неотличим от остальных, а нажатие даёт второй отказ подряд. Пометка по ПАРЕ «пункт + зона»: тот же пункт в другой зоне — законная запись. **Ручной перечень с T173 помечается тоже** — пометка ушла в текст над клавиатурой, кнопка не тронута ни знаком (двенадцатая очередь).

**Двенадцатая очередь — T173 (занятая пара в ручном перечне)**

- [x] Занятая пара «пункт + зона» видна и в ручном перечне, а не только в перечне модели: там она была помечена с T137, здесь пункт был неотличим от свободных, и нажатие давало отказ по поводу, о котором бот уже говорил.
- [x] Формулировка на кнопке не пострадала ни на знак: пометка живёт в тексте сообщения над клавиатурой. Проверено сравнением надписей до знака, а не «до и после» — сравнение «до и после» зелено и тогда, когда пометка съела формулировку у всех кнопок разом.
- [x] Пометка называет номер занявшей записи — тот же, который назовёт отказ движка.
- [x] Помечается ПАРА, а не код: тот же пункт в другой зоне — законная запись, и отговаривать от неё нельзя.
- [x] Пометка говорит только о пунктах ЭТОЙ страницы; на странице без занятых её нет вовсе.
- [x] Занятый пункт остаётся в перечне и нажимаемым: убрать его значило бы решить за человека и спрятать часть методики.

**Двенадцатая очередь — T183 (сырые слова аудитора)**

- [x] Сырые слова аудитора хранятся рядом с записью — тем же приёмом, что источник (D044) и предложение системы (D077): свой ключ в блоке `domain` файла проверки, а не поле в структуре движка.
- [x] Слова доезжают со ВСЕХ путей, где они звучали: подпись к кадру, отдельное сообщение следом, расшифровка голоса; через модель, через быстрый путь и через ручной перечень.
- [x] Разбор голого кадра слов не придумывает: аудитор не сказал ничего, и формулировка модели его речью не объявляется.
- [x] Правка записи слова не трогает и не дописывает к ним — разница между сказанным и записанным и есть весь сигнал.
- [x] Снятая запись уносит свои слова: речь о нарушении, которого в проверке нет, засорила бы выборку управляющей компании.
- [x] Чужая структура на месте слов — отказ чтения, а не молчаливое «неизвестно что» в выборке.

**Двенадцатая очередь — T179 (вторая половина требования D069)**

- [x] Кадр, приложенный к полю информационной части, доезжает до СТРАНИЦЫ отчёта, а не до команды движка: подпись становится ответом, снимок печатается под ним. Проверено собранным документом — страница приложения открыта глазами.
- [x] Прежняя фраза «в отчёт попадает только текст» снята из каталога целиком: оставленная, она однажды всплыла бы на другом пути.
- [x] Кадр без подписи ждёт ответа на тот же вопрос и не переезжает на следующий. Вопрос пропущен вместе с кадром — бот говорит об этом, а не выбрасывает присланное молча.
- [x] Ссылка кадра поля попадает в карту кадров отчёта наравне со ссылками записей; кадры поля скачиваются при сборке тем же заходом. Без этого движок напечатал бы партнёру красную отметку «фотография не приложена».
- [x] Потерянный кадр поля назван ПОЛЕМ, а не «записью №INF01»: записи с таким номером не существует.
- [x] Кадр, приложенный к полю, не числится кадром без записи: `/records` и итог не пугают потерей того, что в отчёт как раз попадёт.

**Одиннадцатая очередь — T181, T182 (решение D077)**

- [x] Предложение системы сохраняется рядом с записью и доезжает до базы: пункт, класс, зона и уверенность. Предложением считается ПЕРВЫЙ ответ модели, а не нажатая кнопка, — иначе расхождения не существовало бы как явления.
- [x] Быстрый путь кладёт своё предложение без уверенности: сверка её не считает, а ноль был бы ложью. Ручной перечень не кладёт ничего: модель там не отвечала.
- [x] Правка записи предложение не трогает — их разница и есть сигнал. Удаление записи уносит и предложение: номер освобождается, и оставленное досталось бы чужой записи.
- [x] Боевой список слов бот не пополняет и не предлагает пополнить: D077 запрещает прямо. Дело блока — сохранить сигнал.
- [x] Расхождение версии методики на сливе не называется отказом базы: своя ветка по типу `db.VersionMismatchError`, обе версии человеку, те же два выхода кнопками, что и на подсчёте (T182).
- [ ] **Не закрыто и вынесено:** явное указание аудитора «это <процесс>» (T166) — признак найден и измерен, но складывать его некуда: место, которое построил T181, привязано к записи, а такое указание записи может не породить вовсе. Разбор — ниже, в «Решениях».

**Тринадцатая очередь — T205, T206 (решение D081)**

- [x] Кадр, присланный ОТВЕТОМ на свои же слова (голосовое, подпись, правку), уходит в ту самую запись, а не заводит вторую и не спрашивает «Разобрать?». Разбора у него нет: пункт уже найден по словам человека.
- [x] Карт сообщений в заметках две и они не смешаны: ответ СЛОВАМИ на сообщение бота правит запись (T204), ответ КАДРОМ на своё сообщение пополняет её (T205).
- [x] Досланный кадр попадает в список присланного (T068), а непринятый движком кадр называется вслух: аудитор не должен считать фотофиксацию сделанной, когда её нет (D078).
- [x] Пачка кадров БЕЗ комментария разбирается по кадру, и отбивка приходит списком отдельными сообщениями — по сообщению на кадр, с номером кадра.
- [x] Кадры С комментарием остаются одним нарушением с несколькими фото: случаи разводит наличие слов, а не число кадров.
- [x] Ответ на любое сообщение отбивки правит именно ту запись — тем же механизмом T204, второго не заведено.
- [x] Нажатие адресует то предложение, под сообщением которого стоит кнопка. Неадресованное при живой пачке и нажатие из уже разобранного сообщения отвечают «устарело», а не записывают чужой кадр.
- [x] Подтверждение кадра человеком (D064) и запрет разбора без нажатия (D046) в силе: пачка ждёт нажатия по каждому кадру, число разбираемых кадров названо до начала.

**Четырнадцатая очередь — T229, T230, T231, T232 (решения D090, D091, D093)**

- [x] Слова, сказанные ДО кадра, придерживаются и ждут его: бот отвечает, что ждёт фотографию, и собирает запись в момент прихода кадра. Отказа «комментарий без кадра» больше нет, а фотофиксация обязательна по-прежнему (D078) — записи без кадра не появляется.
- [x] Очередь придержанных слов симметрична очереди кадров: тот же FIFO, то же время жизни (память процесса), подпись к кадру сильнее придержанного.
- [x] Отбивка о сохранении одна на ВСЕ пути добавления записи — сверку по словам, кнопку кандидата и ручной перечень (`view.stored_headline`, ключ `record.stored`). Правка записи её не несёт: там записи не прибавилось.
- [x] Ответ, назвавший ТОЛЬКО зону, переставляет зону записи, не трогая пункт, класс, текст и кадры, и подтверждается новой отбивкой. Модель за такую правку не зовётся.
- [x] Признак «в словах только зона» взят у самой сверки (`recognize.fastpath.NO_CUE`), а не выдуман ботом: при любом другом её отказе объект назван, и пункт ищется заново, как раньше.
- [x] Разбирается не больше пяти кадров пачки (`record.BATCH_LIMIT`), про остальные бот говорит словами — что не разбирал их и что прислать их надо отдельной пачкой. Кадры при этом принимаются все и остаются в списке присланного (T068).
- [ ] **Вынесено диспетчеру:** вычет в отбивке о сохранении. Пример владельца в D090 назван с числом баллов, но оценка по ходу обхода не показывается вовсе (T162, D072), и число здесь стоило бы подпроцесса движка на каждую запись. Развилка двух решений владельца — не дело блока.

**Пятнадцатая очередь — T209, T241 (решение D087)**

- [x] Меню телеграма называет все пять команд бота, спрятанных больше нет. Порядок — порядок работы: начать, посмотреть записанное, снять последнее, завершить; установка MCP последней, она не шаг обхода.
- [x] Пункт «Установка MCP» присылает объяснение и готовую строку для терминала РАЗНЫМИ сообщениями: сообщение копируется целиком, склеенный текст уехал бы в терминал вместе с командой.
- [x] Токена в строке нет. Бот не знает, чей токен чей: `MCP_TOKENS` связывает токен со СТОРОНОЙ, а не с человеком, и открывает всю её историю проверок, — значит, любой напечатанный был бы выдан наугад. На его месте заглушка, рядом сказано, что токен личный, где его берут и что пересылать нельзя.
- [x] Адрес сервера — из своей переменной `BOT_MCP_URL`, а не из `MCP_HOST`/`MCP_PORT`: блоки `bot` и `mcp` пиры и друг друга не импортируют, а сервер слушает петлю — в команде стоит адрес машины человека или его туннеля. Не задан — такая же заглушка, а не молчаливая петля.
- [x] Снятия сданной проверки в меню нет (D086) — и это сторожит тест состава, а не память того, кто будет пополнять список.
- [x] Слова, не дождавшиеся кадра, называются при завершении и по «Что записано» — тем же списком, что и кадры без записи (T068). Текст возвращается текстом, голосовое — самим голосовым: расшифровки у придержанного голоса ещё нет.
- [x] Показ очередь не пустошит: кадр, присланный уже после итога, собирает запись из тех же слов. Показ ничего не решает за аудитора.
- [ ] **Вынесено диспетчеру:** остальной состав меню (повтор отчёта, язык отчёта, шпаргалка «как это работает», снятие придержанного) — предложения, ждущие слова владельца. Названы им только два пункта, и додумывать состав блок не стал.

**Снято требование** «фотография без комментария вызывает уточняющий вопрос и не фиксируется» — отменено решением D043 и заменено кнопкой «Разобрать?» (D046).

**Уточнено требование** «несколько кадров одного объекта — одно нарушение с несколькими фото»: оно осталось в силе для кадров С комментарием и разведено с пачкой без комментария (T206, D081).

## Статус

`in_progress` — **четырнадцатая очередь готова и предъявлена к приёмке**: T229 (слова до кадра придерживаются), T230 (единая отбивка о сохранении), T231 (ответ зоной правит зону записи), T232 (предел разбираемой пачки — пять кадров).

**Четырнадцатая очередь, состояние волны на 06.09.2026.** Копия `feat/bot18` от свежего `main`, своя `STATE_DIR`, порты 8630–8639. Каждая задача закоммичена отдельно; `main` влит в ветку перед сдачей.

**Что тронуто за пределами роутеров:** `src/bot/material.py` (вторая очередь — придержанные комментарии), `src/bot/view.py` (`stored_headline`), `src/bot/texts.py`. Блоки `domain`, `recognize`, `report`, `db` и `engine/` не тронуты вовсе: правка зоны идёт через уже существующий `domain.edit_finding`, признак «объекта в словах нет» читается публичной константой `recognize.fastpath.NO_CUE`.

**Ключ `material.no_photo` снят.** Отказа на комментарий без кадра больше не существует ни на одном пути, и мёртвый текст в каталоге читался бы как живой запрет.

**Развилка, вынесенная диспетчеру:** отбивка о сохранении и вычет — см. последний пункт DoD четырнадцатой очереди.

### Прежний статус (двенадцатая очередь)

`in_progress` — **двенадцатая очередь готова и предъявлена к приёмке**: T179 (кадр информационной части доезжает до отчёта, задача #146), T183 (сырые слова аудитора хранятся рядом с записью) и T173 (занятая пара в ручном перечне).

**Двенадцатая очередь, состояние волны на 04.09.2026.** Копия `feat/bot13` от свежего `main`, база `dodo_audit_bot13` на локальном Postgres, своя `STATE_DIR`. Окно подписки израсходовано на 88 % на момент старта — каждая задача закоммичена отдельно.

**T179 потребовал тронуть `domain` и `report`, и разрешение спрошено ДО правки.** Цепочка «кадр → отчёт» по половинам бессмысленна, и диспетчер разрешил три точки, названные поимённо: параметр `photos` у `domain.set_info`, поле `info` у `domain.Inspection` (плюс `models.InfoAnswer` и `state.read_info`, которыми оно читается) и обход информационного блока в `report.resolve_photos`. Четвёртой правкой оказалась одна строка типа в `report/errors.py`: `PhotoMissing.misses` теперь несёт `int | str` — номер записи или код поля. `src/db` и `engine/` не тронуты вовсе.

**Почему без блока `report` задача была бы вредна.** `build_pdf` отдаёт движку карту «ссылка → файл» ВСЕГДА, а движок при заданной карте резолвит только по ней. Ссылка кадра поля мимо карты напечаталась бы партнёру красной отметкой «фотография не приложена» — то есть вместо честного предупреждения бота получилось бы молчаливое «фото потеряно». Проверено на движке: `tests/test_engine_info_photo.py` этот исход уже описывает.

**T183: что считать сырыми словами и что делать при правке — решено внутри блока.**
Разбор — ниже, в «Решениях, принятые внутри блока».

**Кадр ждёт ответа, а не наоборот.** Движок печатает снимок ПОД ТЕКСТОМ своего поля, значит кадра без ответа в отчёте не существует. Поэтому кадр без подписи не пишется никуда сам: он ждёт слов на тот же вопрос, а пропуск вопроса вместе с кадром бот называет вслух (`info.photo_dropped`). Молчаливый выброс присланного и есть тот дефект, ради которого задача заведена, — воспроизводить его на новом месте нельзя.

`in_progress` — **одиннадцатая очередь готова и предъявлена к приёмке**: T181 (предложение системы сохраняется рядом с записью, задача #148), T182 (расхождение версии методики на сливе не выглядит отказом базы, #149) и T166 (исследование, закрыто ответом «признак есть, места под него нет» — см. ниже).

**Одиннадцатая очередь, состояние волны на 04.09.2026.** Копия `feat/bot12` от свежего `main`, база `dodo_audit_bot12` на локальном Postgres. Окно подписки израсходовано на 88 % на момент старта — каждая задача закоммичена отдельно. Исполнителям не отдано ничего: T182 — двадцать строк, T181 — про границу между блоками (что считать предложением и где оно живёт), готового контракта под неё не было; T166 — исследование, отдавать нечего.

**T181 потребовал тронуть блок `domain`, и граница названа.** Диспетчер разрешил это явно. В `domain` добавлено ровно то, чем предложение запоминается, — тем же приёмом, каким уже запоминался источник записи (D044, T108): `models.Suggestion` (тройка «пункт, класс, зона» плюс уверенность), четыре плоских поля у `Finding`, ключ `suggestions` в блоке `domain` файла проверки, `remember_suggestion`/`forget_suggestion`/`read_suggestions` рядом с `remember_source`/`forget_source`/`read_sources`, параметр `suggested` у `add_finding` и снятие предложения в `drop_finding`. Больше ничего: `edit_finding` не тронут намеренно — правка обязана менять запись и НЕ трогать предложение, иначе расхождение стирается ровно в тот момент, когда возникает. `src/db` и `engine/` не тронуты вовсе.

**Что нужно от блока `domain`:** его контракт (`docs/forge/blocks/domain.md`) о новом параметре `add_finding`, о `Suggestion` и о четырёх полях `Finding` не знает — правка чужого контракта за границей этого блока, и её надо назначить отдельно.

**Плоские поля, а не вложенный объект, — потому что так их берёт слив.** `db.push` читает `finding.suggested_code` и три соседних через `getattr` (форма заложена задачей T164 до появления значения). Вложенный `Suggestion` пришлось бы разворачивать на границе блоков, то есть в третьем месте, и это место разъехалось бы с обоими.

**Предложением считается ПЕРВЫЙ ответ модели, а не нажатая аудитором кнопка.** Это главное решение задачи. Запиши мы выбранного кандидата — предложенная и итоговая тройки совпадали бы всегда, и «модель предложила одно, аудитор поправил на другое» перестало бы существовать как измеримое явление. Выбор второго кандидата — это и есть правка. Побочно: пустая зона кандидата приводится к `UNKNOWN` — у модели это ОТВЕТ («из слов места не видно»), а пустая строка уехала бы в базу как `NULL`, то есть как «модель не предлагала ничего», рядом с названным ею пунктом.

**Быстрый путь предложение оставляет, уверенность — нет.** Сверка со списком слов и есть тот список терминов, который владелец просил пополнять, и промах в ней опаснее всех: подтверждения у неё нет (D064), увидеть промах можно только правкой следом. Уверенности она не считает вовсе — строгий критерий либо сходится, либо нет, — и `NULL` тут честнее нуля: «система ни в чём не уверена» осмысленное утверждение, и по нему ставят порог отбора. **Оговорка для T165:** отбор по порогу `confidence >= x` эти строки не увидит, а они самые ценные. Условие в MCP придётся писать как «порог ИЛИ уверенности нет».

Проверено прогоном 04.09 в своей рабочей копии: `make fmt lint types dead bounds` зелёные целиком (`mypy --strict` на 86 файлах, `vulture`, `deptry`, `lint-imports` — 2 контракта из 2), полный `make test` с подключённым Postgres — **1683 теста, 0 пропущено, покрытие 98 %**, 2 мин 40 с. `make regress` даёт прежние 97.5 %/A/5×D1 и 97.0 %/A/6×D1.

Проверка порчей T181: 11 порч, 11 адресных покраснений, восстановление копиями, снятыми до порчи; `models.py`, `state.py`, `findings.py`, `record.py` вернулись байт в байт по md5. Порчи снимали защиты по одной: предложение не записывается (9 красных), удалённая запись оставляет своё предложение (1), бот молчит о предложении модели (4), предложением записан последний кандидат вместо первого (1), молчание модели о зоне записано пустотой (1), быстрый путь предложения не оставляет (2), ручной выбор человека записан как предложение модели (1), предложение без пункта принимается молча (1), чужая шкала уверенности принимается (1), чужая структура предложения читается молча (1), предложение подставляется из самой записи (16). **Первый заход порчи «удалённая запись оставляет своё предложение» прошёл зелёным, и это поймано, а не засчитано:** якорь вставлял пустую строку перед комментарием, а сам вызов `forget_suggestion` не трогал — порча была пустышкой. Переделана на удаление вызова, после чего покраснела адресно.

Проверка порчей T182: 3 порчи, 3 адресных покраснения (ветки нет — 3 красных; текст без клавиатуры — 1; версии перепутаны местами — 1), `finish.py` вернулся байт в байт.

Живой смоук T181 04.09: разговор через настоящий диспетчер на боевой методике, модель подменена (ни одного платного вызова), сессия телеграма записывающая — наружу не ушло ничего. Модель предложила `CLN02`/`D1`/`hot_kitchen` с уверенностью 0.51, аудитор нажал ВТОРОГО кандидата и следом сменил зону кнопкой. Отчёт собран и отдан, бот сам слил проверку в живой Postgres. Запрос к базе:

```
точка | Смоук T181     n | 1
пункт | CLN05          класс | D1        зона | dining
предложен_пункт | CLN02  предложен_класс | D1  предложена_зона | hot_kitchen
уверенность | 0.510
```

Обе тройки на месте, и `FindingRow.corrections()` считает по ним `{"code", "zone"}` — то, что аудитор поправил, третьей копией не хранится.

`in_progress` — **десятая очередь готова и предъявлена к приёмке**: T152 (вид проверки связан кодом, задача #123), T138 (кадр без записи показан кадром, #109), T156 (зона из памяти названа догадкой на всех путях, #127) и T167 (расхождение версии методики показано человеку, #135). Очереди с первой по девятую — ниже.

**Десятая очередь, состояние волны на 04.09.2026.** Копия `feat/bot11`, параллельно работает блок `db`. Окно подписки израсходовано на 88 % на момент старта, за сутки волна обрывалась трижды — поэтому каждая задача закоммичена отдельно, и обрыв стоит одной задачи, а не блока. Исполнителям не отдано ничего: T152 — про границу «код или формулировка» и про то, где живёт таблица перевода (готового контракта не было, его и надо было провести); остальные три — по десятку строк каждая, и запуск исполнителя стоил бы дороже самой правки.

**T152 сделан не буквально по брифу, и это осознанно.** Бриф говорил «в состоянии и в базе хранить код». Код теперь и лежит в состоянии — в блоке `domain` файла проверки (`Inspection.kind`), и оттуда же его берут база и отпечаток. Но `meta.type`, шапку, которую движок читает сам и печатает партнёру, заполняет перевод этого кода на язык отчёта — потому что `engine/` чужой блок и трогать его нельзя, а запиши мы туда код, партнёр получил бы в шапке слово `planned`. Перевод берётся по тому же `meta.lang`, с которым движок эту шапку и напечатает, поэтому сопоставлять там строки не с чем: `TYPE_EN` в `engine/report.py` стал мёртвым грузом. **Что нужно от `engine-fix`:** научить `report.py` брать вид проверки кодом (из блока `domain` в состоянии) и снять `TYPE_EN` — тогда перевод в шапку уйдёт отсюда совсем. **Что нужно от `db`:** колонка `kind` теперь хранит код (`planned`), а не слово; отпечаток проверки (`src/db/fingerprint.py`) от этого меняется, и чтение (`src/db/queries.py`) отдаёт код — показывать его человеком должен тот, кто показывает.

`in_progress` — **девятая очередь готова и предъявлена к приёмке**: T162 (процент только в конце, D072), T139 (посмотреть записанное, не завершая), T128 в переоткрытой части (имя файла отчёта в 255 байт) и T158 (информационная часть перед сборкой отчёта, D069 и D070). Первая (T050-T054, T059, T063), вторая (T055-T058, T067, T068), третья (T108), четвёртая (T117) и пятая (T121) приняты раньше; шестая (T123, T124, T126, T127, T128), седьмая (T131) и восьмая (T126 переоткрыта, T147, T151, T153) предъявлены и ждут приёмки.

**Девятая очередь.** Порядок завершения проверки изменился: кнопка «Собрать отчёт» открывает информационную часть, а сборка идёт за её последним вопросом (T158). Это единственное место очереди, которое меняет существующий сценарий, поэтому им пришлось поправить пятнадцать чужих тестов — теперь они собирают отчёт общей оснасткой `bot_harness.build_report`, где нажатие кнопки и прохождение части стоят рядом. Одна часть требования D069 осталась незакрытой и лежит вне блока: кадр, приложенный к информационному полю, печатать нечем — движок хранит в `info` строку (пара к T159).

**Седьмая очередь, T131 (задача #83, находка блока infra).** Язык интерфейса до начала проверки перестал быть константой: `DEFAULT_UI_LANG = "ru"` осталось умолчанием, но поверх него встал параметр развёртывания `BOT_UI_LANG` (`texts.default_ui_lang`). Боевой стенд переменную не задаёт и говорит по-русски, как и раньше; демо-профиль в `docker-compose.yml` ставит себе `en` и становится английским целиком. Неизвестное значение — отказ на старте (`config._parse_ui_lang` → `BotConfigError`), а не молчаливый откат: опечатка в переменной демо иначе обнаружилась бы на показе.

Правки приветствия оказалось мало, и это выяснилось на живом прогоне демо, а не по коду: надписи кнопок мастера (`Новая проверка`, `Продолжить`, `Начать новую`, три вида проверки) стояли строками прямо в `keyboards.py` — единственные строки интерфейса мимо каталога. Английский текст с русской кнопкой под ним — та же поломка, только незаметнее, поэтому они переехали в каталог, а клавиатуры мастера приняли язык параметром.

Вид проверки при этом развели по двум языкам: `KIND_LABELS` был одной строкой на два разных употребления — надпись на кнопке (язык интерфейса) и значение, уезжающее в шапку отчёта партнёру (язык отчёта). Теперь `KIND_TITLES` + `kind_title(code, lang)`, и на кнопке стоит «Planned», а в русском отчёте — «Плановая». Побочно исправилось предсуществующее расхождение: до T131 английский отчёт получал в шапку русскую «Плановую».

Проверено прогоном 04.09 в своей рабочей копии: `make fmt lint types dead bounds` зелёные целиком (`mypy --strict` на 77 файлах, `vulture`, `deptry`, `lint-imports` — 2 контракта из 2), полный `make test` с подключённым Postgres — **1315 тестов, 0 пропущено, покрытие 98 %**, 3 мин 16 с. Без базы в оболочке тот же набор даёт 1150 пройденных и 165 пропущенных (весь блок `db`), и общее покрытие тогда 94 % — цифра о базе, а не о правке. Покрытие блока: `keyboards.py` и `texts.py` — 100 %, `config.py` — 99 %, `start.py` — 96 % (непокрытое — предсуществующие ветки «нажатие без сообщения»; новых непокрытых строк задача не добавила). `make regress` даёт прежние 97.5 %/A/5×D1 и 97.0 %/A/6×D1.

Проверка порчей: 10 порч, 10 адресных покраснений, восстановление копиями, снятыми до порчи; `texts.py`, `keyboards.py`, `config.py`, `routers/start.py`, `docker-compose.yml`, `.env.example` вернулись байт в байт по md5. Порчи снимали защиты по одной: чтение переменной (2 красных), молчаливый откат на русский (2), проверка на старте (2), кнопка входа строкой в коде (2), русский вид проверки на кнопке (2), кнопки продолжения строками (2), вид проверки в отчёт на языке интерфейса (2), молчаливый откат вида проверки (1), демо-профиль без переменной (1), переменная не описана в `.env.example` (1).

**Первый заход порчи был неэквивалентным, и это поймано, а не пропущено.** Прогон шёл в урезанном окружении (`PATH`, `HOME`), и в нём находился ДРУГОЙ `docker` — без плагина compose. Проверка демо-стенда падала на любой порче, включая те, к стенду не относящиеся, и была бы засчитана как поймавшая. Контрольный прогон на чистом коде в том же окружении показал тот же красный — порча переделана на полное окружение процесса.

Живой смоук демо 04.09, образом демо-стенда и окружением демо-профиля (`docker compose --profile demo run`), сеть подменена записывающей сессией — наружу не ушло ни одного сообщения. Приветствие: «Field audit bot. Tap “New inspection” to begin.», кнопка «New inspection»; дальше «Which pizzeria? Type the name.» → «Inspection type?» с кнопками «Planned / Repeat / Unscheduled» → «Inspection language? …» → «Inspection started. … Type: Planned». `/start` в чате с насеянной демо-проверкой: «This chat has an unfinished inspection…» с кнопками «Continue / Start a new one». Контрольный прогон тем же образом с пустой `BOT_UI_LANG` даёт прежний русский разговор целиком — боевого поведения задача не меняет.

**Шестая очередь.** Готова и предъявлена к приёмке раньше (T123, T124, T126, T127, T128 — расхождения, найденные сверкой со спекой).

**Пятая очередь, T121 (решение владельца D064).** Подтверждение нажатием снято с фиксации словами: сошлась сверка со списком нарушений — запись появляется сразу, `_try_fast` сам зовёт `_save`. Кнопки «Записать» больше нет (`FAST_CALLBACK`, `fast_keyboard` и текст `record.fast` удалены), обработчик `on_fast` удалён вместе с ней. Фиксация по кадру не тронута: «Разобрать?» → кандидаты → запись только по нажатию.

Взамен снятой кнопки показ записи собран `view.fixed_block` (текст `record.fixed`) и виден: строка записи, **вопрос пункта словами**, слова аудитора **целиком**, сработавшая строка карты. Под ним `fixed_keyboard` — четыре кнопки правки и «Разобрать моделью». Живой смоук: 452 знака при лимите телеграма 4096.

Границу решения пришлось определить самому, и она записана в «Решениях, принятые внутри блока»: подтверждение снято ровно там, где запись целиком собрана человеком (зона названа, строка карты произнесена, текстом записи стали слова аудитора), — то есть на сверке со словами. Разбор моделью и ручной перечень нажатие сохраняют: там нажатие не подтверждение, а выбор из нескольких кандидатов.

Оговорка владельцу про тихий промах проверена на живой карте: его собственный пример «кассовая зона, просрочка чизкейк» сегодня быстрым путём **не проходит вовсе** ни в одной зоне (строгий критерий из пяти условий не сходится) и уходит модели, как раньше. Класс риска это не снимает — на фразе, которая критерий проходит, промах возможен, — и именно против него в показе стоит формулировка пункта.

Проверено прогоном 03.09 в своей рабочей копии: `make check` зелёный целиком (формат, линт, `mypy --strict` на 74 файлах, **1215 тестов, 0 пропущено, покрытие 98 %**, `vulture`, `deptry`, `lint-imports` — 2 контракта из 2). `record.py`, `keyboards.py`, `pending.py`, `edit.py`, `finish.py`, `sidecar.py`, `texts.py`, `albums.py`, `material.py` — по 100 %. `make regress` даёт прежние 97.5 %/A/5×D1 и 97.0 %/A/6×D1. Число тестов не изменилось: `test_bot_fastpath.py` остался на 15 случаях (два про снятую кнопку убраны, два новых — про видимый показ и про сохранённое подтверждение по кадру), `test_bot_fastpath_material.py` — на пяти.

Проверка порчей: 8 порч, 8 адресных покраснений, восстановление копиями, снятыми до порчи (не `git checkout`); `record.py`, `view.py` и `keyboards.py` вернулись байт в байт по md5. Порчи снимали защиты по одной: блок показа возвращался к одной строке (8 красных), отказ движка становился тупиком (1), слова аудитора резались предпросмотром в 160 знаков (1), вопрос пункта пропадал из показа (1), выход к модели пропадал из-под записи (6), кнопка «Разобрать моделью» переставала помнить материал (2), фиксация словами отключалась целиком (14), источник записи подменялся на догадку по кадру (6).

Живой смоук 03.09: `getMe` на настоящем токене (`@dodo_as_bot`, id 8766820253), `python -m src.bot` поднимает long polling и останавливается по SIGTERM без остатка (процесс проверен по `ps`). Список разрешённых ID на время смоука подменён заведомо чужим, чтобы ни один хендлер не мог сработать: наружу не отправлено ни одного сообщения. Показ записи посмотрен глазами на настоящих методике и карте — 452 знака при лимите телеграма 4096.

**Четвёртая очередь, T117.** Быстрый путь подключён в разговор: `fast_path` зовётся в `_analyze` до `classify`, и на однозначных словах модель не зовётся вовсе — сообщения «Разбираю…» нет, кадр даже не скачивается. Рядом с ответом стоят слова аудитора **целиком** (не предпросмотром в 160 знаков) и сработавшая строка карты, рядом же — выход к модели. Фиксация идёт словами аудитора, источник — `SOURCE_COMMENT`. `reason` уходит в журнал и на экран не попадает никогда. Показ пункта кнопкой подтверждения, каким он был принят в этой очереди, заменён пятой (T121).

Проверено прогоном 03.09 в своей рабочей копии: `make check` зелёный целиком (формат, линт, `mypy --strict` на 72 файлах, **1024 теста, покрытие 98 %**, `vulture`, `deptry`, `lint-imports` — 2 контракта из 2). `record.py`, `keyboards.py`, `pending.py`, `edit.py`, `finish.py`, `sidecar.py` — по 100 %. `make regress` даёт прежние 97.5 %/A/5×D1 и 97.0 %/A/6×D1.

Проверка порчей: 16 порч, 16 адресных покраснений, восстановление копией файла, снятой до порчи; `record.py` возвращался байт в байт по md5. Две порчи из первого захода прошли зелёными и вскрыли ложные проверки — они переписаны, см. журнал.

Живой смоук 03.09: `getMe` на настоящем токене (`@dodo_as_bot`, id 8766820253), `python -m src.bot` поднимает long polling и останавливается по SIGTERM без остатка (процесс проверен по `ps`, а не по коду команды остановки). Наружу не отправлено ни одного сообщения.

Смоук быстрого пути глазами, с настоящими методикой и картой кадров: на фразу с двумя нарушениями (185 знаков) бот показывает её целиком, строку карты «Печь» отдельной строкой, пункт `CLN05 · D1 · Горячий цех` и три кнопки; сообщение 374 знака при лимите телеграма 4096. По «Записать» ложится запись `CLN05/D1/hot_kitchen` со словами аудитора и источником «со слов аудитора».

**Третья очередь, T108.** Источник записи бот больше не хранит у себя: он передаётся параметром `domain.add_finding(..., source=...)` при фиксации и читается из `Finding.source` при предвычитке. Из заметок бота источник убран целиком — вместе с `SOURCE_COMMENT`, `SOURCE_PHOTO`, `remember_source`, `forget_source` и полем `Notes.sources`; заметки помнят кадры (T068) и зону (D048), и это остаётся. Заметки, написанные до переезда, читаются по-прежнему: незнакомый ключ `sources` просто не читается, отказ означал бы, что начатую тогда проверку стало нечем завершить. Проверено прогоном в своей рабочей копии: `make check` зелёный целиком, **694 теста, покрытие 98 %**, `record.py`, `edit.py`, `finish.py`, `sidecar.py` — по 100 %; `make regress` даёт прежние 97.5 %/A/5×D1 и 97.0 %/A/6×D1.

Проверено прогоном 02.09 в своей рабочей копии: `make check` зелёный целиком (формат, линт, `mypy --strict`, **546 тестов, покрытие 98 %**, `vulture`, `deptry`, `lint-imports` — 2 контракта из 2). Роутеры второй очереди и заметки бота покрыты полностью: `record.py`, `edit.py`, `finish.py`, `sidecar.py` — по 100 %. `make regress` даёт прежние 97.5 %/A/5×D1 и 97.0 %/A/6×D1.

Живой смоук: `getMe` на настоящем токене (`@dodo_as_bot`), диспетчер собирается со всеми пятью роутерами в нужном порядке, `python -m src.bot` поднимает long polling и останавливается без остатка. Наружу не отправлено ни одного сообщения.

Смоук разговора целиком (кадр → «Разобрать» → фиксация → правка зоны → кадр без записи → `/finish` → PDF и письмо) прогнан с подменённым разбором и настоящими движком, оценкой и сборкой отчёта: PDF собрался под именем `Аудит Белград 2 - Владимир Гарро - 02.09.2026.pdf`, письмо пришло текстом.

## Как устроено

| Модуль | Что делает |
|---|---|
| `src/bot/app.py` | Сборка диспетчера и запуск long polling. Порядок роутеров задан здесь и важен |
| `src/bot/config.py` | Настройки из окружения: токен, разрешённые ID, режим, карта имён проверяющих |
| `src/bot/access.py` | Внешняя мидлварь доступа на message и callback_query |
| `src/bot/texts.py` | Каталог текстов интерфейса, язык — параметр. Строк в хендлерах нет. Язык стенда до начала проверки — `BOT_UI_LANG` (T131) |
| `src/bot/keyboards.py` | Все клавиатуры и коды кнопок. В `callback_data` едет код, не формулировка. Надписи — из каталога, язык параметром |
| `src/bot/states.py` | Состояния диалога: мастер начала проверки и ожидание новой формулировки |
| `src/bot/material.py` | Связывание кадров с комментарием тремя способами, FIFO по чату |
| `src/bot/albums.py` | Склейка альбома по `media_group_id` окном ожидания |
| `src/bot/sidecar.py` | Заметки бота файлом рядом с проверкой: присланные кадры и последняя зона |
| `src/bot/zones.py` | Зона из слов аудитора: имена зон из методики плюс произносимые формы |
| `src/bot/lang.py` | Языки чата, на которых нельзя упасть: испорченное состояние не отнимает речь |
| `src/bot/refusal.py` | Отказ движка человеческими словами: пункт, зона, занявшая пару запись |
| `src/bot/pending.py` | Что ждёт нажатия кнопки — только в памяти, и это осознанно |
| `src/bot/photos.py` | Скачивание кадров и голоса из телеграма; карта кадров для сборки отчёта |
| `src/bot/view.py` | Строки, которые читает аудитор: подтверждение, список записей, кандидаты |
| `src/bot/routers/start.py` | Мастер начала проверки и продолжение незавершённой |
| `src/bot/routers/material.py` | Приём кадров и комментариев; кто их разбирает — параметр |
| `src/bot/routers/record.py` | «Разобрать?», сверка со списком до модели, кандидаты кнопками, фиксация, ручной перечень страницами. Отсюда же в домен уходит предложение системы рядом с записью (T181) |
| `src/bot/routers/edit.py` | Правки записи кнопками и `/undo` |
| `src/bot/info.py` | Состав информационной части и разбор дат: какие поля спрашиваются и чем они разные |
| `src/bot/routers/info.py` | Информационная часть в конце проверки: вопросы, ответы, переход к сборке отчёта |
| `src/bot/routers/records.py` | Показ записанного посреди обхода: `/records`, он же первая часть итога. Кадры без записи (T068) и слова без кадра (T241) — здесь же |
| `src/bot/routers/finish.py` | Итог, предвычитка, кадры без записи и слова без кадра, PDF и письмо, слив завершённой проверки в историю |
| `src/bot/routers/mcp.py` | Пункт меню «Установка MCP»: готовая команда для терминала, `BOT_MCP_URL`, заглушка вместо токена |

### Решения, принятые внутри блока

**У кнопки зоны свой предел подписи, и он ровно вдвое больше общего (T140).**
Общий `BUTTON_TITLE_LIMIT` = 34 замерен под кнопку в ПОЛОВИНУ ряда: два
столбца делят ширину пополам. Зоны и стояли по две в ряд, и на боевой методике
самое длинное название не влезало — аудитор видел «…курьерская з…».

Ряд из одной кнопки — это те же две половины плюс промежуток между ними, то
есть текста влезает вдвое больше: `ZONE_TITLE_LIMIT` = 2 × 34 = 68, промежуток
в счёт не берётся намеренно. На боевой методике десять зон, самое длинное
название — 36 знаков по-русски и 32 по-английски: не режется ни одно, и
остаётся запас в 32 знака на то, что управляющая компания напишет позже.
Считано по самой методике, а не на глаз, и закреплено тестом на ней же
(`tests/test_bot_zone_buttons.py`, `requires_data`).

Плата — свиток из десяти рядов вместо пяти, и она осознанная: зону выбирают
раз на запись, а подпись читают каждый раз, одной рукой и в шуме. Предел при
этом не снят, а поднят: подпись, которая не влезла, обязана быть видна
обрезанной, иначе на её месте окажется полэкрана — это отдельный тест.

Порядок зон не тронут: он методический и станет маршрутным, когда придут
данные управляющей компании (T133). Клавиатура его только показывает — тоже
под тестом, чтобы будущий маршрут не пришлось искать по коду.

**Пометка занятости в ручном перечне живёт в ТЕКСТЕ, а не на кнопке (T173).**
Задача была сформулирована как разговор о виде перечня, и вот в чём разница.
Перечень модели — это СПИСОК СТРОК в сообщении плюс кнопки выбора; пометке
T137 там есть где стоять, и она стоит в строке пункта. Ручной перечень —
это ОДНИ КНОПКИ, а сообщение над ними до сих пор несло только номер страницы.
На кнопку отведено 34 знака под формулировку (`BUTTON_TITLE_LIMIT`, предел
замерен: дальше надпись разъезжается на телефоне в три строки), и любая
пометка внутри неё отнимает знаки у того, ради чего аудитор перечень и открыл.

Поэтому пометка занимает пустующее место — текст сообщения, — а связывает её с
кнопкой КОД пункта: он и так стоит на кнопке первым, и считать позиции
аудитору не приходится. Значок и оборот взяты те же, что у T137: случай один и
тот же, а вторая формулировка одного и того же разошлась бы с первой молча.

Названы только пункты текущей страницы: строка про пункт, которого на экране
нет, — шум, а на боевой методике перечень зоны это десять страниц, и полная
сводка занятых оказалась бы длиннее самой страницы.

Убрать занятый пункт из перечня вместо пометки — отвергнуто по тому же доводу,
по которому T137 выбрал пометку: это решение за человека и спрятанная часть
методики. Отказ движка тупиком не заканчивается — он приводит к кнопкам
занявшей записи (T127).

**Сырыми словами считается ВЕСЬ материал аудитора об этом кадре, каким он
пришёл (T183).** Подпись к кадру, отдельное сообщение следом и расшифровка
голоса — это три способа сказать одно и то же, и до разбора они доходят одной
строкой (`Proposal.note`). Разделять их незачем: для разбора промаха важно то,
на чём система принимала решение, а решала она по этой строке.

*Расшифровка голоса словами считается, хотя это машина.* Довод против —
расшифровка может ослышаться, и хранить её как «слова аудитора» не вполне
честно. Довод за сильнее: на точке говорят чаще, чем пишут, и без голоса
выборка потеряла бы большую часть себя; главное же — промах модели разбирают по
тому, что модель ВИДЕЛА, а видела она расшифровку. Пометки «это был голос» не
заводится: у неё нет потребителя, а источник записи (D044) уже отвечает на
вопрос, была ли речь вообще.

*Формулировка модели, вопрос пункта и текст правки словами НЕ считаются.*
Первые два — не речь человека вовсе. Третий сложнее: правка формулировки тоже
написана аудитором, но это другой речевой акт — сочинение отчёта партнёру, а не
описание нарушения на точке.

**Слова пишутся один раз и не меняются никогда — ни заменой, ни дописыванием
(T183).** Это показание о моменте, а не поле записи. Заменённые правкой, они
стирали бы сигнал ровно тогда, когда он появляется: смысл хранения в РАЗНИЦЕ
между сказанным и записанным. Дописанные — смешали бы два разных речевых акта,
и «это чистота» стало бы неотличимо от правки текста, то есть признак, ради
которого всё затевалось, снова перестал бы быть измеримым. Единственный
кажущийся встречный случай — «Разобрать моделью» — несёт ТОТ ЖЕ материал, и
дописывать там нечего.

Пусто в словах означает две вещи сразу: аудитор промолчал (разбор голого кадра)
либо запись начата до T183. Различаются они источником записи: у первой он
`photo`. Третьего значения не заводится — пустая строка и отсутствие ключа
означали бы одно и то же, а два способа сказать одно и то же дают два способа
прочитать это по-разному.

**Предложение системы — первый ответ модели, а не нажатая кнопка (T181).**
Разобрано выше, в статусе одиннадцатой очереди: это главное решение задачи, и
от него зависит, существует ли расхождение как измеримое явление вообще.

**T166 закрыт ответом «признак есть, надёжного признака нет, и складывать его
некуда».** Задача была исследовательской, и вот что выяснено — счётом, а не
прикидкой.

*Признак существует и он проверяемый.* Аудитор в примере владельца называет
**процесс**: «ГРЯЗЬ НА ПОЛКЕ В ГОРЯЧЕМ ЦЕХЕ, ЭТО ЧИСТОТА». «Чистота» — это
колонка `process_ru` методики (`data/checklist.csv`), и таких названий в боевой
методике ровно семь: Безопасность, Информация, Менеджмент, Пищевая
безопасность, Работа с продуктом, Техническое состояние, Чистота. Признак
двухчастный: названо имя процесса И оно связано с описанием связкой («это»,
«=», «→»). Имена обязаны читаться из данных на каждом обращении — по тому же
правилу, по которому там же читаются названия зон (`src/bot/zones.py`):
переименованный управляющей компанией процесс обязан начать узнаваться сам.

*Одного имени мало, и это очевидно на живых фразах.* «Нарушена пищевая
безопасность», «нет информации о сроках», «техническое состояние печи» —
обычные комментарии, и все три содержат название процесса. Связка признак
сильно сужает, но не делает надёжным: «просрочка на полке, это пищевая
безопасность» — одновременно и указание словарю, и обычное описание. По тексту
они неразличимы, потому что это один и тот же речевой акт.

*Мерить оказалось не на чем, и это и есть главная находка.* Замер по эталонным
проверкам (`examples/*/inspection.json`, 17 непустых текстовых полей записей):
название процесса не встречается **ни разу**, конструкция — **ни разу**. Но
выборка не та: в записях лежат отшлифованные формулировки, а признак живёт в
**сырых словах аудитора**, которых продукт не хранит нигде. Комментарий в
запись не пишется намеренно (движок печатает `comment` партнёру), а
`pending` умирает вместе с процессом. Единственный путь, где слова аудитора
доживают до записи, — быстрый путь, где они становятся её текстом; но фраза,
которой учат словарь, по определению быстрым путём не проходит — иначе учить
было бы нечему. **Долю ложных срабатываний посчитать не на чем, и пока её не
на чем посчитать, признак применять нельзя.**

*Два запрета, которые сильнее самого признака.* Первый: перехватывать поток
нельзя. Сочти бот такой комментарий указанием и пропусти обычный разбор —
аудитор, который имел в виду нарушение, потерял бы запись молча, а это ровно
тот отказ, против которого написаны D046 и D078. Что бы ни делалось, оно
делается **в дополнение** к обычному пути, никогда вместо. Второй: попадание
некуда положить. Место, которое построил T181, привязано к ЗАПИСИ
(`findings.suggested_*`), а указание записи может не породить вовсе. Строки на
комментарий у базы нет, и заводить её — работа блоков `db` и `mcp` (T165), за
границей этого блока.

*Что предлагается вместо.* Порядок обратный тому, в котором задача поставлена.
Сначала — **хранить сырые слова аудитора** рядом с проверкой: без них ни
признак не измерить, ни выборку для управляющей компании не собрать, и это
дешевле и полезнее детектора. Потом — сам детектор чистой функцией, читающей
имена процессов из методики, и он работает в дополнение к разбору, а не вместо.
И только потом, по собранным сырым комментариям, — замер доли ложных
срабатываний, до которого выборкой пользоваться нельзя.


**Язык стенда читается по месту, а не раздаётся аргументом через роутеры.**
`BOT_UI_LANG` разбирается в `texts.default_ui_lang()` и берётся оттуда же, где
раньше стояла константа. Альтернатива — протащить язык из `BotSettings` через
`build_dispatcher` в каждый роутер и в `chat_ui_lang(chat_id)` — выглядела
чище, но лгала бы: язык до начала проверки один на процесс, а параметр в пяти
подписях обещает, что он бывает разным у разных чатов одного бота. Проверка
значения при этом стоит не по месту чтения, а на старте (`load_bot_settings`):
до первого сообщения аудитору, а не вместо него.

**Умолчание осталось русским намеренно.** Соблазн был сделать умолчанием
английский — «продукт двуязычный, английский нейтральнее». Но боевой бот
говорит по-русски с T050, аудиторы на точке к этому привыкли, и смена языка
задним числом на молчащем стенде — ровно то молчаливое изменение поведения,
которого задача просила избежать. Английский включается одной строкой в
профиле, и демо это и делает.

**Демо ставит `en` в профиле, а не переменной `DEMO_BOT_UI_LANG` в `.env`.**
Остальные различия демо (токен, список ID, база) вынесены в `.env` под
префиксом `DEMO_`, потому что у них нет правильного значения — их знает только
запускающий. У языка демо правильное значение есть и оно одно: демо обязано
быть английским. Переменная в `.env` означала бы, что демо можно поднять
русским, забыв её заполнить, — и заметили бы это на показе.

**Вид проверки разведён по двум языкам, а не переведён на язык интерфейса.**
Одна строка `KIND_LABELS` служила и надписью кнопки, и значением, которое
уезжает в шапку отчёта партнёру. Перевести её «на язык интерфейса» значило бы
поставить в английский отчёт «Плановую» — что там до сих пор и стояло. Язык
отчёта известен на шаг позже кнопки, поэтому в состоянии мастера едет код, а
слово подставляется в самом конце, когда язык отчёта уже выбран.

**Названия языков на кнопке выбора не переводятся.** «Русский» остаётся
«Русским» и в английском интерфейсе демо: язык называют на нём самом — так
делают все, у кого есть переключатель языка, и так его находит глазами тот,
кому он нужен. Это единственная надпись мимо каталога текстов, и мимо она не
по недосмотру: перевода у неё нет по существу.

**Комментарий аудитора не попадает в отчёт партнёру.** Поле `comment` записи движок печатает в отчёте, а сказанное вслух на точке для партнёра не предназначено. В запись идёт только формулировка, собранная по правилам фиксации; сами слова аудитора остаются в переписке.

**Заметки бота лежат отдельным файлом, а не в `inspection.json`.** Список присланных кадров (T068) и последняя зона (D048) — сведения бота, а не проверки: в отчёт они не идут и партнёру не показываются. Класть их в поля движка значило бы либо засорить отчёт, либо завести вторую договорённость о формате состояния поверх чужого блока.

**Источник записи (D044) из заметок бота уехал — и это правильно.** Он казался сведением бота, пока читал его один бот. Как только источник понадобился в базе, стало видно: это свойство самой записи, а не переписки. Хранит его теперь блок `domain` внутри проверки (`add_finding(..., source=...)`, `Finding.source`), бот только называет источник при фиксации и читает при предвычитке. Второй копии в заметках нет намеренно: две копии одного факта расходятся, а спорили бы они о том, за какую формулировку отвечает аудитор. У проверок, начатых до переезда, источника в состоянии нет вовсе — такая запись читается как «неизвестно» и пометки не получает: выдать её за слова аудитора значило бы соврать, за догадку — оболгать.

**Что ждёт нажатия — только в памяти.** Предложения модели перезапуск не переживают намеренно: кнопка под ответом, которого больше никто не помнит, зафиксировала бы запись по устаревшему разбору. Нажатие на забытую кнопку — нормальный исход, бот отвечает «предложение устарело».

**Формулировка при ручном выборе без модели** берётся из комментария аудитора, а при его отсутствии — из вопроса пункта, и тут же правится кнопкой «Формулировка». Отдельный шаг «введите формулировку» на точке дороже: аудитор идёт с занятыми руками, а исправить он может сразу же и при завершении.

**Слова аудитора при фиксации словами не режутся предпросмотром.** Везде ещё комментарий сжимается до 160 знаков (`view.NOTE_PREVIEW_LIMIT`): аудитору нужна отметка, что бот услышал. Здесь ровно наоборот, и это главное в T117. Сверка отвечает по одной сработавшей строке карты, а второе нарушение фразы (правило 11) обычно стоит в конце — предпросмотр спрятал бы именно его, и потеря стала бы молчаливой. С T121 цена ещё выше: подтверждения нет, и своих слов рядом с записью аудитор больше нигде не увидит. Потолок всё же есть (`FAST_NOTE_LIMIT = 3000`), и причина у него одна: сообщение обязано влезть в лимит телеграма в 4096 знаков вместе с пунктом, строкой карты и подсказкой. Живым словам он не мешает — самая длинная формулировка боевых проверок 204 знака при медиане 124.

**Вопрос чек-листа при фиксации словами — на языке интерфейса, а не отчёта.** Ручной перечень (T034) берёт заголовки на языке отчёта намеренно: там заголовок пункта может стать текстом записи и уйти партнёру. Здесь стать текстом записи ему нечем — им всегда становятся слова аудитора, — поэтому заголовок остаётся чистой строкой интерфейса и слушается языка интерфейса.

**Граница D064 проходит по сверке со словами, а не по всему, что аудитор написал.** Решение снимает подтверждение с «фиксации словами» и оставляет его «для разбора кадра». Единственный путь, где запись целиком собрана человеком, — сверка со списком нарушений: зону назвал человек, строку карты он произнёс целиком, а текстом записи становятся его собственные слова. Разбор моделью, даже начатый с текста, устроен иначе: пункт и формулировку предлагает модель, кандидатов несколько, и нажатие там не подтверждение, а выбор — снять его было бы нечем. Поэтому изменился ровно один переход, а `classify`, ручной перечень и путь кадра остались как были.

**Формулировка пункта показывается при фиксации словами — вопреки «не пересказывать пункт».** Правило шага 5 `docs/06-mvp-bot.md` написано для записи, подтверждённой нажатием: пункт аудитор уже прочитал на кнопке. Без кнопки читать его негде, а строка `#1 CLN02 · D1 · Кассовая зона · 99,5 %` промах не показывает — код глазами не читается, формулировка читается. Так владелец риск и объяснял: «кассовая зона, просрочка чизкейк» напрашивается на `CLN02` («оборудование в зоне мойки без загрязнений») вместо верного `PRD10`, и разницу видно только по формулировке. Отступление одно и только здесь; таблицы после каждого кадра по-прежнему нет.

**«Разобрать моделью» переехало из предложения под саму запись — и это не оформление.** Правка записи (T056) меняет зону, класс, формулировку и удаляет запись, но **не код пункта**. Значит, промах по коду чинится только разбором заново, а те же слова снова поднимут тот же пункт: удаление и повтор дают петлю. Кнопка рядом с записью — единственный выход из неё. Запись при этом не трогается: разбор предлагает кандидатов, а прежнюю запись убирает аудитор кнопкой «Удалить». Удалять её молча было бы хуже — отказ модели оставил бы человека и без записи, и без разбора.

**Отказ движка при фиксации словами передаёт материал модели, а не заканчивается ничем.** Пара «пункт + зона» занимается по-настоящему часто: тот же пункт в той же зоне аудитор снимает дважды за обход. Раньше отказ приходил на нажатие, и рядом оставались кнопки; теперь нажатия нет, и молчаливый тупик был бы прямым следствием снятой кнопки. Причина названа строкой «Не записал: …», после чего материал идёт обычным разбором, где пункт можно выбрать другой.

**Предложение после фиксации словами не забирается из памяти.** Всюду ещё `take_proposal` вызывается сразу: запись сделана, второй раз по тем же кнопкам нельзя. Здесь предложение живёт дальше — ровно ради «Разобрать моделью» под записью. Опасных кнопок это не открывает: под записью показаны только правки и выход к модели, а нажатие на кандидата или ручной перечень из прошлого сообщения упирается в пустые списки и отвечает «предложение устарело».

**Кнопки «Не записывать» под записью нет.** Она была отказом от предложения, а отказываться больше не от чего: запись уже сделана, и её отменяет «Удалить» — та же кнопка, что у любой другой записи. Оставить обе значило бы дать два разных способа убрать одно и то же.

**`fast_path` вызывается через `asyncio.to_thread`, хотя ни сети, ни подпроцесса в нём нет.** Граница проходит не по «движок или не движок», а по цене: вызов читает с диска методику и карту кадров и стоит 2.3 мс на сработавшем комментарии против 0.046 мс у `get_state`, который в цикле оставлен. На двадцати аудиторах это та же растущая очередь, из-за которой в поток уехали вызовы движка (T101). Инвариант закреплён тем же тестом `tests/test_bot_event_loop.py`, и его смысл расширен с «дорогие вызовы домена» на «дорогие вызовы».

**На кадре без слов сверка не зовётся вовсе.** Формально она и так ответила бы отказом — сверять пустые слова не с чем. Но это был бы отказ ценой чтения двух файлов с диска на каждое нажатие «Разобрать» плюс строка в журнале о том, что аудитор ничего не говорил. Проверяется явно: тест считает вызовы, не подменяя саму сверку.

**Память о зоне — догадка, и она названа вслух на КАЖДОМ пути записи (T156).** D048 разрешает подставлять последнюю названную зону, D064 снял подтверждение с фиксации словами — вместе это давало сообщение «записал по вашим словам» под записью, зона которой из слов не бралась вовсе. Врать тут дороже, чем шуметь: код пункта аудитор глазами не читает, а название зоны читает, и «Горячий цех» под словами «в зале» он заметит, только если сказать, откуда зона взялась.

Оговорка стояла сначала только там, где подтверждения нет, — довод был «на подтверждённой записи зону аудитор прочитал на кнопке». Довод оказался слабее правила: зону аудитор действительно видит, но откуда она взялась — не видит нигде, а вычет уезжает партнёру именно в неё. Пометка, которая появляется через раз, к тому же перестаёт что-либо значить. Поэтому она одна на все пути и с одним условием: **зону не назвал человек — ни словами комментария, ни кнопкой зоны — и записана та самая зона, которую бот подставил из памяти.** Своя зона модели догадкой не считается: это её ответ, а не прошлая запись.

**Произносимые формы зон живут в коде, названия — в методике.** `data/zones.csv` правит управляющая компания, и переименованная зона обязана начать узнаваться сама, поэтому имена читаются оттуда на каждом обращении. Но «Зал ресторана / касса» вслух не произносят — говорят «в зале», и такой формы в методике нет и не появится. Список коротких форм в `src/bot/zones.py` намеренно крошечный, и источник у каждой строки только один из двух: слова владельца или формулировка боевой проверки в `examples/`. Слов «холодильник» и «морозильный шкаф» там нет специально — это оборудование, и в боевых проверках оно стоит в горячем цехе, а не в одноимённой камере.

**Отчёт не закрывает проверку.** Признака «завершена» у движка нет, и заводить его в боте — значит завести вторую правду о состоянии. После отчёта проверка остаётся открытой, и `/start` по-прежнему предложит продолжить или начать новую.

**«Установка MCP» не печатает токен, и это единственный честный вариант, а не осторожность (T209).** У соседнего продукта токен вписан в строку установки потому, что он там ЛИЧНЫЙ по устройству: бот выпускает его на конкретный Telegram ID, хранит хэшем, перевыпуск убивает старый. Здесь всё иначе (`src/mcp/config.py`): токены задаёт человек в `.env` записями «арендатор=токен», принадлежат они СТОРОНЕ, открывают всю её историю проверок, а отзыв — это правка `.env` и перезапуск сервера, то есть отъём доступа у всех, кто им пользуется. Про собеседника бот знает ровно одно: его ID стоит в общем списке `ALLOWED_TELEGRAM_IDS`. Связи «этот человек — этот токен» у него нет ни в каком виде, поэтому любой напечатанный токен был бы выдан наугад: одному он предназначен, остальным — нет, а различить их нечем. Плюс к тому напечатанное остаётся в переписке навсегда и пересылается одним движением. Поэтому в команде стоит заглушка, а рядом сказано словами, что токен личный, откуда его берут и что пересылать его нельзя. Личные токены, выпускаемые ботом на человека, — своя задача со своим хранилищем, хэшированием и отзывом; до неё команда честнее недоговорит, чем отдаст чужой доступ.

**Адрес MCP-сервера бот берёт из своей переменной, а не из `MCP_HOST`/`MCP_PORT` (T209).** Причин две. Блоки `bot` и `mcp` — пиры и друг друга не импортируют (`pyproject.toml`), а вторая важнее первой: сервер слушает петлю и наружу не публикуется, значит, в команде у человека стоит адрес ЕГО машины или его туннеля, а не то, на чём сервер поднялся. Это разные факты, и копией одного другой не является. Переменной нет — на месте адреса такая же заглушка, как у токена: подставить петлю молча значило бы выдать догадку за настройку стенда.

**Одиннадцатая очередь, состояние волны на 05.09.2026.** Копия `feat/bot14`. Машина под нагрузкой 180–330 при обычных единицах (грузит не стройка, а браузер и редактор владельца), поэтому полный набор не гонялся по ходу — только адресные прогоны затронутых файлов, по одному за раз. Исполнителям не отдано ничего: T189 — про границу «что можно повторить, не сделав из записи дубль», T155 — про то, что защищать и на каком уровне; в обеих задачах цена ошибки — потерянная или удвоенная запись аудитора, и готового контракта, который можно отдать, тут не было.

## Журнал работы

| Задача | Кто делал | Состояние | Заметка |
|---|---|---|---|
| — | сам | — | Начало: `feat/bot` отставала от `main` на 20 коммитов (принятый `report`, требования созвона 01.09). Влил `main` в ветку до первой строки кода — иначе блок писался бы под отменённые требования |
| T050 | сам (первая сессия) | готово | Каркас на aiogram 3.31: `config`, `access` мидлварью на message и callback, `texts` с языком-параметром, `app.build_dispatcher`. Коммит `652c7fe` |
| T051, T052, T063 | сам (первая сессия) | готово | Мастер начала проверки и продолжение незавершённой, `routers/start.py`. Коммит `652c7fe` |
| T053 | сам (первая сессия) | готово | Три способа связать комментарий с кадром: `material.py` юнит-тестами, `routers/material.py` через настоящий диспетчер. Коммиты `652c7fe`, `7a4e1ae` |
| T054 | сам (первая сессия), тесты добиты диспетчером | готово | Склейка альбома по `media_group_id`: `albums.py` плюс `tests/test_bot_album_router.py`. Коммиты `652c7fe`, `7e59061` |
| T059 | сам (обе сессии) | готово | Последовательная обработка обновлений (`handle_as_tasks=False`) плюс FIFO по чату. Тесты пачки — исполнитель, см. ниже |
| — | сам (вторая сессия) | готово | Разбор состояния после обрыва по лимиту: гейты (`ruff`, `mypy --strict`, `vulture`, `deptry`, `lint-imports`) зелёные, 289 тестов, покрытие 94 % — базовая точка, от которой шла работа второй сессии |
| T059 (тесты пачки) | исполнитель | готово | `tests/test_bot_burst.py` — семь случаев доставки пачкой после потери связи. Отдано исполнителю: контракт готов, случаи перечислены, образец рядом, объём под сотню строк одинаковых по форме тестов |
| T050–T052 (отказы мастера) | исполнитель | готово | `tests/test_bot_start_errors.py` — непокрытые ветки отказов: отказ движка, кадр вместо названия, чужие коды кнопок, пропавшая проверка |
| T050, T059 (точка входа) | сам | готово | `tests/test_bot_app.py`: `start_polling` проверяет методику до первого сообщения и поднимает polling с `handle_as_tasks=False`. Не отдано исполнителю — это решение T059, а не механика |
| T053, T054 (подтверждение приёма) | сам | готово | `tests/test_bot_confirmation.py`: настоящий `confirm_link` — голосовой комментарий, обрезка длинного, отказ фоновой задачи альбома в журнале. Не отдано исполнителю: три теста, запуск подмастерья дороже |
| — | сам | готово | Живой смоук против Telegram: `getMe` на настоящем токене и запуск `python -m src.bot` на семь секунд. Наружу ничего не отправлялось, ни одного сообщения никому |
| — | сам | найдено | `.env` никем не читается: `load_bot_settings` смотрит `os.environ`, а перенести туда файл некому — ни `python-dotenv`, ни цели `run`. Документированный запуск `python -m src.bot` без экспорта падает. Чинить не стал: `pyproject.toml` и `Makefile` общие для блоков. Задача #75 |
| — | сам | готово | Вторая сессия начата вливанием `main`: без него блок писался бы против непринятого `recognize`. Слияние прошло без конфликтов, база — 411 тестов, покрытие 96 % |
| — | сам | найдено | В `.venv` рабочей копии не было `openai`, который принёс слитый `recognize`, — 4 файла тестов не собирались. Поставил в своё окружение (`pyproject.toml` его уже объявляет, общий файл не трогал) |
| T067 | сам | готово | «Разобрать?» кнопкой на кадр без комментария; комментарий следом снимает вопрос и гасит кнопку. `routers/record.py`, `pending.py`. Не отдано исполнителю: это ядро решения D046, а не механика |
| T055 | сам | готово | Кандидаты кнопками, фиксация по нажатию, подтверждение одной строкой. Там же — деградация на `manual_candidates` страницами по 8 при недоступной модели |
| T057 | сам | готово | Замеры идут тем же путём, отличаются классом `D0` и словом «замер» в подтверждении. Блок `info` движка не используется — проверено чтением сырого `inspection.json` в тесте |
| T056 | сам | готово | `routers/edit.py`: кнопки «Зона», «Класс», «Формулировка», «Удалить» под подтверждением плюс `/undo`. Классы предлагаются только разрешённые пункту, процент — из `domain.score()` |
| T058, T068 | сам | готово | `routers/finish.py`: итог, список зафиксированного с пометкой источника (D044), кадры без записи, затем PDF файлом и письмо сообщением. Потеря кадра оставляет решение аудитору |
| — (заметки бота) | исполнитель | готово | `src/bot/sidecar.py` и 18 тестов к нему: источник записи, присланные кадры, последняя зона — файлом рядом с проверкой. Отдано исполнителю: контракт расписан целиком, работа механическая (чистое хранилище JSON), два файла, ~200 строк. За собой оставил `BotNotesError` и правку общей пустышки `Notes` (общий экземпляр с изменяемым словарём протёк бы между чатами) |
| T055, T067 (тесты) | сам | готово | `tests/test_bot_record_router.py`, 18 случаев. Не отдано исполнителю: два запрета (модель не звать без нажатия, запись не создавать без подтверждения) — смысл блока, и формулировать их проверку надо самому |
| T058, T068 (тесты) | сам | готово | `tests/test_bot_finish_router.py`, 12 случаев, включая настоящую сборку PDF через движок и WeasyPrint: файл доезжает документом с прежним именем |
| T056 (тесты) | исполнитель | готово | `tests/test_bot_edit_router.py`, 16 случаев по готовому списку. Отдано исполнителю: код написан и стабилен, случаи перечислены, образец рядом |
| T229 | сам | готово | Придержанные слова: вторая очередь в `ChatMaterialQueue`, ожидание вместо отказа в `routers/material.py`, ключ `material.waiting_photo` вместо снятого `material.no_photo`. Не отдано исполнителю: это отмена части D078, а не механика. Коммит `12d3a75` |
| T229 (правка пяти существующих тестов) | исполнитель | готово | Пять файлов, ссылавшихся на снятый ключ `material.no_photo`, приведены к новому поведению. Отдано исполнителю: контракт готов целиком (что было, что стало, где именно), работа механическая, образец нового поведения лежал рядом готовым |
| T230 | сам | готово | Отбивка о сохранении одна на все пути добавления: ключ `record.stored`, `view.stored_headline`, параметр `{stored}` в трёх показах. Коммит `679b968` |
| T231 | сам | готово | Ответ, назвавший только зону, правит зону записи. Вызов сверки переехал из `_try_fast` в `analyze`: её причину отказа читает правка зоны. Коммит `bc749f7` |
| T232 | сам | готово | Предел разбираемой пачки `BATCH_LIMIT = 5` и отдельная отбивка про неразобранные кадры. Коммит `0d09990` |
| T034, photos (тесты) | исполнитель | готово | `tests/test_bot_record_manual.py` и `tests/test_bot_photos.py`: непокрытые ветки ручного перечня страницами и отказов скачивания |
| — | сам | найдено и починено | Смоук всего разговора целиком (кадр → разбор → правка → отчёт) вскрыл, что правка зоны у записи `D0` теряла пометку «замер»: строка правки собиралась одним шаблоном на все классы. Тестами не ловилось — видно только глазами. Коммит `b0ad274` |
| — | сам | готово | Живой смоук: `getMe` на настоящем токене (`@dodo_as_bot`), сборка диспетчера со всеми пятью роутерами в нужном порядке и `python -m src.bot` на семь секунд с чистой остановкой. Наружу не отправлено ни одного сообщения |
| — | сам | готово | Возврат с приёмки: покрытие 95 % против 96 % на прошлой. Закрыты пути отказа, а не дописаны тесты ради процента: нажатие без сообщения по всем пятнадцати кнопкам, испорченный код кнопки, устаревшее предложение, не скачавшееся голосовое, не прикрепившийся кадр, отказ телеграма снять кнопку, отказ движка на удаление, исчезнувшая между правкой и ответом запись, отчёт без проверки, письмо после уже отданного PDF, испорченный список кадров и сорванная запись заметок. Каждый тест проверен порчей: защита снималась по одной, тест краснел, файл восстанавливался из копии, снятой до порчи (не через `git checkout`). Итог: **546 тестов, покрытие 98 %**, четыре названных модуля — по 100 % |
| T108 | сам | готово | Источник записи передаётся параметром `domain.add_finding(..., source=...)`, а не отдельным вызовом следом; показ при предвычитке — из `Finding.source`. Из `sidecar` убрано только переехавшее: `SOURCE_COMMENT`, `SOURCE_PHOTO`, `SOURCES`, `Notes.sources`, `remember_source`, `forget_source`, разбор ключа `sources` при чтении. Кадры (T068) и зона (D048) остались — это другая механика. `sidecar.forget_source` из `routers/edit.py` убран как лишний: удаление записи уносит источник вместе с ней (`domain.drop_finding`). Не отдано исполнителю: правка мелкая (пять файлов, десятки строк), и её суть — граница между блоками, а не механика; контракт исполнителю пришлось бы расписывать дольше, чем сделать |
| T108 (тесты) | сам | готово | Один сквозной от кадра до `/finish` (`test_the_source_set_at_recording_reaches_the_proofreading`): проверяется не «где-нибудь есть источник», а та самая строка предвычитки, которую читает аудитор. Плюс переживание сброса заметок бота, запись без источника при предвычитке, пометка не переезжает на соседа после удаления, старые заметки с ключом `sources` читаются. Каждый проверен порчей — защита снималась по одной, тест краснел, файл восстанавливался копией, снятой до порчи (не `git checkout`) |
| T108 (счёт тестов) | сам | — | 696 → 694: шесть тестов `test_bot_sidecar.py` проверяли хранение источника в заметках, а хранения этого больше нет; вместо них четыре новых по существу. Покрытие осталось 98 %, четыре модуля роутеров и заметок — по 100 % |
| T117 | сам | готово | Сверка со списком нарушений до модели: `fast_path` в `_analyze` перед `classify`, показ пункта с словами аудитора и строкой карты, кнопки «записать» и «разобрать моделью», фиксация словами аудитора. Не отдано исполнителю: задача — одно связанное решение (порядок вызовов, что показываем, что пишем в запись), а её остаточный риск — правило 11, и формулировать его проверку надо самому |
| T117 (тесты) | сам | готово | `tests/test_bot_fastpath.py`, 15 случаев. Главный — фраза с двумя нарушениями: слова обязаны быть показаны целиком, а не покрытой частью |
| T117 (тесты на видах материала) | исполнитель | готово | `tests/test_bot_fastpath_material.py`, 5 случаев: быстрый путь на пяти способах прислать материал (подпись, сообщение следом, ответ на кадр, голосовое, альбом). Отдано: контракт расписан целиком, случаи перечислены, образец рядом, работа механическая — пять одинаковых по форме тестов, ни миграций, ни политик, ни входа, ни денег. Принято не по отчёту: одна из трёх его порч воспроизведена своими руками (`source=proposal.source` → `SOURCE_PHOTO`), покраснели все пять, `record.py` вернулся байт в байт |
| T117 (`.vulture-ignore`) | сам | готово | `on_fast` и `on_model` в белый список к тридцати таким же: имена хендлеров aiogram читает только декоратор. Поимённо, а не маской — как заведено в файле |
| T117 (каталог текстов) | сам | готово | Проверка, что наборы параметров строки совпадают во всех языках. Расхождение видит только второй язык: русский аудитор ничего не заметит, английский получит отказ каталога вместо ответа. У быстрого пути шесть параметров в одном сообщении — самая длинная строка каталога, и цена такой ошибки выросла. Проверено порчей английской строки |
| — | сам | найдено и починено | Проверка порчей вскрыла две зелёные на сломанном коде проверки. Первая искала подстроку «Печь» — её давал и заголовок пункта, поэтому пустая строка карты проверку не роняла; сообщение теперь сверяется целиком. Вторая: нажатие кнопки быстрого пути поверх живых кандидатов модели не проверял никто, а без проверки на сам пункт фиксация уходила в движок с `None` вместо кода. 16 порч из 16 краснеют адресно |
| T121 | сам | готово | Снятие подтверждения при фиксации словами (D064): `_try_fast` сам зовёт `_save`, кнопка «Записать» и обработчик `on_fast` удалены, `record.fast` заменён на `record.fixed`, `fast_keyboard` — на `fixed_keyboard`. Не отдано исполнителю: задача не механическая — в ней три решения, которые надо было принять и обосновать (где именно проходит граница D064, чем заменить кнопку, что делать с отказом движка), а правка кода при этом мелкая |
| T121 (показ записи) | сам | готово | `view.fixed_block`: строка записи, вопрос пункта словами, слова аудитора целиком, строка карты. Формулировка пункта показывается вопреки «не пересказывать пункт» (`docs/06-mvp-bot.md`, шаг 5) — намеренно и только здесь: то правило написано для записи, пункт которой аудитор прочитал на кнопке, а кнопки больше нет |
| T121 (выход к модели) | сам | найдено по ходу | Правка записи не меняет код пункта, а те же слова снова поднимут тот же пункт — удаление и повтор дают петлю. Поэтому «Разобрать моделью» переехало из предложения под саму запись. Не было бы этого разбора — снятие кнопки создало бы тупик, которого до T121 не существовало |
| T121 (отказ движка) | сам | готово | Пара «пункт + зона» занимается часто. Раньше отказ приходил на нажатие и рядом оставались кнопки; теперь причина названа, а материал уходит модели — иначе снятая кнопка оборачивалась бы молчаливым тупиком |
| T121 (тесты) | сам | готово | `tests/test_bot_fastpath.py` переписан под новое поведение, 15 случаев. Не отдано исполнителю: половина проверок — это ровно то, что решение меняет и чем оно рискует (запись без нажатия, видимость промаха, сохранённое подтверждение по кадру), формулировать их надо самому |
| T121 (тесты на видах материала) | исполнитель | готово | `tests/test_bot_fastpath_material.py`, те же 5 случаев под новое поведение: нажатие убрано, две прежние проверки слились в одну. Отдано: контракт расписан целиком, образец рядом (переписанный сосед), работа механическая — пять одинаковых по форме тестов, ни миграций, ни политик, ни входа, ни денег. Принято не по отчёту: обе его порчи воспроизведены своими руками в общем прогоне порчей (№8 — подмена источника, №1 — потеря блока показа), покраснели все пять; `record.py` вернулся байт в байт по md5 |
| T121 | исполнитель | найдено и починено | Тест альбома держался на `asyncio.sleep(0.1)`. Раньше закрытие альбома только показывало кнопку и до движка не доходило, теперь оно само зовёт `_save` — два подпроцесса (`add_finding`, `score`) при окне альбома 0.01 с. Фиксированная пауза стала гонкой и падала стабильно, а не изредка. Заменена опросом до появления сообщения (последний шаг `_save`), а не до появления записи: между ними запись ещё без прикреплённых кадров, и проверка кадров ловила бы промежуточное состояние |
| T121 (порча) | сам | готово | 8 порч, 8 адресных покраснений, восстановление копиями, снятыми до порчи; `record.py`, `view.py`, `keyboards.py` вернулись байт в байт по md5. Гонялось с `PYTHONDONTWRITEBYTECODE=1`: без него порча того же размера, живущая меньше секунды, подбирает старый байткод |
| T121 (найдено, не наше) | сам | найдено | `docs/forge/blocks/recognize.md` в требованиях к боту всё ещё говорит «показывать рядом с кнопкой подтверждения» и «фиксировать по подтверждению». Суть требований соблюдена (слова и строка карты видны, выход к модели есть), устарела формулировка. Файл чужого блока — не правил, оставляю диспетчеру |
| T121 (покрытие) | сам | найдено | `view.py` — 98 %: не покрыта ветка пометки «зона не из списка пункта» в `confirm_line`. Пробел предсуществующий, к T121 отношения не имеет (число непокрытых строк по проекту не изменилось: 64 и до, и после) |
| T123 | сам | готово | Слив завершённой проверки в историю: `archive` в `routers/finish.py` зовёт `db.push_inspection` и следом `db.upload_photos`, картой кадров — той же, что скачана для отчёта. Не отдано исполнителю: это граница между блоками и порядок, в котором аудитор получает отчёт раньше базы, а не механика. Функции блока `db` были написаны и работали — их просто никто не звал, и `inspections` стояла пустой |
| T123 (порядок и отказы) | сам | готово | Три исхода разведены намеренно: `ConfigError` — работы без базы, строка в журнал и ни слова аудитору; прочий `DbError` — сообщение «история не сохранена» с прямым советом не начинать новую проверку поверх этой; успех — молча. Временная папка кадров расширена до конца работы: выгрузка читает то, что уже скачано, а второй поход в телеграм стоил бы лимитов |
| T123 (тесты) | сам | готово | `tests/test_bot_archive.py`, 7 случаев. Главный — сквозной через настоящий диспетчер: запись нажатием, отчёт кнопкой, а в базу тест только смотрит. Тест, который зовёт `push_inspection` сам, этой задачи не проверяет вовсе |
| T123 (порча) | сам | готово | 5 порч, все адресные: снятый вызов слива (6 из 7 краснеют), слив перед отдачей PDF, молчание при отказе базы, сообщение о сбое при незаданном `DATABASE_URL`, пропуск выгрузки кадров. Гонялось с `PYTHONDONTWRITEBYTECODE=1`; `finish.py` вернулся байт в байт по md5 |
| T123 (герметичность прогона) | сам | найдено и починено | С этой задачи продукт ходит в базу сам — и любой тест бота, доводящий разговор до отчёта, при выставленной в оболочке `DATABASE_URL` начал бы писать в базу разработчика. Набор вёл бы себя по-разному с базой и без неё — ровно тот разлад, который сверка уже ловила. Переменная снимается автоматической фикстурой `tests/conftest.py` и выдаётся только через `db_env`/`pg_dsn`; строка запускающего снимается один раз при сборе (`BASE_DSN`) |
| T124 | сам | готово | Порядок «слова, потом память» в `_analyze`: `zones.zone_from_words(note)` первым, `sidecar.read(chat_id).zone` — только если о зоне в этих словах не сказано. Не отдано исполнителю: задача целиком про границу источника зоны — что считать словами аудитора, а что догадкой, — и про то, как это показать; правка кода при этом в три строки |
| T124 (`src/bot/zones.py`) | сам | готово | Имена зон читаются из методики на каждом обращении, в коде — только произносимые формы («зал», «касса», «тестомес», «морозилка») с источником на каждую строку. Совпадение по имени целиком, длинное сильнее короткого: «морозильный шкаф в горячем цеху» — это горячий цех, а не морозильная камера. Названы две зоны — молчание, а не выбор за человека |
| T124 (честность показа) | сам | найдено по ходу | Само сообщение о записи было неправдой: «✅ Записал сразу, по вашим словам», а зона в этой записи из слов не бралась вовсе. Одного порядка вызовов мало — зона из памяти теперь названа догадкой прямо под строкой записи, и только там, где подтверждения нет (D064). На подтверждённой записи оговорки нет: там зону аудитор прочитал на кнопке |
| T124 (тесты) | сам | готово | `tests/test_bot_zone_from_words.py`, 32 случая: 20 фраз разбора зоны (все — из боевых проверок `examples/` и слов владельца), четыре без зоны, неоднозначность, длина имени, зоны только из методики, плюс пять через настоящий диспетчер. Три чужих теста поправлены по существу, а не подогнаны: их материал теперь называет зону и уходит быстрым путём, поэтому зона из фраз убрана — иначе они проверяли бы не то, ради чего написаны |
| T124 (порча) | сам | готово | 5 порч, все адресные: обратный порядок «память впереди слов», зона всегда «названа словами», выбор за аудитора при двух зонах, совпадение по пересечению вместо имени целиком, пустой словарь произносимых форм. Отдельно замечено: `zones.py` — новый файл, и `git diff` на нём молчит, поэтому попадание порчи проверялось счётом вхождений в самом файле, а не диффом |
| T126 | сам | готово | Глобальный обработчик `on_unexpected_error` у диспетчера: без него aiogram глотал неперехваченное исключение, и на испорченном `inspection.json` бот молчал на каждый вход. `/start` вынут из-под общего отказа отдельной веткой — он и есть выход из тупика, и молчаливой перезаписи в нём нет: новую проверку заводит человек кнопкой. Не отдано исполнителю: это вход в продукт и последний рубеж обработки ошибок |
| T126 (`src/bot/lang.py`) | сам | найдено по ходу | Обработчика было мало: язык интерфейса живёт в самой проверке, и `chat_ui_lang` падал на том же испорченном файле — то есть бот не мог даже выбрать язык, которым сказать о поломке. Чтение состояния защищено узко и только ради языка; работа, которой состояние нужно по-настоящему, падает как падала. Заодно ушли четыре копии `chat_ui_lang` из роутеров |
| T126 (тесты, мои 5) | сам | готово | `tests/test_bot_error_handler.py`: выход через `/start`, кнопка из тупика ведёт дальше, испорченный файл не переписан молча (T052), ответ на кадр, разделение «в чат причина — в журнал разбор». Проверено порчей: снятая регистрация обработчика и `/start` без своей ветки краснеют адресно |
| T126 (тесты, остальные входы) | исполнитель | готово | Дописаны случаи по остальным входам и краевым: `/finish`, `/undo`, кнопка правки, текстовый комментарий, нажатие без сообщения, отказ телеграма на само сообщение о сбое. Отдано: код готов и стабилен, контракт расписан целиком, случаи перечислены, образец рядом — пять моих тестов в том же файле; ни миграций, ни политик, ни входа, ни денег |
| T127 | сам | готово | `src/bot/refusal.py`: отказ движка разбирается. Пункт — вопросом чек-листа, зона — названием, оба на языке интерфейса; занятая пара «пункт + зона» опознаётся по самой проверке, а не разбором текста движка — парсить чужое сообщение значило бы завести вторую копию его формата. Текст движка уходит в журнал целиком. Не отдано исполнителю: задача — решение о том, что аудитору показывать вместо чужого сообщения, а не механика |
| T127 (правка тоже) | сам | найдено по ходу | Тот же совет с командной строкой приходил и на правку: смена зоны у записи упирается в ту же занятую пару (`engine/audit.py: cmd_edit` зовёт тот же `check_pair_free`). Чинить только фиксацию значило бы оставить дефект на самом частом способе в него упереться. Удалению дан свой текст: пункта и зоны у него нет, называть нечего |
| T127 (тесты) | сам | готово | `tests/test_bot_refusal.py`, 8 случаев: нет командной строки, пункт и зона по-человечески, кнопки занявшей записи, английский интерфейс — английский отказ, текст движка в журнале, тупика нет, то же при правке и при неразрешённом классе. Два чужих теста переписаны по существу: «отказ доходит дословно» больше не требование — оно и было дефектом |
| T127 (порча) | сам | готово | 5 порч, все адресные: занятая пара не распознаётся, в чат уходит текст движка, кнопок под отказом нет, зона кодом вместо названия, журнал молчит. `refusal.py`, `record.py`, `edit.py` вернулись байт в байт по md5 |
| T128 | сам | готово | Три мелочи из пяти. «Предложение устарело — бот перезапускался»: причин у устаревшего предложения несколько (забрано после фиксации, погашено новой проверкой, нажата кнопка из старого сообщения), и выбранная наугад посылала человека искать поломку, которой не было, — причина убрана. `ui_lang` и `speech_lang` задаются из мастера: до этого не задавались никогда, и выбор английского отчёта оставлял разговор русским при живом поле. Название точки ограничено 60 знаками |
| T128 (предел названия) | сам | готово | Число замерено, а не выбрано: имя файла отчёта — «Аудит <точка> - <аудитор> - <дата>.pdf», кириллица в UTF-8 два байта на знак, предел имени на ext4 (площадка, D053) 255 байт. Фактическим прогоном: 300 знаков → `File name too long` и отчёт не собирается вовсе; 100 знаков → имя 258 байт. С аудитором в 40 знаков на название остаётся около шестидесяти. Отказ приходит на вводе — в конце проверки переименовать пиццерию уже нечем |
| T128 (тесты) | сам | готово | `tests/test_bot_small_lies.py`, 7 случаев: причина не названа наугад, выбор языка ложится во все три поля и меняет язык разговора (и обратный случай), название сверх предела не принимается, на пределе принимается, имя файла на пределе влезает в 255 байт. Проверено порчей: снятые языки, снятый предел, возвращённый «перезапускался» краснеют адресно |
| T128 (не моё) | сам | найдено | Письмо при пустой проверке («незначительных отклонений D1: 0» и «перечисленные отклонения устраняются») собирает `engine/report.py` — `summary_lines()` и шаблон `LETTER` для чистой проверки. Движок мне трогать запрещено, воспроизвёл фактическим прогоном `build_letter` на проверке без записей и оставляю диспетчеру |
| T123, T126, T127 (ветки отказов) | исполнитель | готово | `tests/test_bot_refusal_units.py`, 14 случаев на то, до чего сквозной диалог не достаёт: неизвестный методике код, нечитаемое состояние при разборе отказа, `skip` у правки, свободная пара, три вида события у `_asked_here`, три случая чтения кадра с диска. Отдано: код готов и стабилен, контракт расписан по веткам, образец рядом, работа механическая — ни миграций, ни политик, ни входа, ни денег. Принято не по отчёту: одна из трёх его порч (снятое условие `finding.n != skip`) воспроизведена своими руками — покраснели два теста, `refusal.py` вернулся байт в байт по md5. `src/bot/refusal.py` — 100 % |
| T131 | сам | готово | Язык интерфейса до начала проверки стал параметром развёртывания: `BOT_UI_LANG`, разбор в `texts.default_ui_lang`, проверка на старте в `config`. Не отдано исполнителю: задача — четыре решения (где живёт значение, каким остаётся умолчание, где его задаёт демо, что делать с видом проверки), а правка кода при этом мелкая |
| T131 (кнопки мастера) | сам | найдено по ходу | Правки приветствия было мало: надписи `Новая проверка`, `Продолжить`, `Начать новую` и три вида проверки стояли строками в `keyboards.py` — единственные строки интерфейса мимо каталога. Английский текст с русской кнопкой под ним оставил бы демо наполовину русским, и увидел это живой прогон демо, а не чтение кода |
| T131 (вид проверки) | сам | найдено и починено | `KIND_LABELS` служил и надписью кнопки (язык интерфейса), и значением в шапке отчёта (язык отчёта). Разведено на `KIND_TITLES` + `kind_title(code, lang)`. Попутно закрылось предсуществующее расхождение: английский отчёт получал в шапку русскую «Плановую» |
| T131 (тесты) | сам | готово | `tests/test_bot_ui_lang.py`, 14 случаев: значение переменной (умолчание, отказ, старт бота), весь мастер на языке стенда через настоящий диспетчер, приоритет начатой проверки над стендом, вид проверки по языку отчёта, описание переменной в `.env.example` и `en` у демо-профиля через `docker compose config`. Не отдано исполнителю: половина случаев — это ровно то, чем задача рискует |
| T131 (порча) | сам | готово | 10 порч, 10 адресных покраснений, восстановление копиями, снятыми до порчи; шесть файлов вернулись байт в байт по md5 |
| T131 (порча, ложный красный) | сам | найдено и починено | Первый заход шёл в урезанном окружении, и в нём находился другой `docker` — без плагина compose. Проверка демо-стенда краснела на любой порче, в том числе не относящейся к стенду. Контрольный прогон на чистом коде в том же окружении показал тот же красный; порча переделана на полное окружение процесса |
| T131 (изоляция прогона) | сам | готово | `BOT_UI_LANG` снимается автоматической фикстурой `tests/conftest.py`: оставленная в оболочке после ручного прогона демо, она перекрасила бы половину набора в английский, и падало бы это не там, где причина |
| T131 (смоук) | сам | готово | Демо поднято образом демо-стенда с окружением демо-профиля: приветствие, кнопка входа, вопросы мастера и надписи кнопок — английские; контрольный прогон тем же образом с пустой переменной даёт прежний русский. Сеть подменена записывающей сессией, наружу не ушло ни одного сообщения |
| T126 (переоткрыта, #101) | сам | готово | Прошлая приёмка проверила, что обработчик **подключён** (снятая регистрация краснела), а не что он работает: защита была уже задачи. `chat_ui_lang` ловил только `DomainError`, а порча приходит и `PermissionError`, и `UnicodeDecodeError`, и `TypeError` — на них последний рубеж умирал сам и писал в журнал, что не смог сказать аудитору о сбое. Плюс две беззвучные формы: чужой JSON сходил за незавершённую проверку с пустыми полями, каталог на месте файла — за непочатую, и обе теряли содержимое при следующей «Новой проверке» |
| T126 (`src/bot/inspection.py`) | сам | готово | Весь блок читает проверку через `read_inspection`, а не `domain.get_state` напрямую (15 мест): любая неудача чтения приводится к `DomainError`, каталог на месте файла отличается от «не начата» по `path.exists()`, чужая структура — по пустой дате (её движок ставит сам, поэтому проверки без даты не бывает). Ни одного `except` в роутерах менять не пришлось: они ловили `DomainError` и раньше, просто до неё не доходило |
| T126 (корень не мой) | сам | найдено, отдано диспетчеру | Чинить надо в блоке `domain`: `state.py::_read_raw` оборачивает только `json.JSONDecodeError` и пропускает `OSError`, `_inspection` не проверяет тип `raw["findings"]`, `read_state` отвечает `None` и на «файла нет», и на «файл не файл». У меня — обёртка на своей стороне, чтобы бот отвечал в любом случае |
| T126 (тесты) | сам | готово | `tests/test_bot_state_corruption.py`, 36 случаев: решётка 8 форм порчи × 8 входов на немоту, `/start` с выходом на каждой форме, файл не переписан ни одной формой, две беззвучные формы отдельно, разбор в журнале, язык на нечитаемом файле, ответ при упавшем выборе языка. Проверка начинается по-настоящему движком и портится уже боевая |
| T126 (порча) | сам | готово | 4 порчи, все адресные: снятая нормализация чужих исключений (краснеет `/start` на трёх формах), снятая проверка каталога, снятая проверка чужой структуры, снятая защита языка у последнего рубежа. `PYTHONDONTWRITEBYTECODE=1`, восстановление копиями до порчи, три файла вернулись байт в байт по md5 |
| T147 (#118) | сам | готово | Пометку `zone_unusual` показывала одна первичная фиксация. Правка зоны — самый частый способ увести запись туда, где пункта нет — отвечала «Поправлено» и не настораживала ничем; список перед сборкой отчёта молчал тоже. Собственная документация `view.confirm_line` формулировала правило, которое путь правки нарушал: «показать пометку обязан бот, иначе о ней узнает только партнёр». Партнёр получал вычет по одной зоне со свидетельством про другую |
| T147 (одно правило вместо трёх) | сам | готово | `view.zone_unusual_mark` — общая функция на фиксацию, правку и список. Три отдельных условия в трёх местах и есть причина, по которой два из трёх оказались забыты; шаблон `finish.record_line` получил `{unusual}` |
| T147 (тесты) | сам | готово | `tests/test_bot_zone_unusual.py`, 6 случаев: правка в чужую зону называет пометку, правка обратно её снимает, список перед сборкой показывает, обычная запись список не засоряет, одно правило на три строки, английский интерфейс. Запись уводится в чужую зону кнопкой через настоящий диспетчер (`ez:1:dining`), а не подкладывается флагом |
| T147 (порча) | сам | готово | 5 порч, 5 адресных покраснений, ни одного сбоя сборки строки: каждая возвращает дефект (молчащая правка, молчащий список, пометка не собирается, перевёрнутое условие, признак подменён похожим), а строка остаётся корректной. Отдельно прогнан контроль неэквивалентной порчи: снять только аргумент `unusual`, оставив `{unusual}` в шаблоне, — красный приходит от `BotTextError` «не передан параметр», то есть доказательством не является. `view.py` и `texts.py` вернулись байт в байт по md5 |
| T147 (не моё) | сам | найдено, отдано диспетчеру | В отчёте партнёру пометки нет вообще: `engine/report.py` флаг `zone_unusual` не читает (движок трогать запрещено). Поэтому список в чате перед сборкой — единственное место, где нетипичную зону видно перед отправкой. Заодно устарела строка `docs/04-engine.md`: «нигде, кроме вывода команды, не читается» — флаг читают `bot`, `db` и `mcp` |
| T151 (#122) | сам | готово | Отказ сборки отчёта обходил запрет T127 стороной: пересказывал текст движка дословно. Человек в пиццерии читал «Отчёт не собрался: Отчёт не собран: Рендерер PDF недоступен: … Нужен WeasyPrint 69.x и системные библиотеки Pango. HTML сохранён: /var/folders/…» — удвоенный служебный префикс, абсолютный путь во временный каталог и инструкцию по установке разом. Текст движка ушёл в журнал, в чат — что случилось, что записи целы и как попробовать снова |
| T151 (письмо — свой текст) | сам | найдено по ходу | Неудача сборки письма шла тем же текстом «Отчёт не собрался», хотя PDF аудитор к этому моменту уже получил: прямая неправда. Заведён `finish.letter_failed` |
| T151 (`start.failed`) | сам | найдено по ходу | Тот же дефект на входе в проверку: движок отвечает вызывающему из командной строки полным стеком с путями к своим файлам, а `start.failed` отдавал его аудитору дословно. Воспроизведено настоящим отказом — каталог на месте состояния, как после неудачного монтирования тома |
| T151 (тесты) | сам | готово | `tests/test_bot_build_refusal.py`, 5 случаев на настоящем отказе: движок запускается подпроцессом в окружении с заглушкой `weasyprint`, которая падает при импорте, и отказывает своими словами. Проверяется отсутствие внутренностей, неудвоенный префикс, наличие выхода, свой текст письма, отсутствие стека при отказе старта |
| T151 (два чужих теста) | сам | переписаны по существу | `test_failed_build_is_reported…` и `test_failed_letter_is_reported…` утверждали, что текст отказа доходит до аудитора дословно, — а это и было дефектом (тот же случай, что в T127). Существо сохранено: аудитор не остаётся ни без ответа, ни с ложным отчётом, причина не теряется — она проверяется в журнале |
| T151 (порча) | сам | готово | 5 порч, все адресные и все с собранной строкой. Одна из них — M3 — с первого раза оказалась **негодной**: вносила `NameError` вместо возврата дефекта (`replace` с первого вхождения попал в чужой `except` того же вида), и красный шёл от сбоя, а не от утечки. Обнаружено измерением того, что реально прочитал аудитор, а не чтением вывода. Переделана на замену блока целиком с проверкой единственности. Заодно в двух чужих тестах переставлен порядок assert: существо раньше сверки с каталогом, иначе порча, вернувшая `{reason}` в шаблон, роняла бы тест `BotTextError`-ом и красный не был бы доказательством. Три файла вернулись байт в байт по md5 |
| T153 (#124) | сам | готово | `/start` показывал одну фразу на два разных положения дел: «есть незавершённая проверка… новая сотрёт эту». Для сданной проверки это неправда дважды — она завершена, и стирать в ней нечего, — и звучала она в начале каждой второй проверки за день. Признака «завершена» у движка нет, выдумывать его за движок бот не вправе; зато он вправе помнить, что **сам** собрал и отдал отчёт |
| T153 (где живёт признак) | сам | решение | В заметках бота (`bot.json`) рядом с проверкой: они уже лежат в её папке, переживают перезапуск и обнуляются на старте новой проверки — то есть у признака ровно тот срок жизни, что и у проверки. Не в состоянии движка (чужой блок, своего признака у него нет) и не в базе: работа без базы законна (D027), а признак нужен и там |
| T153 (число, а не «да/нет») | сам | решение | Хранится число записей на момент сдачи. Аудитор вправе продолжить сданную проверку и дописать запись — отданный отчёт её уже не содержит, и звать такую проверку сданной нельзя. Число сверяется с делом само, без обнуления признака из пяти мест. Умолчание `-1`, а не `0`: ноль записей — законная сданная чистая точка |
| T153 (тесты) | сам | готово | `tests/test_bot_handed_over.py`, 9 случаев на настоящем прогоне (движок, WeasyPrint, живой PDF): сданная не зовётся незавершённой, выбор остаётся за аудитором, проверка в работе не тронута, дописанная запись возвращает незавершённость, несобравшийся отчёт сданной не делает, новая проверка признак обнуляет, признак лежит на диске, чистая проверка без записей не сданная — и обратное |
| T153 (порча) | сам | готово | 5 порч, 5 адресных покраснений, ни одного сбоя сборки строки: одна фраза на оба случая, бот не помнит отдачи отчёта, признак не сверяется с числом записей, считается нажатая кнопка вместо отданного отчёта, умолчание признака `0` вместо «не сдавалась». Три файла вернулись байт в байт по md5 |
| T162 (#134) | сам | готово | Процент во время обхода снят (решение владельца D072). Показ был в четырёх местах: фиксация по кадру, фиксация словами, правка, удаление. Вместе с показом убран и расчёт — `domain.score` в этих путях не зовётся вовсе: подпроцесс на 26 мс за число, которое выбрасывается. Оценка целиком осталась в итоге при завершении, и это проверяется отдельным случаем — снять её и там значило бы, что аудитор не видит процент никогда |
| T162 (тесты) | сам | готово | `tests/test_bot_percent_at_finish_only.py`, 5 случаев через настоящий диспетчер. Написаны до правки и покраснели на неисправленном коде 4 из 5 (пятый — про итог — был зелёным и им же обязан остаться). Поправлены тесты, в которых процент был вписан ожиданием: `test_bot_record_router.py`, `test_bot_edit_router.py`, `test_bot_fastpath.py`, `test_bot_zone_unusual.py` |
| T139 (#110) | сам | готово | Показ записанного получил свой вход: команда `/records` и та же функция `show_records`, которой пользуется первая часть `/finish` (две копии списка разошлись бы молча — так уже терялась пометка нетипичной зоны в T147). Итог с процентом и кнопки завершения в показ не входят: он зовётся посреди обхода, где оценка не показывается (D072), и обязан быть безопасным на вид, иначе аудитор не нажмёт и его |
| T139 (меню) | сам | решение | Одной команды мало: аудитор на точке не набирает `/records` по памяти. Команды объявляются в меню телеграма (`app.announce_commands`, `set_my_commands`) — там же стали видны `/undo` и `/finish`, которые до этого знал только тот, кто читал документацию. Отказ телеграма на объявлении бота не роняет: без меню он работает, а не подняться из-за меню — цена несоразмерная |
| T139 (тесты) | сам | готово | `tests/test_bot_records_command.py`, 6 случаев через настоящий диспетчер. Красный до правки: 4 случая падали «бот не отправил ни одного сообщения» — команды не было вовсе. Порча: команда убрана из меню → краснеет случай про меню; показ кадров без записи снят → краснеет случай про кадры. Первая попытка второй порчи была НЕэквивалентной (файл обрезан, красное шло от `ImportError` на сборке) и переделана |
| T128 (переоткрыта, #103) | исполнитель | готово | Предел имени файла отчёта считался знаками, а он в БАЙТАХ (255 на ext4). Не покрыто было два места: имя аудитора из профиля телеграма не ограничено ничем (худший достижимый случай имени файла — 399 байт), а предел названия точки в знаках не ловит эмодзи (60 эмодзи — те же 60 знаков, но 240 байт). Бюджет разложен замером: постоянная часть формулы движка 31 байт, точке 120 байт (ровно прежние 60 кириллических знаков — живым названиям предел не мешает), аудитору 100. Имя аудитора отказать нельзя (оно из профиля) — оно обрезается, обрезка видна знаком «…» и названа аудитору строкой при старте |
| T128 (проверка формулы) | сам | готово | Формула имени, собранная в тестах руками (импорт движка из тестов запрещён контрактом), сверена с настоящим движком фактическим прогоном: `audit.py init` с названием в 60 кириллических знаков и именем профиля в 200 знаков, затем `report.py pdf` — файл собрался, имя 250 байт при пределе 255. То есть проверяется не пересказ формулы, а её совпадение с движком |
| T128 (тесты) | исполнитель | готово | `tests/test_bot_report_filename.py`, 7 случаев через настоящий диспетчер: 200 знаков профиля влезают, эмодзи-имя влезает, живое короткое имя не трогается, обрезка названа в чате, 60 эмодзи в названии не принимаются, 60 кириллических принимаются, худший случай целиком ≤ 255 байт. Порча (с `PYTHONDONTWRITEBYTECODE=1`, восстановление копией, md5 сверен): снят байтовый предел названия → красит ровно случай с эмодзи; снята обрезка имени → красят ровно четыре случая про имя, с фактическими числами 447, 287 и 551 байт |
| T128 (чужой тест) | сам | готово | `t()` отказывает на недостающем параметре, а в `start.started` появился `{auditor_note}` — на этом падал `tests/test_bot_texts.py`. Поправлен вызов в тесте, а не смягчён `t()`: отказ на недостающем параметре и есть то, ради чего он написан |
| T158 (покрытие) | сам | готово | После первого прогона новые модули дали 86 %: не покрыты были ветки отказов. Дописаны случаи на методику без информационных пунктов (отчёт собирается сразу, как до задачи — тот же урок, что D068 и D075) и на отказ `set_info` пустому полю. Стало: `src/bot/info.py` и `src/domain/info.py` — 100 %, `routers/info.py` — 92 % (непокрытое — ветки «нажатие пришло без сообщения», уже проверенные общим случаем `test_bot_callback_edges.py`) |
| T152 (#123) | сам | готово | Вид проверки жил в системе формулировкой: слово писалось в проверку, уезжало в колонку базы и в отпечаток, а в отчёте переводилось обратно сопоставлением строки с молчаливым возвратом исходника. Таблица перевода переехала из `keyboards.py` в предметную область (`src/domain/kinds.py`): вид проверки нужен ещё и базе, и письму по записанной проверке, а две копии таблицы разошлись бы. `start_inspection(kind=...)` принимает КОД и отказывает на формулировке ДО вызова движка; язык отчёта проверяется там же и первым, иначе отказ «вид не заведён на языке sr» объяснял бы человеку не то. Поправлено 130 чужих вызовов в 59 файлах тестов и два инструмента — сплошной правкой скриптом, а не руками: руками в таком объёме пропускается один |
| T152 (порча) | сам | готово | 6 порч, 6 адресных покраснений: слово вместо кода в проверке, шапка всегда по-русски (язык — константа), молчаливый возврат незнакомого кода, откат незаведённого языка на русский, дырка в таблице переводов, код вместо слова в чате. `PYTHONDONTWRITEBYTECODE=1`, восстановление копией, md5 сверен на каждой |
| T152 (смоук) | сам | готово | Настоящий диспетчер + настоящий `engine/report.py letter`, оба направления: интерфейс `en` + отчёт `ru` → в проверке `planned`, в шапке «Плановая», письмо движка «Вид проверки — Плановая»; интерфейс `ru` + отчёт `en` → в проверке `planned`, в шапке «Planned», письмо «Inspection type: Planned». То есть проверялся не код, а то, что дойдёт до партнёра |
| T138 (#109) | сам | готово | Механизм T068 правильный и уже спасал сценарий — бесполезной была адресация: номер сообщения в телеграме человеку не показывается. Кадр возвращается в чат самим кадром, ответом на своё сообщение, ответ мягкий (удалённое сообщение показ не отменяет). Пачка ограничена десятью: тридцать фотографий подряд хоронят итог и кнопки завершения, ради которых аудитор и нажимал «Завершить»; остаток назван числом. `view.unclaimed_lines` и текст `finish.unclaimed_line` сняты — адресовать кадр номером больше негде. Порча: 5 порч, 5 покраснений |
| T156 (#127) | сам | готово | Пометка «зона из памяти» стояла только на быстром пути. Условие сведено к одному на все пути: зону не назвал человек — ни словами, ни кнопкой зоны — и записана та самая, которую подставила память. Своя зона модели догадкой не считается. Первая порча (кнопка зоны снова считается памятью) НЕ покраснела: ветку «зону спросили кнопкой, пункт выбран в ручном перечне» не стерёг ни один тест. Тест дописан, порча краснеет — итого 5 порч, 5 покраснений |
| T167 (#135) | сам | готово | Отказ `ChecklistVersionMismatch` доезжал до аудитора общим текстом последнего рубежа. Теперь в чате обе версии и две кнопки: перевести проверку на действующую методику (`domain.sync_checklist_version`, след остаётся в проверке) или оставить как есть. Ни одна ветка не выбирает за человека; после перевода показывается обычный итог, а не одно сообщение о переводе. Порча: 5 порч, 5 покраснений |
| Покрытие очереди | сам | готово | Первый прогон дал непокрытыми ветки отказов: `src/bot/routers/records.py` 91 %, `finish.py` 93 %. Дописаны случаи на отказ телеграма при показе кадра (остальные кадры показываются, число непоказанных названо), на неудачный перевод методики (текст отказа с путём на диске в чат не уходит) и на нажатие выбора по снесённой проверке; два новых нажатия добавлены в общий перечень `test_bot_callback_edges.py`. Стало: `records.py` и `src/domain/kinds.py` — 100 %, `finish.py` — 97 %. Заодно снят `domain.kind_codes` — публичная функция, которую не звал никто (YAGNI) |
| Общее покрытие 94 % | сам | так и есть | Не 98 %, как в брифе, и это не просадка блока: 169 тестов пропущено — набор шёл без поднятого Postgres, а `src/db` покрывается только им. Все тронутые очередью файлы — 97–100 %. Порог `fail_under` в проекте не настроен, `make test` зелёный |
| T189 (#156) | сам | готово | Под нагрузкой интерпретатор подпроцесса умирает ДО первой строки `audit.py` (`getpath` ловит `InterruptedError: [Errno 4]`), и `run_audit` отдавал этот ненулевой код наверх как вердикт движка — аудитор получал «Не записал… Сбой на моей стороне» там, где движок команды не видел. Лечится в одном месте, `src/domain/engine.py`: несостоявшийся старт повторяется (3 попытки), умерший ПОСЛЕ инициализации не повторяется никогда, отказ движка словами остаётся отказом движка и не повторяется тоже. Отличает одно от другого сам интерпретатор (`Python runtime state: core initialized`), а не догадка по коду возврата — образец взят из `src/mcp/checklist.py` (T187) |
| T189 (тесты) | сам | готово | `tests/test_domain_engine_start.py`, 6 случаев. Первая версия считала запуски счётчиком внутри подделки движка и оказалась негодной ровно по причине задачи: на этой машине один из трёх запусков подделки умирал сам, не дойдя до своей первой строки, и тест показывал 2 из 3 на исправном коде. Запуски считаются на стороне родителя (подмена `_launch`); настоящий подпроцесс остался в одном тесте — на опознание признака в живом `stderr`, без утверждений о числе |
| T189 (порча) | сам | готово | Защита снята целиком (ветка `RUNTIME_FATAL` вырезана, любой ненулевой код снова вердикт): 4 из 6 покраснели, 2 «не повторять» остались зелёными по построению — снятие повтора их сломать не может. `PYTHONDONTWRITEBYTECODE=1`, восстановление копией, md5 сверен (`39a4cacd…`) |
| T155 (#126) | сам | готово | Заметки бота (`bot.json`) писались без блокировки, хотя файл проверки в той же папке ею защищён. Каждое изменение здесь — «прочитать, дополнить, записать», и два писателя внахлёст теряют дополнение молча: замерено 36 кадров из 60 на трёх процессах по двадцать. Тройка целиком заведена под `_change`, замок — `domain.state.state_lock` на `bot.json.lock`, то есть тот же `flock` и то же имя, что у проверки (второй экземпляр протокола разошёлся бы с первым). `reset` тоже под замком: снятие файла в обход позволило бы писателю со старой основой воскресить кадры прошлой проверки. Комментарий «двух писателей здесь не бывает» снят — это было допущение, а не гарантия |
| T155 (тесты) | сам | готово | `tests/test_bot_sidecar_lock.py`, 4 случая: потеря дополнения при перекрытии (два потока с явным окном между чтением и записью — без окна перекрытия не случается и дефект не воспроизводится), взятие НАСТОЯЩЕЙ файловой блокировки (захват того же файла отдельным дескриптором — внутрипроцессный замок такую проверку бы прошёл, а дефект бы остался), потеря кадров у нескольких настоящих процессов и очистка под замком. Процессы разводятся `os.fork`, а не запуском интерпретатора: на занятой машине новый интерпретатор может не стартовать вовсе (T189), и тест ловил бы беду машины вместо дефекта заметок |
| T155 (порча) | сам | готово | 3 порчи, 3 покраснения: замок снят целиком (3 из 4 краснеют), замок взят только вокруг записи (краснеют оба теста на потерю, а тест «замок взят» остаётся зелёным — он и должен, замок ведь взят: разделение показывает, что тесты не дублируют друг друга), очистка в обход замка (краснеет её тест). `PYTHONDONTWRITEBYTECODE=1`, восстановление копией, md5 сверен (`b26a1efc…`) |
| T189 (повтор и дубль) | сам | готово | Отдельная проверка на вопрос «не превратит ли повтор запись в дубль»: два теста на НАСТОЯЩЕМ `engine/audit.py` над настоящим файлом проверки. Подставной корень (`config._REPO_ROOT`) подсовывает обёртку движка всем обращениям блока сразу. Старт не состоялся → движок зовётся дважды, в проверке одна запись; движок отработал и умер после → движок зовётся один раз, в проверке одна запись. Считаются именно заходы: движок отвергает вторую запись по той же паре «пункт + зона» сам, поэтому по одному числу записей дубль не виден и тест бы не различал. Порча в обе стороны: снять различение `core initialized` (краснеет тест про смерть после работы) и снять повтор совсем (краснеет тест про несостоявшийся старт), md5 сверен (`1b1b42b7…`) |
| Полный набор и якорь | сам | готово | Машина освободилась (нагрузка 2 против 250–330 днём), полный набор прогнан целиком: **1618 passed, 202 skipped, 0 failed** за 2 мин 11 с, покрытие 94 %. `make fmt lint types dead bounds` — чисто. `make regress` — дословно `belgrade-1: 97.5%, A, D1=5` и `belgrade-2: 97%, A, D1=6`. Пропуски — набор шёл без поднятого Postgres и без боевой методики вне git |
| T202 (#165) | сам | готово | Кадр с комментарием больше не уходит в модель: правило `needs_photo` сведено к «слов нет — идёт кадр». Прежняя оговорка отправляла картинку всегда, кроме «один поднятый пункт с единственным классом», — и это НОЛЬ случаев из семнадцати боевых, а не один: прогон старого правила по `examples/` даёт 17 отправок из 17 и на карте слов от 05.09, и на той, которой её днём подменили. То есть экономии у оговорки не было вовсе, она только выглядела экономией. Число из брифа («1 из 17») исправлено в четырёх местах — docstring, тест, спека бота, контракт `recognize`. Бот заодно перестал скачивать такой кадр у телеграма; правило спрашивается у `recognize`, второй копии в боте нет |
| T202 (тесты и порча) | сам | готово | 4 случая написаны до правки, все 4 красные на неисправленном коде. Чужой тест `test_bot_fastpath` требовал обратного (кадр доезжает до модели по кнопке «Разобрать моделью») и переписан под D081. Порча обоих мест разом (`needs_photo` всегда True, скачивание без условия) — 4 адресных покраснения, восстановление копией, md5 сверен |
| T202 (замер качества) | исполнитель + сам | ЧАСТИЧНО | Живого прогона «комментарий+кадр против комментария» НЕТ: в этой копии `OPENAI_API_KEY` подставной, а ключ основной копии брать не дал слой разрешений. Оснастка написана исполнителем по готовому контракту, проверена мной целиком без сети (модель подменена на уровне `ask_model`: 34 вызова, 17 с картинкой в плече A, ноль в плече B) — `tools/comment_only_measure.py`, прогон одной командой меньше минуты. Что померено детерминированно и сети не требует: эталонная пара «пункт + класс» лежит в перечне, который видит модель, в 17 случаях из 17, и перечень в обоих плечах ОДИН И ТОТ ЖЕ (`shortlist` кадра не видит) — потолок разбора правка не двигает; зона по контракту `recognize` из кадра не выводится вовсе; пунктов с выбором класса, где масштаб виден на кадре, а не в словах, среди семнадцати ровно ОДИН. Непокрытый риск — выбор модели внутри того же перечня, его меряет только живой прогон |
| T204 | сам | готово | Правка записи ответом на сообщение бота. Пункт ищется заново тем же путём (сверка → модель → ручной перечень): своя дорога для правки разошлась бы с первой молча. Адресат — карта «сообщение → запись» в заметках (издание формата 3), а не «последняя запись»: между разбором и ответом аудитор проходит полцеха. Записи не прибавляется — иначе одно нарушение уехало бы партнёру двумя строками. Ответ не про запись пропускается дальше нетронутым (`SkipHandler`), прежнее связывание комментария с кадром не тронуто |
| T204 (что нашёл смоук) | сам | готово | Настоящий диспетчер на боевой методике показал то, чего тесты не стерегли: перечень кандидатов под правкой спрашивал «Что записать? Кадров: 0» и предлагал кнопку «Записать» — обещал вторую запись ровно там, где аудитор от неё уходит. Стало «Чем поправить запись #1?» и «Поправить на это», число кадров из показа убрано (кадры остались у записи); на это дописан тест |
| T204 (тесты и порча) | сам | готово | 9 случаев, все до правки; 7 из 8 первых красные на неисправленном коде, восьмой (сторож прежнего связывания) обязан был остаться зелёным и остался. Порча: 5 порч, 5 адресных покраснений — адресат берётся как последняя запись, `correcting` не доезжает до сверки, сообщение о записи не запоминается, нажатие заводит новую запись, снятая запись не проверяется. md5 сверен по четырём файлам |
| T204 (чего НЕ делал) | сам | решение | Карта «сообщение → запись» при удалении записи не чистится, и это проверено фактом, а не оставлено на авось: движок номера НЕ переиспользует (после снятия записи №1 следующая получает №3 — прогон `add_finding` над настоящим движком), а адресат ищется в самой проверке, где снятой записи нет ни под каким номером. Комментарий в `domain.drop_finding` утверждает обратное («номер после удаления освобождается») — расхождение доки с движком в блоке `domain`, вынесено диспетчеру |
| T201 (#164) | сам | готово | Сданная проверка запечатана: «Продолжить» у неё нет, а кадр, комментарий, фиксация, правка кнопками, правка ответом и `/undo` упираются в отказ с причиной и двумя выходами. Запрет стоит там, где проверка МЕНЯЕТСЯ, а не только на входе в разговор: нажатие под предложением, показанным ДО сдачи, входа с порога не имеет вовсе. Признак сдачи вернулся к смыслу «отчёт отдан» — сверка с числом записей была нужна, пока дописывать было можно, а теперь стала бы дырой в запрете |
| T201 (что значит «удалить») | сам | решение | Владелец назвал два выхода: удалить отчёт или завести новый. Бот делает то, что может честно: убирает копию проверки в чате (`domain.drop_inspection`) — ту, что мешает начать следующую, — и вслух говорит, что отданный отчёт и строка в истории точки остаются. Удаления из истории НЕ сделано и наспех делаться не должно: в `src/db` нет ни одной операции удаления, слив идёт по слепку содержимого с `on conflict do nothing`, кадры лежат в объектном хранилище отдельно. Объём: миграция под признак снятия (или физическое удаление), операция в `src/db` с тестами, уборка кадров в хранилище, подтверждение в боте — и до всего этого решение владельца, можно ли вообще удалять строку истории. Это не блок `bot` |
| T201 (тесты и порча) | сам | готово | 12 случаев (`tests/test_bot_sealed.py`), два прежних случая T153 переписаны — они защищали ровно то, что владелец отменил. Порча: 7 порч, 7 адресных покраснений, из них 5 хирургических — по одному входу за раз (материал, `_save`, правки и `/undo`, правка ответом, кнопка удаления без проверки); каждая валит ровно свои тесты и ни одного чужого. md5 сверен по семи файлам |
| Утечка методики | сам | частично | `data/photo-cues.md` подменили днём (18:10, посреди волны), и сторож `test_methodology_leak` покраснел на семи цитатах. Четыре мои или моего блока — пример владельца в двух файлах и название оборудования в `zones.py` с тестом — переписаны без цитат. Три остальные (`src/recognize/cues.py`, `tests/test_recognize_cues_distinctive.py`) в блок `bot` не входят и мной не тронуты; `make test` из-за них красный, вынесено диспетчеру |
| T140 (#111) | сам | готово | Кнопки зон: по одной в ряд, свой предел подписи `ZONE_TITLE_LIMIT` = 68 (вдвое от общего, потому что кнопка занимает весь ряд, а не половину). Исполнителю не отдано: работа состояла в том, чтобы ПОСЧИТАТЬ предел и доказать фактом, что хвостов не осталось, а не в замене двойки на единицу. Факты: в боевой методике 10 зон, самое длинное название 36 знаков по-русски и 32 по-английски — при 34 резалось ровно одно и ровно то, о котором задача («…курьерская з…»); при 68 не режется ни одно ни на одном языке интерфейса. Тесты написаны до правки, три из четырёх красные на неисправленном коде (`tests/test_bot_zone_buttons.py`); четвёртый — сторож порядка зон под T133, красное ему дала порча. Четыре порчи: зоны снова по две в ряд, предел вернулся к общему, предел снят вовсе (подпись не режется никогда), клавиатура переставляет зоны — каждая валит ровно свои тесты. `PYTHONDONTWRITEBYTECODE=1`, восстановление копией файла, сверено по md5 `keyboards e4f0ecb4…`. Общий прогон трёх задач одной волной: `make fmt lint types dead bounds` зелёный, полный набор с Postgres — **1941 passed** (без `tests/test_db_migrate_roles.py`, #128), `make regress` — 97.5%/A/5×D1 и 97%/A/6×D1 дословно. |
| T205 | сам | готово | Кадр ответом на СВОИ слова уходит в ту же запись. Разбора у него нет и быть не должно: пункт уже найден по словам человека, кадр их подтверждает (D078), а не оспаривает. Карт сообщений в заметках стало две (издание формата 4), и это не дубль: `records` — сообщения бота, ответ на них ПРАВИТ запись (T204); `origins` — сообщения аудитора, ответ на них КАДРОМ пополняет запись. Слей их в одну — и ответ словами на своё же голосовое стал бы правкой, тогда как правку владелец описал ровно как ответ на сообщение бота. Исток переезжает и через «Разобрать моделью»: те же слова — та же досылка |
| T205 (тесты и порча) | сам | готово | 12 случаев (`tests/test_bot_frame_reply.py`) через настоящий диспетчер. 7 порч, все адресные: подхват выключен целиком (5 покраснений), кадр не попадает в заметки, снят запрет сданной проверки, снята проверка «запись ещё жива», `origin_of` читает карту бота (6 покраснений, включая сторож прежнего поведения), карты слиты в одну (краснеет ровно сторож границы), исток не запоминается. Отдельно проверены ветки не-молчания: отказ движка на кадре и кадр в убранную проверку — обе покраснели, когда ответ аудитору убран. Восстановление копией, md5 сверен по четырём файлам |
| T205 (заметки, юнит-тесты) | исполнитель | готово | `tests/test_bot_sidecar_origins.py`, 11 случаев на карту `origins`: пара находится, незнакомое сообщение даёт `None`, повтор не дублируется, две карты не путаются, карта переживает перезапуск, старые заметки без ключа читаются, испорченная карта — отказ, `reset` её стирает. Отдано исполнителю: контракт был расписан целиком, работа механическая, файл один. Проверено мной ДВУМЯ своими порчами поверх его отчёта — `origin_of` читает карту бота (3 покраснения) и испорченная карта подменяется пустой (6 покраснений), md5 сверен |
| T206 | сам | готово | Пачка кадров БЕЗ комментария разбирается по кадру, отбивка приходит списком отдельными сообщениями с номером кадра. Кадры С комментарием остаются одним нарушением с несколькими фото: разводит случаи наличие слов, а не число кадров. До задачи пачка давала одну запись на все кадры, а в модель уходил только первый (`analyze` брал `file_ids[0]`) — второй и третий уезжали в отчёт прикреплёнными к чужому пункту, никем не разобранные. Цена разведения названа вслух и в доке: разбор пачки — столько вызовов модели, сколько кадров, и число говорится ДО начала. D046 в силе: без нажатия человека не уходит ни один кадр |
| T206 (адресность нажатия) | сам | готово | Предложение в чате перестало быть одним: `PendingStore` держит их по номеру разговора (`Proposal.slot`) и знает, каким сообщением каждое показано (`propose(..., at=)`). Нажатие ищет своё предложение по сообщению, под которым стоит кнопка, — в `callback_data` этого нет, а телеграм присылает само сообщение. Неадресованное нажатие при живой пачке отвечает «устарело», а не догадкой по последнему показанному: догадка записала бы чужой кадр молча |
| T206 (что нашла порча) | сам | найдено и починено | Порча «взятое предложение снимается не со всех своих сообщений» не покраснела ни на одном тесте — и разбор показал настоящую дыру, существовавшую и ДО задачи: кнопка из уже разобранного сообщения падала на последнее предложение чата и записывала материал ЧУЖОГО кадра. Правило переписано: сообщение опознано — ответ только по нему, и «разговор окончен» честно даёт «устарело». Сообщения разговора после фиксации намеренно НЕ забываются, иначе та же кнопка снова падала бы на последнее предложение. На это написан отдельный случай, порча после него краснеет |
| T206 (тесты и порча) | сам | готово | 9 случаев (`tests/test_bot_batch_frames.py`). 6 порч, все адресные: пачка снова разбирается одним вызовом (7 покраснений), каждой записи достаются все кадры, отбивка не называет номер кадра, адресация нажатия выключена, снят запрет догадки по пачке, опознанное сообщение падает на последнее предложение. Прокомментированный альбом стережётся своей порчей (подпись перестаёт собирать материал). В тестах пришлось отказаться от закрытия альбома таймером: с окном 10 мс он срабатывает МЕЖДУ кадрами и рвёт альбом посередине — тест на прокомментированный альбом получал два кадра из трёх и падал не на том, что в его имени. Альбом закрывается событием, окно длинное |
| T205, T206 (смоук) | сам | готово | Настоящий диспетчер, настоящий движок подпроцессом, синтетическая методика: кадр → голосовое → запись → кадр ответом на голосовое (в записи два кадра) → пачка из трёх → отбивка тремя сообщениями «Кадр k из 3» и три вызова модели → нажатие под третьим записывает третий кадр → ответ на сообщение о записи её правит → кадр ответом на сообщение БОТА остаётся обычным кадром → завершение: три записи, кадры без записи показаны все три, PDF и письмо отданы. Модель и расшифровка подменены — боевого ключа в копии нет, живого замера качества не было |
| T205, T206 (доки) | сам + исполнитель | готово | `docs/06-mvp-bot.md` (шаги 3 и 5: досылка кадра, разводка пачки и прокомментированного альбома, цена в вызовах модели, граница между правкой и досылкой) — сам. Раздел «Что блок предоставляет» в этом файле — исполнитель по расписанному контракту, сверено мной по коду построчно |
| T228 (#183) | сам | готово | Тесты альбома закрывали пачку окном в 10 мс, и окно срабатывало МЕЖДУ кадрами. Дефект воспроизведён, а не предположен: копия файла с задержкой 20 мс между кадрами роняла ровно четыре теста из десяти — те самые, что ждали таймер (альбом собирался из двух кадров вместо трёх, отказ «проверка не начата» приходил трижды вместо одного раза). Три теста переведены на закрытие СОБЫТИЕМ (`close_by_event` — кадр чужой группы, приём блока bot16), четвёртый оставлен на таймере: таймер и есть его предмет, но окно 0,5 с вместо 0,01 и ожидание по наблюдаемому признаку (`wait_for_reply`) вместо слепого `sleep(0.1)`. После правки та же задержка 20 мс и даже 100 мс на кадр держат все десять зелёными |
| T228 (порча) | сам | готово | `AlbumBuffer.close_other` перестал закрывать чужой альбом — красных пять из десяти, включая все три переведённых на событие. `src/bot/albums.py` вернулся байт в байт (md5 `e62ce0da…`) |
| T217 (#150) | сам | готово | Кнопки ручного перечня стоят по одной в ряд, а подпись резалась по `BUTTON_TITLE_LIMIT` = 34 — пределу, выведенному под кнопку в ПОЛОВИНУ ряда (два столбца делят ширину пополам). То есть треть на код уходила не из ширины экрана, а из половинного бюджета. Место возвращено шириной ряда: `ROW_TITLE_LIMIT` = 2 × 34, из него вычитается длина префикса `КОД · ` — под формулировку 60 знаков вместо 34. Тем же движением `ZONE_TITLE_LIMIT` перестал быть вторым числом 68 и выводится из `ROW_TITLE_LIMIT` |
| T217 (почему код остался) | сам | измерено | Считал, а не прикидывал, по боевой методике: при 34 знаках обрезано 94 % пунктов (медиана формулировки 64 знака, максимум 252), при 60 — 54 %, целиком показывается почти половина вместо каждого двадцатого. Снять код и отдать формулировке все 68 нельзя: после обрезки до 60 знаков 11 пунктов из 123 по-русски и 10 по-английски совпадают подписью буква в букву — аудитор выбирал бы из неразличимых кнопок, а код к тому же единственное, чем кнопка связана с методикой и отчётом |
| T217 (тесты и порча) | сам | готово | 6 случаев (`tests/test_bot_manual_buttons.py`), один из них на боевой методике под `requires_data`. 3 порчи, все адресные: предел вернулся к половине ряда (3 красных), код снят с кнопки (3 красных, включая сторож неразличимости), предел снят вовсе (2 красных плюс сторож зон). `src/bot/keyboards.py` вернулся байт в байт (md5 `66bb9dcb…`). Заодно поправлена устаревшая строка в `tests/test_bot_taken_candidate.py` — она называла 34 знака |
| T219 (#167) | сам | готово | Быстрый путь подписывал текст записи «Ваши слова», путь с подтверждением — «В отчёт», хотя текст один и тот же: на быстром пути слова аудитора и СТАНОВЯТСЯ формулировкой, которую печатает отчёт партнёру. Подпись читалась как расписка в получении — такое не вычитывают, и дальше правка зоны кнопкой оставляла формулировку прежней: партнёру уезжала запись, спорящая сама с собой. Подпись сведена к одной на оба пути (`record.fixed` в `src/bot/texts.py`), на обоих языках. Что слова его, сказано тут же в первой строке сообщения |
| T219 (тесты и порча) | сам | готово | 2 случая (`tests/test_bot_fastpath_note_label.py`): совпадение подписей на обоих языках и та же подпись на живом показе через настоящий диспетчер — сверка шаблонов без второго теста ничего не стоила бы. Сверяется не буква подписи, а совпадение путей: тексты переводятся, требовать конкретную фразу значило бы чинить тест на каждую правку. Порча — возврат прежней подписи: оба теста красные. `src/bot/texts.py` вернулся байт в байт (md5 `3e06fda3…`) |
| T219 (доки) | сам | готово | Довод «у подтверждения ваших слов нет» стоял в трёх местах и опирался на снятую подпись: `docs/06-mvp-bot.md` (шаг 5), `src/bot/view.py` (`confirmed_block`), комментарий над `record.confirmed` в `src/bot/texts.py` |
| T227 (#182) | сам | готово | Отказ занятой пары называет номер записи и ставит под собой её кнопки правки, но в карту «сообщение → запись» не попадал: ответ словами уходил мимо роутера правки в очередь материалов и брал старейший ждущий кадр — в смоуке так появилась запись с посторонним кадром. Отправка отказа сведена в одно место (`shown.tell_refusal`) и там же кладётся связь: два места, посылавших один и тот же отказ, разошлись бы молча. Оба вызова переведены — фиксация (`routers/record.py`) и правка кнопкой (`routers/edit.py`). Граница проведена по `clash`: отказ без записи в карту не попадает, и ответ на него связывается с кадром как раньше |
| T227 (тесты и порча) | сам | готово | 5 случаев (`tests/test_bot_refusal_reply.py`): правка вместо новой записи, ждущий кадр дожидается СВОИХ слов (проверено тем, что следующий комментарий его и берёт), то же на отказе правки кнопкой, отказ без записи в карту не попал и связывание цело, и сама связь в карте. Порча — снять запоминание: 4 красных из 5, пятый (граница) обязан был остаться зелёным и остался. `src/bot/shown.py` вернулся байт в байт (md5 `cdf4cc3a…`) |
| T225 (#181) | сам | готово | Семь мест бота звали справочники домена без чата — по каталогу, который лежит в `AUDIT_DATA_DIR` СЕЙЧАС, а не по изданию проверки (T169). Чат проведён до самого низа: `view.zone_titles/zone_title` и все строки записи поверх них, `refusal.item_title`, `info.question/fields_to_ask`, `zones.phrases/zone_from_words`, кнопки зон и классов в `routers/edit.py`, кнопка зоны в `routers/record.py`. Параметр сделан ОБЯЗАТЕЛЬНЫМ именованным, без умолчания: умолчание и есть тот самый молчаливый откат на действующую методику. Цена решения — 11 мест, которые нашла проверка типов, и 23 вызова в тестах; выгода — забыть чат больше нельзя, это не обещание, а отказ сборки |
| T225 (опыт, а не чтение кода) | сам | готово | 6 случаев (`tests/test_bot_edition_pinned.py`): в каждом заводится проверка, методика ПЕРЕИЗДАЁТСЯ под ней (зона переименована, классы пункта подменены, пункт снят) и дальше спрашивается бот — кнопки зон и классов через настоящий диспетчер, названия зон в показе записи, узнавание зоны в словах, вопрос пункта в отказе, состав информационной части. 6 порч, ровно по одной красной на каждую: снятие `chat_id` в любом из шести мест краснит свой тест и только его. Файлы вернулись байт в байт (md5 сверены по всем пяти) |
| T225 (граница) | сам | найдено, не моё | Ручной перечень и предложения модели по-прежнему собираются по ДЕЙСТВУЮЩЕЙ методике: их строит блок `recognize` (`manual.py`, `shortlist.py`, `prompt.py`, `fastpath.py`, `classify.py`, `schema.py`), и это задача T226, не T225. То есть после переиздания аудитору всё ещё может быть предложен пункт не его издания — но уже только этим путём. Тестом это здесь не закреплялось намеренно: закреплять чужой дефект значило бы мешать блоку `recognize` его чинить |
| T217 (доклад) | сам | попутно | Устаревшая после T217 строка «кнопка в 34 знака» правлена ещё в двух местах: докстринг `_show_manual_page` в `routers/record.py` и контракт блока в этом файле |
| T217 (префикс класса) | сам | готово | Нашлось смоуком, а не чтением задачи: управляющая компания ставит классы в начало САМОЙ формулировки — `(D1, D2) …`, — и на кнопке это ещё 9–13 знаков поверх кода. Считал: на боевой методике префикс встречается 62 раза на двух языках и во ВСЕХ 62 совпадает с колонкой `levels`, которую продукт и так читает и предлагает кнопками следующим шагом. Снимается поэтому только доказанный дубль (`view.without_level_prefix`): содержимое скобки сверяется с `levels` этого пункта, не совпало — скобка остаётся целой. Списка классов в коде нет, сравнивается строка методики со строкой методики |
| T217 (префикс, тесты) | сам | готово | 4 случая: дубль снят, несовпавшая скобка цела, скобка не из классов цела, из одной скобки пустой кнопки не выходит; плюс сторож на боевой методике под `requires_data` — на кнопках префикса не осталось ни на одном языке |
| T225 (промах, пойманный полным прогоном) | сам | найдено и починено | Обязательный `chat_id` у `zones.zone_from_words` уронил 10 тестов замера быстрого пути: `tools/fastpath_measure.py` зовёт его по выгрузкам `examples/`, где живой проверки нет вовсе. Тип расширен до `int | None` БЕЗ умолчания: «проверки нет» осталось законным ответом, а «забыли передать» по-прежнему не собирается. Замер называет пустой чат явной константой `NO_CHAT` с причиной. Промах мой: блочные прогоны были зелёные, поймал полный |
| T225 (щель, найденная порчей) | сам | готово | Седьмое место — кнопки зон на самой фиксации (`_ask_zone` в `routers/record.py`) — снятие чата не красило НИЧЕГО во всём наборе: дорога туда длиннее (кадр без слов, разбор, «Выбрать пункт»), и её никто не проходил. Заведён седьмой случай, он эту порчу и ловит. Вывод не про этот блок: совпадение «правка сделана» и «правка сторожится» надо проверять порчей по каждому месту отдельно, а не по задаче целиком |
| Полный прогон | сам | готово | `make test`: 1873 прошли, 230 пропущены, 0 красных, покрытие 94 % (6307 строк, 391 не покрыта), 3 минуты 18 секунд. Строки «ПРОГОН НЕПОЛНЫЙ» нет — `data/` и `examples/` на месте. Первый прогон был красным (10 тестов замера) и нашёл мой промах с обязательным `chat_id` в инструменте; второй, после починки, зелёный |
| Регресс | сам | готово | `make regress`: belgrade-1 — 97.5 % A, D1 = 5; belgrade-2 — 97 % A, D1 = 6. Дословно как в брифе |
| Смоук | сам | готово | Настоящий диспетчер, настоящий движок подпроцессом, синтетическая методика в отдельной копии, 18 проверок: альбом из трёх кадров закрыт событием и дал ОДНУ запись со всеми тремя кадрами; показ подписан «В отчёт» и без «ваших слов» (T219); повтор пары → отказ → ответ словами на отказ поправил запись #1 (CLN02/dishwashing), записей не прибавилось, сторонний кадр не уехал (T227); следующие слова забрали именно этот кадр; переиздание зоны посреди проверки не изменило ни кнопок зон, ни кнопок классов (T225); ручной перечень через диспетчер — 8 кнопок, префикса класса нет, самая длинная подпись 60 знаков вместо 34, все восемь показаны целиком (T217); оценку посчитал движок. Модель подменена — боевого ключа в копии нет, живого замера качества не было |
| Двенадцатая очередь | сам | — | Копия `feat/bot19`, задачи T209 (меню бота) и T241 (придержанные слова при завершении). Обе про то, что человек ВИДИТ, а не про механику разбора: состав меню назван владельцем, а потеря слов — то же место, что T068, только с другой стороны очереди |
| T209 (#176) | сам | готово | Меню телеграма пополнено пунктом «Установка MCP» (`/mcp`, `routers/mcp.py`), а `/start` назван словом владельца — «Новая проверка», тем же, что стоит на кнопке мастера. Пункт отдаёт объяснение и команду РАЗНЫМИ сообщениями: сообщение копируется целиком, и склеенный текст уехал бы в терминал вместе с командой. Токена в команде нет по разбору выше; адрес — из `BOT_MCP_URL`, не задан — заглушка. Снятия сданной проверки в меню нет (D086), и это сторожится тестом состава, а не памятью. Исполнителю не отдано: вся задача — решение про токен и текст, который его объясняет, а готового контракта тут не было |
| T241 (#197) | сам, тесты — исполнитель | готово | Слова, не дождавшиеся кадра, называются при завершении и по «Что записано»: `ChatMaterialQueue.held_comments()` отдаёт снимок очереди, показ живёт в `records.show_records` рядом с кадрами без записи (две копии одного списка разошлись бы молча — T147), очередь ожидания доезжает до обоих роутеров параметром. Текст возвращается текстом, голосовое — самим голосовым: расшифровки у придержанного голоса нет, она делается при сборке записи. Пустое придержанное отсеивается ДО показа, иначе счёт в заголовке разошёлся бы с показанным. Тесты отданы исполнителю: контракт готов (сигнатуры, ключи текстов, восемь случаев), образец рядом (`test_bot_unclaimed_frames_shown.py`), объём — один файл однотипных случаев |
| T241 (порча) | сам | готово | 7 порч, 7 адресных покраснений: слова не отдаются наружу, показ не зовётся, показ пустошит очередь, голосовое пересказано текстом, предел не соблюдён, отказ телеграма проглочен, порядок перевёрнут. Сторож обратной лжи (`слова_дождавшиеся_кадра_не_называются`) на порчах «ничего не показано» обязан был остаться зелёным и остался. `PYTHONDONTWRITEBYTECODE=1`, восстановление копией, md5 сверен на каждой |
| T246 (пункт меню) | сам | готово | Версия сборки объявлена в меню: соседняя сессия роутер и текст сделала, а `MENU_COMMANDS` намеренно не трогала, чтобы не столкнуться в одном списке. Пункт добавлен при сведении `main` вместе с тестом состава меню целиком — команда, заведённая роутером и забытая в меню, это ровно та спрятанная функция, из-за которой показ записанного жил за «Завершить» (T139) |
| Полный прогон | сам | готово | `pytest` с поднятой базой блока (`DATABASE_URL` в оболочке, иначе 282 теста db и mcp пропускаются молча): **2345 прошли, 0 пропущено, 0 красных, покрытие 98 %** (6693 строки, 151 не покрыта), 4 минуты 43 секунды. Затронутое: `routers/mcp.py`, `routers/records.py`, `material.py`, `texts.py` — 100 %, `routers/finish.py` — 98 %, `app.py` — 97 %. Гейты зелёные все: `ruff format`, `ruff check`, `mypy --strict`, `vulture`, `deptry`, `lint-imports` |
| Регресс | сам | готово | `make regress`: belgrade-1 — 97.5 % A, D1 = 5; belgrade-2 — 97 % A, D1 = 6. Дословно как в брифе |
| Смоук | сам | готово | Настоящий диспетчер, настоящий движок подпроцессом, синтетическая методика в отдельной копии. Меню объявило все шесть команд; `/mcp` отдал объяснение и команду ДВУМЯ сообщениями, токен из `MCP_TOKENS` в переписку не попал; мастер прошёл целиком; двое слов без кадра придержаны, кадр забрал старейшие (осталось одно); `/records` и `/finish` назвали и кадр без записи (кадром), и слова без кадра (голосовым); кадр, присланный ПОСЛЕ итога, забрал придержанное голосовое — показ очередь не съел. Модель подменять не пришлось: боевого ключа в копии нет, и путь разбора честно упёрся в её отсутствие |
