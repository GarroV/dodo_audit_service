# Блок: recognize

## Назначение

Превратить комментарий аудитора (и, если нужно, кадр) в несколько предложений: какой пункт, какой класс, какая зона. Предложения, а не решения — фиксирует человек.

## API-контракт

```python
classify(note: str, photo: bytes | None, zone_hint: str | None, *, chat_id: int | None) -> Suggestion
transcribe(audio: bytes) -> str
manual_candidates(zone_hint: str | None, *, lang: str = "ru", chat_id: int | None) -> tuple[ManualCandidate, ...]
fast_path(note: str, zone_hint: str | None, *, lang: str = "ru", chat_id: int | None) -> FastPath
process_hint(note: str, *, lang: str = "ru", chat_id: int | None) -> ProcessHint | None
```

`chat_id` — **обязательный именованный параметр без умолчания** (T226): весь
разбор читает справочники и карту слов из издания ТОЙ проверки (T169), а не из
каталога, который лежит в `AUDIT_DATA_DIR` сейчас. Умолчания у него нет
намеренно — умолчание и есть тот молчаливый откат на действующую методику,
ради снятия которого параметр заведён. «Проверки нет» — законный ответ и
называется вслух константой `src.recognize.config.NO_CHAT`: так блок зовут
замеры по выгрузкам `examples/` и команды методики MCP. Тем же приёмом живёт
`src/bot/zones.py` (T225). Те же правила у внутренних функций блока
(`shortlist`, `picks_for`, `question_text`, `load_cues`, `class_thresholds`,
`column_words`, `cues_path`).

`Suggestion.candidates` — не более пяти вариантов `(code, level, zone, wording, confidence)`; `needs_human` поднимается, когда кандидатов нет или уверенность низкая. Пустой список — валидный ответ.

`classify` и `transcribe` поднимают отказ (`RecognizeError` и подклассы —
`src/recognize/errors.py`), а не возвращают его в значении: тип отказа решает,
что делает бот дальше. На `ModelUnavailable` бот зовёт `manual_candidates`
(T034) — тот же перечень пунктов, без сети и без ранжирования по словам, для
показа кнопками. `classify("", photo=..., zone_hint=...)` — разбор голого
кадра по нажатию «Разобрать» (T064, D043, D046); звать ли модель без
комментария вообще — решает бот, не этот блок.

`fast_path` (T113, D063) — быстрый путь **до** `classify`: слова однозначны →
`FastPath.item` содержит готовый пункт `(code, level, zone, title, cue)`, и
модель не зовётся вовсе; не однозначны → `item is None`, а `reason` говорит,
чего не хватило. Отказ здесь — обычный ответ, а не сбой, и исключений функция
не поднимает (кроме отказа `domain` на неизвестную зону, как у `shortlist`).
Порядок вызова — решение бота: `fast_path` ничего не знает о кнопках, а
`classify` не зовёт `fast_path` сам, иначе бот не отличил бы «пункт по словам»
от «предложение модели», а показываются они по-разному.

`process_hint` (T166, D077) — распознавание указания аудитора словарю
(«<описание>, это <процесс>»). Чистая функция **в дополнение** к разбору: её
не зовут ни `classify`, ни `fast_path`, она ничего не решает и ничего не
пополняет. Ответ `None` — не отказ, а «такой формы в комментарии нет».

Вызов модели изолирован за одной функцией: смена провайдера или модели — правка конфига.

## Зависимости

`domain` — оттуда берётся перечень допустимых кодов и классов.

## Definition of Done блока

- [x] Перечень сужается до кандидатов **до** вызова модели: по словам комментария и карте `data/photo-cues.md`. Все 136 кодов в запрос не уходят.
- [x] Код возвращается строго из переданного перечня — гарантируется схемой ответа, а не проверкой постфактум.
- [x] Класс возвращается только из разрешённых для этого пункта.
- [x] Зона берётся из комментария или подсказки, а не выводится из вида кадра.
- [x] Кадр анализируется, только когда комментария нет вовсе и аудитор нажал «Разобрать» (D043, D046, T064). Есть комментарий — разбирается комментарий, кадр в запрос не идёт (D081, T202): прежняя оговорка «неоднозначен или расходится с изображением» снята владельцем — «фото с комментм - обрабатываем коммент». Правило целиком живёт в `needs_photo(note)`, и бот его не повторяет своими словами, а спрашивает.
- [x] Правила формулировок из `docs/03-recording-rules.md` зашиты в промпт целиком.
- [x] Модель не пишет про масштаб за пределами кадра и не хвалит то, чего не проверяла, — проверено тестами на формулировках из разбора боевых ошибок.
- [x] Голосовое перекодируется и транскрибируется; сбой транскрипции не роняет проверку, а просит написать текстом.
- [x] Блок деградирует: при недоступной модели бот предлагает выбрать пункт кнопками вручную.
- [x] Однозначные слова аудитора дают пункт без вызова модели (T113, D063), и однозначность строгая: пять условий сразу, иначе разбор идёт как раньше. Доля срабатываний меряется командой, а не на глаз, и меряется так, как быстрый путь зовёт бот, — с зоной из слов аудитора, а не из эталонной записи (T125).
- [x] Названная человеком зона участвует в решении об однозначности: сужает коды **внутри** покрытой строки карты, но саму строку не отбрасывает — строка, которая в названной зоне не ведёт никуда, остаётся громким отказом (T143, #114). Разводятся только коды с непересекающимися перечнями зон.
- [x] Слова, которыми аудитор называет колонку строки карты («грязь это или поломка»), правит управляющая компания в самой карте; код держит встроенный минимум и **дополняется** картой, а не заменяется (T143, D077). Отсутствие раздела — не отказ (D068).
- [x] Порог различающего слова не зависит от размера карты: слово различающее, если ведёт не более чем к шести различным кодам, сколько бы строк его ни содержало (T142, #113). Обосновано замером на двух версиях карты, а не рассуждением.
- [x] Правила разбора слов — **данные**, а не константы в коде (T192, #157): стоп-слова, окончания, частицы отрицания, встроенный словарь колонок и заголовки разделов карты объявлены на каждый язык в `src/recognize/language_rules.json`. Английский разбирается теми же правилами, что русский; третий язык добавляется записью в файле правил, а не правкой кода.
- [x] Быстрый путь не записывает то, что аудитор ОТРИЦАЕТ (T195, #160): защита распространена со слов колонки на саму строку карты и сверяет отрицание с двух сторон. Замерена в обе стороны — и сколько отрицаний ловится, и сколько срабатываний при этом потеряно.
- [x] Остаток отрицания, названный замером, закрыт (T197, #160): основа, сказанная и отрицаемая в одной фразе разом, не считается ни сказанной, ни отрицаемой. На боевой карте пойманных отрицаний 94% → 96% (пропусков 25 → 17), срабатываний и НЕВЕРНЫХ — 1 и 0 до и после.
- [x] Указание аудитора «<описание>, это <процесс>» распознаётся чистой функцией **в дополнение** к разбору, ничего не перехватывая и ничего не пополняя автоматически (T166, D077). Доля ложных срабатываний меряется командой `make processhint`, и отсутствие боевого корпуса она называет словами, а не показывает нулём.
- [x] Связка признака — слово языка аудитора, а не русская константа (T196, #161): объявлена на каждый язык в правилах и складывается, как и остальные правила разбора. Английская связка не взята на веру — под неё найден корпус того же рода, каким меряется русская.
- [x] Разбор идёт по изданию ТОЙ проверки, а не по действующему каталогу (T226, #181): пункты, классы, зоны, имена процессов и карта слов читаются через `chat_id`, который обязателен и умолчания не имеет. Карта входит в отпечаток издания (D063/D064), поэтому мимо издания её читать нельзя вдвойне: по ней быстрый путь показывает пункт, а такая запись становится находкой в отчёте партнёру. «Проверки нет» — отдельный названный ответ (`NO_CHAT`), а не то же самое, что забытый параметр. Проверено опытом: проверка заводится, методика под ней переиздаётся, и разбор идёт по её изданию (`tests/test_recognize_edition_pinned.py`).

## Статус

`done`

## Как устроено

| Модуль | Что делает |
|---|---|
| `src/recognize/cues.py` | Карта кадров `data/photo-cues.md`: строки «что видно → пункты», колонки строки, пороги классов для промпта. Карта берётся из издания той проверки, а не из действующего каталога (T226): она входит в отпечаток издания (D063/D064) |
| `src/recognize/shortlist.py` | Перечень кандидатов для запроса: зональная база плюс подсказки карты |
| `src/recognize/errors.py` | Отказы блока: нет карты, недоступна модель, не разобрано голосовое |
| `src/recognize/config.py` | Настройки из окружения: модель, ключ, таймаут, пороги — правка `.env`, не кода. Там же `NO_CHAT` — названный вслух ответ «проверки нет» для обязательного `chat_id` (T226) |
| `src/recognize/schema.py` | Строгая JSON-схема ответа: код и класс склеены в одно перечислимое поле `item` |
| `src/recognize/prompt.py` | Текст запроса: правила фиксации из `docs/03-recording-rules.md`, перечень кандидатов, три тона про кадр (нет / с комментарием / без него по «Разобрать») |
| `src/recognize/client.py` | Единственное место, где блок зовёт провайдера; любой отказ — в `ModelUnavailable` |
| `src/recognize/classify.py` | `classify()` целиком: сузить → собрать схему → спросить модель → разобрать ответ |
| `src/recognize/rules_check.py` | Второй слой после промпта: помечает формулировку, если модель всё же дописала масштаб, додумала повреждение или похвалила без основания |
| `src/recognize/manual.py` | `manual_candidates()` — деградация без сети, перечень для кнопок (T034) |
| `src/recognize/fastpath.py` | `fast_path()` — пункт без вызова модели, когда слова однозначны (T113); зона разводит коды внутри строки карты (T143); встроенный минимум слов колонок берётся из правил языка (T192); отрицание сверяется и в строке карты, и в словах аудитора (T195); основа, сказанная и отрицаемая в одной фразе разом, не считается ни сказанной, ни отрицаемой (T197) |
| `src/recognize/language.py` | Правила разбора слов по языкам: чтение и проверка `language_rules.json`, сложение правил всех языков (T192). Кода, знающего список языков, в продукте больше нет |
| `src/recognize/language_rules.json` | Сами правила: стоп-слова, окончания, частицы отрицания **с направлением** (T195), связки указания процесса (T196), встроенный словарь колонок, заголовки разделов карты — на каждый язык, с полем `about` о том, откуда взяты слова |
| `src/recognize/process_hint.py` | `process_hint()` — распознаёт указание аудитора «<описание>, это <процесс>» (T166, D077); связка-слово берётся из правил языка, символы «=», «→» — из кода (T196). Чистая функция В ДОПОЛНЕНИЕ к разбору: ничего не решает, ничего не перехватывает, ничего не пополняет |
| `src/recognize/transcribe.py` | Голосовое → текст: перекодировка `ffmpeg`, вызов транскрипции, отказ в `TranscriptionFailed` |
| `src/recognize/models.py` | `Suggestion`, `Candidate` — что блок отдаёт наружу |
| `tools/bench_dataset.py` | Офлайн-набор из 31 кадра боевых проверок с эталонным ответом (для T035) |
| `tools/bench_run.py` | Прогон набора через `classify()` на нескольких моделях, числа точности |
| `tools/process_hint_measure.py` | `make processhint` — доля ложных срабатываний признака «это <процесс>» по корпусам, каждый подписан тем, чем НЕ является и на каком языке написан (T196). Детерминированный, денег не стоит |
| `tools/fastpath_measure.py` | `make fastpath` — доля срабатываний на боевых записях, зона добывается **как у бота** (T125), плюс замер защиты от отрицания: сколько отрицаний ловится и сколько срабатываний при этом теряется (T195). Детерминированный, денег не стоит |
| `tools/comment_only_measure.py` | Замер цены решения D081 (T202): один вход через два плеча — «комментарий и кадр по старому правилу» против «только комментарий». **Платный**, 34 вызова модели, и запускается только по решению владельца; цели в `Makefile` намеренно нет. Требует `STATE_DIR` (методику читают оба плеча), не задан — внятный отказ и код 2. Коды: 0 — прогон прошёл, 1 — не удался ни один вызов, 2 — данных нет или окружение не настроено; при любом отказе модели отчёт говорит вслух, что числа замером не являются (T224) |

### Решения, принятые внутри блока

**Перечень собирается сложением, а не пересечением.** База — все пункты-нарушения,
применимые к зоне (для горячего цеха это 77 кодов из 123). Карта кадров только
поднимает свои коды вперёд и **добавляет** те, которых в зоне нет. Причина —
`docs/forge/research/recognize-probe.md`: на срезанном перечне модель не отказалась, а
уверенно предложила похожий пункт. Пересечение зоны с картой давало бы такой срез на
каждом кадре, где аудитор описал объект неожиданным словом.

**`INF09`, `INF10`, `INF11` остаются в перечне.** В методике они заведены как
`kind=violation` с единственным классом `D0` — это замеры и фото продукта, которые в
отчёте нужны. Убрать их значило бы заставить модель натягивать настоящее нарушение на
кадр с дисплеем термометра. Служебные `kind=aggregate` и `kind=info` (`INF01`–`INF08`)
не предлагаются — правило 8 из `docs/03-recording-rules.md`.

**`MGM22` и `MGM23` модели не показываются** — по карте кадров это ручное решение
аудитора. В перечне для ручного выбора кнопками (`with_manual=True`) они есть.

**Совпадение слов — по усечённой основе, без морфологического разбора.** «печи» и
«Печь», «маркировкой» и «маркировки» сводятся к одному ключу отсечением окончаний.
Подсказка срабатывает при двух совпавших словах или одном различающем — иначе слово
«продукт» поднимало бы полкарты. Различающим считается слово, ведущее не более чем к
шести **различным кодам**; в скольких строках карты оно встречается, значения не имеет
(T142, см. ниже).

**Без `data/photo-cues.md` блок работает, но медленнее** (T157, D068). Прежде он
отказывался (`RecognizeConfigError`), и отказ всплывал не при подъёме стенда, а в
чате у аудитора на первом же комментарии. Теперь пустая карта означает «ни одна
строка не произнесена»: быстрый путь молчит, разбор идёт моделью. Перечень при
этом не режется — карта его только дополняет и переставляет, поэтому опасение
про тихо усечённый вход сюда не относится.

**Деградация без модели — отказ, а не флаг в ответе (T034).** `classify` и
`transcribe` поднимают исключение (`ModelUnavailable`, `TranscriptionFailed`),
блок не пытается сам решить, что показать вместо предложения: это решение бота
(кнопки ручного выбора vs. переспросить текстом). `manual_candidates` — готовый
перечень для первого случая: без сети, без ранжирования по словам (комментарий
взять неоткуда — модель как раз недоступна), с теми же зональными границами,
что и у запроса к модели, и с `MGM22`/`MGM23`, которые модели не показывают
никогда.

**Кадр без комментария разбирается по кнопке, а не сам собой (T064, D043,
D046).** Решение звать ли модель на голый кадр — у бота («Разобрать?» с
кнопкой), но текст запроса и разбор ответа — здесь: `classify("", photo=...)`
не отказывается и не требует непустого комментария, а `prompt.py` меняет тон
инструкции про кадр на «посмотри сам, найди то, что стоит зафиксировать,
пунктами, коротко, без воды» — иначе модель отвечает так же многословно, как
на уточнение к словам аудитора, и аудитор не успевает прочитать на ходу.
Формулировки при этом проходят те же правила фиксации и ту же пометку
(`rules_check.py`), что и обычный разбор — освобождения от правил 2–4 у
самостоятельного разбора нет.

**Выбор модели подтверждён замером, а не остался мнением (T035, D013).**
Прогон `tools/bench_run.py` на 31 кадре двух боевых проверок (`examples/`),
три модели, вызов `classify("", photo=..., zone_hint=case.zone)` — ровно то,
что делает бот по кнопке «Разобрать»:

| Модель | Кадров | Код верный | Код+класс верно | Нужен человек | Ошибок | Токены вход/выход (среднее) |
|---|---|---|---|---|---|---|
| `gpt-5.6-sol` (флагман, по умолчанию) | 31 | 94% | 94% | 10% | 0 | 7179 / 208 |
| `gpt-5.4-mini` (средняя) | 31 | 77% | 77% | 3% | 0 | 7179 / 94 |
| `gpt-4o-mini` (дешёвая) | 31 | 23% | 23% | 10% | 0 | 42694 / 106 |

Замер от 02.09.2026, прогон занял 80 секунд. Числа подтверждают D013: разница
между флагманом и средней моделью — 17 п.п. точности по коду, а не единицы;
дешёвая модель (D013 её уже отклонило по цене) здесь ещё и втрое хуже по
точности и неожиданно дороже по входным токенам — на этом кадре и промпте её
кодирование изображения обходится в 5–6 раз дороже, чем у двух других моделей.
`needs_human` не коррелирует с точностью впрямую (у средней модели он ниже, чем
у флагмана, при более низкой точности) — порог уверенности откалиброван на
разведке (`recognize-probe.md`), а не на этом наборе, и калибровка под три
модели сразу — вне рамок задачи. Подробный протокол по каждому кадру —
`reports/bench.json` (вне индекса, `reports/` в `.gitignore` — как и `examples/`,
это не то, что кладут в публичный репозиторий).

**Быстрый путь: что считается однозначностью (T113, D063).** Владелец просил
сверку со списком нарушений вместо рассуждения там, где думать не о чем.
Опасность при этом та же, что у сужения перечня, только острее: без модели
неверный пункт выглядит так же убедительно, как с ней, а аудитор подтверждает
его одним нажатием — предложение становится решением за него. Поэтому
однозначность — совпадение **пяти условий сразу**, и каждое отсекает свой
способ ошибиться:

1. **Зона названа человеком.** Правило 6: зону определяет аудитор, а не догадка.
2. **Строка карты произнесена целиком — и утвердительно.** Все значимые слова
   подсказки есть в словах аудитора. Порога сужения (два слова или одно
   различающее) для решения за человека мало: «Пол в курьерской зоне» задевает
   строку «Пол, стены, потолок, двери на кухне» одним словом из пяти, и по
   этому касанию нельзя показывать готовый пункт. **Отрицание сверяется с двух
   сторон (T195):** слово, отрицаемое аудитором, строку не покрывает («в одной
   таре НЕ смешаны разные партии» — это не смешение партий), а строка,
   сформулированная через отсутствие («Тара без ярлыка» — таких в боевой карте
   13 из 75), требует отрицания и в словах, иначе «тара с ярлыком» покрывала бы
   её ровно наоборот. Частица относится к ближайшему **значимому** слову в свою
   сторону — направление объявлено у каждой частицы в правилах языка — и не
   действует через знак препинания: «жалоб нет, в зале урна переполнена» — про
   жалобы, а не про урну.
3. **Строка даёт ровно один код.** Карта различает грязь и поломку одного
   объекта колонками («Печь | CLN05 | TEH05») и сама это объясняет — значит,
   слова обязаны сказать, о чём речь; сказали и то и другое — это две записи, а
   не одна (правило 11). Слово при отрицании не считается — тем же правилом,
   что и в условии 2: «печь без нагара» и «нагара нет» — не про нагар. Строка
   вида «PRD09, PRD11» — выбор, предложенный самой картой, и быстрым путём не
   проходит никогда.
4. **У пункта единственный допустимый класс.** Где классов несколько, разницу
   задаёт то, чего в словах нет: сколько упаковок, как долго, насколько
   массово. «Живые насекомые» — это D1 или D3 по массовости, и решает человек.
5. **Пункт применим к названной зоне.** Расхождение слов и места — работа для
   модели: «стена за кассовой стойкой» это зал, даже если рядом стоит ведро.

Формулировку быстрый путь не пишет вообще: на кнопке стоит вопрос чек-листа,
текстом записи остаются слова аудитора. Правила 2–4 (масштаб за кадром,
характер повреждения, похвала без основания) нарушить нечем — сочинять некому.

Рассмотрено и отклонено: **отбрасывать покрытые строки, чьи коды не применимы к
зоне**, вместо отказа. Это подняло бы долю (тот же «пол» дал бы `CLN12` в зале
и `CLN03` на кухне), но превратило бы главный сигнал «слова и место не сходятся»
в тихую подстановку, а комментарий про два объекта из разных зон — в один
уверенный ответ. Цена отказа — четыре секунды и вызов модели, то есть сегодняшний
порядок; цена неверного срабатывания — запись в отчёте партнёру, которую никто
не обдумывал.

Что остаётся риском и не закрывается механизмом: **правило 11 — один
комментарий может содержать несколько нарушений**. «Печь в нагаре, пол грязный»
покрывает строку «Печь» целиком, а строку про пол — нет, и быстрый путь покажет
один пункт из двух. Отсюда требования к боту (блок `bot`, не этот):

- звать `fast_path(note, zone)` **до** `classify`, а при `item is None` — как
  раньше, без изменений;
- показывать рядом с кнопкой подтверждения **сами слова аудитора** и строку
  карты (`item.cue`) — из чего сделан вывод, видно человеку, а не только логу;
- держать рядом вторую кнопку — «разобрать моделью»: быстрый путь не должен
  становиться единственным выходом, иначе аудитор с неполным предложением
  зажат в угол;
- фиксировать по подтверждению запись со словами аудитора как текстом и
  источником «со слов аудитора» — быстрый путь формулировок не даёт;
- `reason` человеку не показывать: он для замера и разбора, а не для экрана.

**Замер меряет то, что происходит на точке, а не удобную подстановку (T125,
задача #100).** Прежние «18%» были сняты с зоны, взятой из **эталонной
записи** — то есть из уже известного правильного ответа. У бота этого знания
нет: он берёт зону из слов комментария (`src/bot/zones.py: zone_from_words`), а
память о прошлой записи (D048) подставляет только тогда, когда о зоне в словах
не сказано ничего, — `src/bot/routers/record.py::_analyze`. Замер теперь зовёт
быстрый путь ровно так же и показывает три числа рядом, а не одно:

| Способ добыть зону | Срабатываний из 17 | Верных | Неверных |
|---|---|---|---|
| **Как зовёт бот** (слова, иначе память проверки) | **7 (41%)** | 7 | 0 |
| Только слова аудитора, без памяти | 4 (24%) | 4 | 0 |
| Эталонная зона из записи — верхняя граница, боту недостижима | 9 (53%) | 9 | 0 |

Замер от 05.09.2026, карта слов `data/photo-cues.md` версии
`md5 19d6b8acd6d572b1853e4ae16af35030` — пополненная карта применена задачей
T118. До неё те же 17 записей давали 1 (6%) у бота и 3 (18%) с эталонной зоной. **Числа живут до следующей правки
карты** (D066: карту ведёт управляющая компания), поэтому дату и отпечаток
карты замер печатает сам — число без версии карты через неделю нечем проверить.

Что видно из разбивки, кроме самой доли: зону из своих слов аудитор называет в
**6 записях из 17**, в 9 её подставляет память о прошлой записи, в 2 её нет
вовсе (первая запись проверки). Ограничитель после пополнения карты сменился:
из десяти отказов только два — «ни одна строка карты не произнесена целиком»,
зато четыре приходятся на неоднозначность («по словам не видно, грязь это или
поломка»), два — на «зона не названа» и два — на «пункт не применим к названной
зоне». То есть карта своё отработала, и следующий предел — не словарь, а
различение классов и знание зоны.

Память смоделирована зоной предыдущей записи той же проверки: бот запоминает
зону только что созданной записи (`sidecar.remember_zone`), а в
`examples/*/inspection.json` записи лежат в порядке создания и несут ту зону,
которая в итоге записана. На смене проверки память сбрасывается — у каждой свой
чат и свои заметки.

Команда `make fastpath` — детерминированная, денег не стоит, гоняется после
каждого пополнения карты слов. Проверка `main` возвращает ненулевой код, если
хотя бы в одном из трёх способов появилось **неверное** срабатывание: оно
опаснее отсутствия быстрого пути.

### Отрицание: почему правило именно такое (T195, задача #160)

Защита от отрицания стояла только там, где выбирается колонка («грязь» против
«поломки»). Строку карты она не проверяла вовсе, и на боевой методике
по-русски это выглядело так: строка карты про нарушение и та же строка с
частицей **НЕ** давали **один и тот же пункт**. Строк с одной
колонкой, где защиты не было вовсе, в боевой карте 54 из 75 (23 с ровно одним
кодом и 31, где несколько кодов стоят в одной колонке).

Правило выбрано **замером четырёх вариантов**, а не рассуждением, и мерялось
оно в обе стороны сразу: сколько отрицаний ловится и сколько срабатываний при
этом теряется. Корпус строится из самой карты на ходу — 412 отрицаний по
четырём образцам («не смешаны», «без нагара», «нагара нет», «не в одной таре»)
плюс три безобидных отрицания в другой части фразы, которые обязаны срабатывание
сохранить.

| Вариант правила | Поймано отрицаний | Потеряно строк на безобидном отрицании |
|---|---|---|
| Как было: отрицание видит только выбор колонки | 4% (17 из 388) | 0 из 30 |
| Соседнее слово по направлению частицы, знаки препинания не в счёт | не считалось — отброшен | **30 из 32** |
| Соседнее слово по направлению, частица не переходит знак препинания | 71% (294 из 412) | 0 из 32 |
| **Взято:** ближайшее ЗНАЧИМОЕ слово по направлению, без перехода знака | **94% (387 из 412)** | **0 из 32** |

Корпус у прежнего правила меньше (388 против 412) не по недосмотру: отрицания
строятся только от строк, которые срабатывают утвердительно, а таких у него 30,
а не 32.

Таблица — запись выбора, сделанного в T195, и числа в ней те, что были тогда.
Взятый вариант с тех пор ловит больше: **96% (395 из 412)** после T197, который
закрыл остаток, названный этим же замером (ниже, отдельным разделом).

Три вещи, которые этот замер показал и без которых правило было бы неверным:

* **Направление обязательно.** Частица «не» относится к тому, что стоит после
  неё: «печь не сломана» — про поломку, а не про печь. Правило, снимающее слово
  с обеих сторон, отбросило бы саму «печь», и вместо честного «по словам не
  видно, грязь это или поломка» аудитор получил бы подменённую причину отказа.
* **Знак препинания останавливает частицу.** Без этого «жалоб нет, в зале урна
  переполнена» теряет срабатывание, потому что «нет» и «урна» стоят рядом. На
  боевой карте так теряются **30 строк из 32** — быстрый путь обнуляется молча,
  а выглядит это как «стало безопаснее».
* **Служебные слова частица перешагивает.** «Не в одной таре» — то же отрицание,
  что «не смешаны», и правило «ровно соседнее слово» ловит одно и пропускает
  другое: 101 пропуск из 103 на этом образце.

**Сверка идёт с двух сторон.** Слово, отрицаемое строкой карты, обязано быть
отрицаемым и в словах аудитора: строк, сформулированных через отсутствие («Тара
без наклейки»), в боевой карте 13 из 75, и правило «есть отрицание — значит
отказ» отключило бы их все. Обратная сторона того же правила: «тара с ярлыком»
больше не покрывает строку «тара без ярлыка».

Цена на боевых данных — **нулевая**: 17 записей дают 1 срабатывание и 0 неверных
и до правки, и после; на черновике новой карты (T118) — 7 и 0 в обоих случаях.
Строк карты, срабатывающих утвердительно, стало не меньше, а больше: 30 → 32 на
боевой карте (правило направления вернуло две строки, которые прежняя защита
глушила сама), 171 → 171 на черновике.

**На черновике новой карты правило работает лучше, а не хуже** — это важно,
потому что применять собираются именно её: 171 строка с утвердительным
срабатыванием, 1092 отрицания, пропущено 26, поймано **98%**, потеряно на
безобидном отрицании **0 строк из 171**. Числа снимаются командой `make fastpath`
на той карте, которая подложена в `AUDIT_DATA_DIR`, — после правки карты они
пересчитываются, а не переписываются по памяти.

**Чего правило не ловит — 17 пропусков из 412 (было 25 до T197), и они разные:**

* **14** — строка карты сама сформулирована через отсутствие, и аудитор
  отрицает то же самое ещё раз. Это не промах: отрицание совпало с двух сторон,
  пункт правильный.
* **3** — отрицание перевело комментарий в строку-двойник, сформулированную
  через отрицание, и быстрый путь ответил пунктом для отрицаемого состояния.
  Тоже не промах, а лучший из возможных ответов.
* **8 было — и закрыто задачей T197**, см. ниже. Одна и та же основа встречалась
  в строке карты дважды («Зона мойки, оборудование мойки»), отрицалось одно
  вхождение, второе сохраняло утвердительность.

### Противоречие внутри фразы: остаток отрицания (T197, задача #160)

Разбор считает основы слов **наборами**, и число вхождений в наборе теряется.
Основа, встреченная во фразе дважды — раз под отрицанием, раз без него, —
попадала сразу в оба набора, а покрытие строки карты смотрит только на
утвердительный: `phrase_spoken <= spoken`. Утвердительное вхождение побеждало
всегда.

На точке это звучит так: «в зале урны нет, а в подсобке урна переполнена». Обе
половины — правда, вместе они означают, что в зале нарушения нет, а
записывался ровно противоположный пункт — про зал. Подтверждения такая запись
не требует (D064) и уходит в отчёт партнёру.

**Взято: основа, сказанная и отрицаемая разом, не считается ни сказанной, ни
отрицаемой.** Две строки в `_said`, и правило симметрично — то есть действует
и на комментарий, и на строку карты, потому что разбор у них один и тот же.
Выбор в пользу отказа, а не одной из сторон: фраза противоречива по словам,
решать такое за человека быстрый путь не берётся, а цена ошибки несимметрична
(срабатывание — запись в отчёте без подтверждения, отказ — вызов модели).

Замер `make fastpath` до и после, тем же способом, что у T195:

| Карта | Пропусков было / стало | Поймано | Срабатываний / НЕВЕРНЫХ | Потеряно на безобидном отрицании |
|---|---|---|---|---|
| боевая, 75 строк (`md5 790ebd69…`) | 25 / **17** из 412 | 94% → **96%** | 1 / 0 до и после | 0 из 32 до и после |
| черновик T118, 275 строк (`md5 19d6b8ac…`) | 26 / 26 из 1092 | 98% до и после | 7 / 0 до и после | 0 из 171 до и после |

**На черновике числа не сдвинулись, и это не осечка замера.** Списки пропусков
сверены построчно: на боевой карте ушли ровно восемь известных строк и ни одной
другой, на черновике список не изменился ни на строку — там ни одна строка не
несёт повторённой основы. Строк с утвердительным срабатыванием осталось столько
же на обеих картах (32 и 171): правило только снимает возможности и ни одной не
добавляет, поэтому потерять срабатывание оно могло бы, а придумать новое — нет.

**Отрицание без частицы этой правкой по-прежнему не ловится** — и не может
ловиться, см. следующий абзац: оно закрывается словарём управляющей компании, а
не разбором.

**Отрицание без частицы не ловится вовсе и ловиться этим правилом не может.**
«Печь чистая, а вот пол грязный» — здесь отрицания нет, есть утверждение о
норме и второй объект. Если второй объект в карте есть, спасает условие «слова
указывают на несколько пунктов»; если нет («печь чистая, а подоконник
грязный»), быстрый путь покажет пункт про печь. Закрыть это можно только
словами, которыми аудитор говорит «всё в порядке», а этот словарь — знание
управляющей компании (D077), и выдумывать его за неё нельзя. Место для него в
карте уже есть — тот же механизм, что у раздела про слова колонок.

Замер зовёт `src/bot/zones.py` из блока `bot`, а не заводит свою копию правил:
разъехавшись, копия молча вернула бы замер к измерению не того, что происходит
на точке. Контракт импортов это направление не запрещает и не разрешает — он
описывает пакет `src`, а `tools/` в его граф не входит вовсе (проверено:
`import engine.audit`, запрещённый для `src`, в `tools/` проходит молча).
Правильное место для разбора зоны по слоям — блок `recognize` рядом с
`cues.stems`, которым он и так пользуется, а `bot` оставил бы себе только
обиходные названия зон; это правка чужого блока, и здесь она не сделана.

**Что предлагается внести в `data/photo-cues.md`** (карта — документ
управляющей компании, вне git; здесь только предложение, проверенное на
временной копии). Долю поднимает не код, а карта, и ровно одним приёмом:
**строка на один объект и одно его название**. Перечисление «Рабочий стол,
столешница» требует, чтобы аудитор произнёс оба слова, и не срабатывает
никогда; две отдельные строки срабатывают. Кандидаты взяты из формулировок
боевых проверок, а не придуманы за УК:

| Строка | Колонки | Откуда взято |
|---|---|---|
| Рабочий стол | `CLN06` \| `TEH03` | Белград-1: «Загрязнение рабочего стола в холодном цехе» |
| Столешница | `CLN06` \| `TEH03` | то же обобщение карты, второе название |
| Тестомес | `CLN06` \| `TEH03` | Белград-2: «Тестомес не накрыт… на стенках и спирали налипшее тесто» |
| Дежа | `CLN06` \| `TEH03` | там же: «в дежу падают остатки продуктов» |
| Запайщик стаканов | `CLN06` \| `TEH03` | Белград-2: «Запайщик стаканов: пригоревший налёт по кромке» |
| Посуда | `CLN01` \| `TEH01` | разведение перечисления «Инвентарь, посуда, гастроёмкости» |
| Гастроёмкости | `CLN01` \| `TEH01` | то же перечисление |

С этими семью строками замер на той же временной копии даёт (пересчитано
04.09.2026 уже честным замером, боевая карта не тронута):

| Способ добыть зону | Срабатываний из 17 | Неверных |
|---|---|---|
| **Как зовёт бот** | **4 (24%)** | 0 |
| Только слова аудитора | 3 (18%) | 0 |
| Эталонная зона — верхняя граница | 6 (35%) | 0 |

Прежнее «35%» из этого абзаца и из D065 — это последняя строка таблицы, то
есть верхняя граница, а не то, что увидит аудитор. В боевую карту строки не
внесены: это данные УК (D066), и карту прямо сейчас составляет заново сама
управляющая компания — после её правки замер снимается заново.

Чего в карту вносить не нужно: строк ради пунктов с выбором класса (`PRD09`,
`PRD10`, `FSB03`, `FSB05`, `PRD01`, `FSB17`, `SFT02`, `MGM19`, `SFT07`…) —
условие 4 отсекает их в любом случае, и это правильно. И строк, где один и тот
же объект даёт разные пункты по зонам («пол» — это `CLN03` на кухне и `CLN12` в
зале): общая строка и зональная перекрывают друг друга, и быстрый путь
откажется по условию 3 либо 5.

**Зона участвует в решении об однозначности, но строки по ней не отбрасываются
(T143, задача #114).** Прежний порядок проверял зону последней — уже для
единственного выжившего кода, — и карта из-за этого не могла держать один
объект с разными пунктами по местам. Теперь зона сужает коды **внутри**
покрытой строки, и только те, чьи перечни зон **не пересекаются**: тогда строка
не предлагает выбор, а перечисляет один объект по местам. Пересеклись — это
выбор самой карты («PRD09, PRD11»), и прежний запрет на такие строки остался в
силе как следствие правила, а не отдельным пунктом.

Отбрасывать покрытые строки, чьи коды в зоне не применимы, снова рассмотрено и
снова отклонено — теперь числом. Замер на 17 боевых записях, карта-черновик
T118 (`md5 19d6b8acd6d572b1853e4ae16af35030`), зона как её берёт бот:

| Что делаем со строкой вне зоны | Срабатываний из 17 | Неверных |
|---|---|---|
| Отказ `WRONG_ZONE` (сделано) | 7 | 0 |
| Отбросить строку и ответить по соседней | 8 | **1** |

Восьмое срабатывание и есть неверное: belgrade-2, эталон `CLN03` в зоне
`dining`, быстрый путь показал бы `CLN12`. Причина — расхождение самих данных:
запись эталонной проверки ставит `CLN03` в зону, которой у `CLN03` в методике
нет (его зоны — производственные). Пока такое расхождение существует, тихая
подстановка по зоне покупает ровно одно срабатывание и оно ошибочное.

**Что это дало на боевых данных: ничего, и это ожидаемо.** Доля не сдвинулась —
7 из 17 на черновике карты и 1 из 17 на боевой, ноль неверных в обоих случаях.
Задача снимает ограничение с **карты**, а не с кода: сегодня ни одна строка не
ведёт в несколько пунктов, потому что такую строку до этой задачи было
бессмысленно писать. Изменилась разбивка причин отказа: два отказа «слова
указывают на несколько пунктов» стали «пункт не применим к названной зоне» —
тот самый сигнал «слова и место не сходятся», ради которого он громкий.

**Предупреждение для карты, проверенное на копии.** Напрашивающаяся строка
`Пол | CLN03, CLN12 | TEH02, TEH08` на тех же 17 записях даёт 8 срабатываний
против 7 и **одно неверное** — то же расхождение `CLN03` в `dining`. То есть
заводить зональную развилку под «пол» сегодня нельзя; сначала надо понять, что
верно — зоны `CLN03` в методике или зона в записи. Это вопрос к управляющей
компании, а не решение блока.

**Слова колонок переехали в карту, к тому, кто их знает (T143, вторая
половина).** Словари `DIRT_WORDS` и `BREAKAGE_WORDS` остаются в коде как
**встроенный минимум**, а карта получила раздел `## Слова, которыми аудитор
называет колонку` — таблицу «колонка → слова». Карта **дополняет** словарь, а
не заменяет (владелец про этот механизм сказал «дополняем наш список терминов»,
D077): раздел, забытый в новой версии карты, не должен молча отключать
различение грязи и поломки. Раздела нет — работает минимум, отказа нет (D068).

Повод был не теоретический: на боевой карте самая частая причина отказа —
«по словам не видно, грязь это или поломка», 4 записи из 17 на черновике T118,
и слова управляющей компании «затирки», «потёртости», «затёртости»,
«налипшее» не попадают ни в один встроенный кортеж. **Но замер показывает, что
дело не только в словаре:** из этих четырёх отказов три — правильные (записи
`INF09` и `INF10` это замеры температуры, там нет ни грязи, ни поломки, и
показывать пункт про печь на них нельзя), а четвёртый упирается в неверную
зону из памяти. То есть словарь надо было отдать УК потому, что он её, а не
потому, что это поднимет долю: на сегодняшних боевых записях оно не поднимает
её ни на одну.

**Порог различающего слова считается по кодам, а не по строкам и не по их доле
(T142, задача #113).** Задача звучала как «считать порог долей карты, а не
абсолютным числом»: при 75 строках порог в три строки — это 4 % карты, при 275
строках то же число отнимает силу у обычных слов молча. Замер показал, что
доля лечит симптом и ломает главное, поэтому заменено не число, а величина.

Что даёт доля 4 % на карте-черновике T118 (275 строк, порог 11 строк):
различающими становятся **все 417 основ карты**, то есть отсечка перестаёт
работать целиком. Видно на словах: `match_cues("продукт испорчен")` начинает
поднимать 10 кодов вместо нуля, `match_cues("открытая упаковка")` — 12 вместо
пяти, голова перечня на 17 боевых записях растёт с 7,5 кода до 10,2. Это ровно
та беда, против которой порог и заведён.

Считать надо многозначность самого слова: цена срабатывания — коды, вынесенные
в голову перечня, а не строки, которые их дали. Пять строк «Печь»,
«Конвейерная лента», «Подина», «Загрузочное окно», «Печной привод» — это один
объект, расписанный подробнее, и все ведут в ту же пару `CLN05`/`TEH05`; слово
«печь» после такого расписывания указывать хуже не стало. Размер карты в
правило не входит вовсе, поэтому требование задачи («слово, различающее при 75
строках, обязано остаться различающим при 500») выполняется не подбором
коэффициента.

Порог — шесть кодов, выбран по замеру на обеих версиях карты. Строка обычно
несёт два кода («Грязь | Поломка»), то есть шесть — это около трёх объектов;
дальше слово называет уже категорию, а не объект. Замер:

| Карта | Различающих основ было / стало | Перестали | Стали | Голова перечня на 17 записях |
|---|---|---|---|---|
| боевая, 75 строк (`md5 790ebd69…`) | 224 / 225 из 230 | `интерьер` | `наклейк`, `холодильник` | 5,5 → 5,5 кода |
| черновик T118, 275 строк (`md5 19d6b8ac…`) | 410 / 412 из 417 | — | `заказ`, `наклейк` | 7,5 → 7,8 кода |

Верный код поднимается картой в 14 записях из 17 на черновике и в 7 из 17 на
боевой карте — до правки столько же: на этих записях правило ничего не меняет,
и это ожидаемо. Записи `examples/` — отшлифованные формулировки в несколько
строк, они задевают нужную строку двумя словами и до порога различающего слова
не доходят вовсе. Порог живёт на коротких комментариях, какими говорят на
точке («печи не мыли», «заказ выдан»), а их в боевых данных нет — их не хранил
продукт (T183 это чинит, но корпуса ещё нет).

**Быстрый путь этой правкой не задет ни на одну запись:** он покрывает строку
целиком (`_covered`), порога различающего слова в его условиях нет. `make
fastpath` до и после — 1 из 17 на боевой карте, 7 из 17 на черновике, ноль
неверных.

**Признак «это <процесс>» распознаётся, но ничего не решает (T166, D077).**
Владелец, дословно: «при несостыковках, или если пользователь добавит что-то в
духе "ГРЯЗЬ НА ПОЛКЕ В ГОРЯЧЕМ ЦЕХЕ, ЭТО ЧИСТОТА" то мы долполняем наш список
терминов». Предыдущий заход честно отказался: признак проверяем, но мерить его
было не на чем — сырых слов аудитора продукт не хранил нигде. T183 хранение
завёл, и порядок, предложенный тем заходом, стал выполнимым.

Признак двухчастный: в комментарии стоит **связка** (слово языка аудитора —
«это» или «is», либо символ «=», «→»), а хвост
после неё — **ровно** имя процесса методики, по основам слов. Имена читаются из
методики на каждом вызове, как названия зон: переименованный управляющей
компанией процесс обязан начать узнаваться сам. Тире связкой не считается
намеренно — оно стоит в обычных комментариях сплошь и рядом («нагар — жирные
потёки»), и на нём признак стал бы шумом.

Два запрета сильнее самого признака, и оба соблюдены устройством функции.
Первый: **поток не перехватывается** — `process_hint` чистая, `classify` и
`fast_path` её не зовут и о ней не знают; аудитор, имевший в виду нарушение,
записи не теряет. Второй: **ничего не пополняется автоматически** (D077) — сбор
предложений для управляющей компании остаётся отдельной задачей (T165).

**Замер ложных срабатываний — `make processhint`, 05.09.2026:**

| Корпус | Текстов | С именем процесса | Со связкой | Сработал |
|---|---|---|---|---|
| Сырые слова аудитора (T183) | **0** | 0 | 0 | 0 |
| Записи боевых проверок: формулировки и комментарии партнёру | 17 | 0 | 0 | 0 |
| Вопросы методики по-русски | 136 | 8 | 0 | 0 |
| Вопросы методики по-английски (T196) | 136 | 14 | 31 | 0 |
| Критерии классов, построчно | 874 | 16 | 9 | 0 |

Что из этого следует, дословно: на 1027 текстах методики и боевых проверок
признак не сработал **ни разу**, тогда как одно имя процесса без связки
сработало бы **24 раза**. То есть связка и есть то, что делает форму пригодной,
а не имя процесса; отказ предыдущего захода был про признак «названо имя
процесса», и он верен — сам по себе тот признак ошибается 24 раза из 1027.

**Чего этот замер НЕ говорит, и это важнее чисел.** Ни один из четырёх корпусов
не мог содержать настоящего указания словарю: в записях и вопросах методики его
неоткуда взять. Значит померено не «как часто признак ошибается», а «как редко
эта форма встречается в речи о нарушениях вообще» — 0 из 1027. Доля ложных
срабатываний считается только по сырым словам аудитора, а их накоплено **ноль**:
хранение завели вчера, продукт на точке ещё не работал. Замер это печатает
словами, а не показывает успокоительный ноль, и начнёт считать сам, как только
слова появятся.

**Вывод, который стоит держать в голове до того замера.** Форма редкая, значит
распознавать её дёшево. Но что она означает — «научи словарь» или «запиши
нарушение» — из текста не решается: «просрочка на полке, это пищевая
безопасность» одинаково хорошо читается обоими способами, и это не недоделка
разбора, а свойство самой фразы. Поэтому признак остаётся **дополнением**, а
боевой список слов правит управляющая компания (D077).

**Английская связка взята после того, как нашёлся корпус (T196, #161).**
Предыдущий заход отказался её делать и назвал причину: признак держится на
замере, а корпуса все русские — «is» замеру не поддаётся ни по чему, и есть
вопрос, не слишком ли она частотна. Довод верный по форме, но фактически не
подтвердился: `data/checklist.csv` несёт колонку `question_en`, то есть речь
управляющей компании о нарушениях **на втором языке** лежала рядом всё это
время. Это корпус ровно того же рода, каким меряется русская связка, и замер
брал из той же таблицы только русскую колонку.

Что он показал: 136 английских вопросов методики, в 31 из них стоит «is»,
признак не сработал **ни разу**. Ограничитель здесь не связка, а хвост — после
неё должно стоять ровно имя процесса и ничего больше, — и частотность «is»
против этого правила бессильна. Русские корпуса от добавления связки не
сдвинулись ни на один текст: «is» не встречается в них нигде.

Чего этот замер по-прежнему **не** говорит — того же, чего не говорит русский:
живой английской речи аудитора не существует, боевых английских проверок нет,
и доля ложных срабатываний на них не посчитана ни по чему. Разница с прежним
положением в другом: раньше английская связка не была измерена **ничем**, а
теперь она измерена ровно тем же, чем русская. Цена ошибки здесь несравнима с
быстрым путём: `process_hint` ничего не решает и ничего не пополняет (D077),
лишнее срабатывание стоит одной строки в списке предложений, который читает
человек, — а не записи в отчёте партнёру.

**Правила языка складываются, а не выбираются по `lang` (T192, #157).** Языков
в разборе одновременно три, и совпадать они не обязаны: карту кадров пишет
управляющая компания, комментарий говорит аудитор, отчёт печатается на языке
партнёра. Причём `stems()` зовут и оттуда, где языка нет вовсе, — разбор самой
карты (`cues.load_cues`) и узнавание зоны по словам (`bot.zones`). Выбор правил
по `lang` означал бы, что русская карта перестаёт совпадать, как только аудитор
попросил английский отчёт: молча, без единого отказа и с виду «просто не
сработал быстрый путь». Сложение такого отказа не имеет — чужие стоп-слова в
тексте не встречаются, а окончания чужого алфавита не подходят ни к одному слову
своего.

Платим за это тем, что два языка одного алфавита (сербский рядом с английским)
складываются уже не бесплатно: служебное слово одного может оказаться значимым
словом другого. Пересечение стоп-слов поэтому ловится тестом, а не
договорённостью. Промахом это не становится: основы считаются одинаково и у
строки карты, и у комментария, поэтому чужое окончание даёт не несовпадение, а
другую основу сразу с обеих сторон.

**Что померено, а что померить не на чем (T192).** Русский путь не просел —
замер `make fastpath` на 17 боевых записях до и после правки совпал дословно:
срабатываний 1 (6 %), из них неверных 0; на эталонной зоне 3 (18 %), неверных 0;
разбивка отказов та же (13 × «строка не произнесена целиком», 2 × «зона не
названа», 1 × «не видно, грязь или поломка»). Сверх замера основы слов сверены
поштучно со старым разбором на 1220 русских текстах (карта, боевые записи,
вопросы методики, критерии): расхождений в русских словах **ноль**; два
расхождения — на латинских кусках внутри русского текста (`https` → `http`,
`levels` → `level`), ни одно не попадает в строку карты и ни на что не влияет,
потому что и строка карты, и комментарий разбираются одним и тем же кодом.
`make processhint` тоже не сдвинулся: 0 срабатываний на 1027 текстах.

**Английский мерить не на чем, и это надо говорить прямо.** Боевых английских
проверок не существует — доля срабатываний и, главное, число НЕВЕРНЫХ
срабатываний на английском не посчитаны ни по чему. Показывать здесь ноль
значило бы выдать отсутствие данных за успех. Проверено поведение правил на
единственной англоязычной методике в git (демо-набор): срабатывание есть там,
где слова однозначны, и снимается отрицанием. Как только английские проверки
появятся, `make fastpath` посчитает их сам — инструменту всё равно, на каком
языке запись.

**Английские окончания — только множественное число и прошедшее время.** Формы
на `-ing` в список не взяты намеренно: отсечка идёт по ПЕРВОМУ совпавшему
окончанию и в один проход, поэтому `-ing` рядом с `-s` разъезжается
(«ceiling» → «ceil», а «ceilings» → «ceiling») и стоит больше совпадений, чем
приносит. Частицы отрицания, наоборот, в стоп-слова НЕ вынесены, хотя по-русски
«без» и «нет» там стоят: строка карты со словом «without» не должна покрываться
комментарием, говорящим ровно обратное.

## Журнал работы

| Задача | Кто делал | Что сделано |
|---|---|---|
| T030 | сам | Сужение перечня: карта кадров, зональная база, тесты. Не отдано исполнителю — от порядка и полноты перечня зависит, увидит ли модель правильный код вообще |
| T031 | сам | Вызов модели: строгая JSON-схема (код+класс одним перечислимым полем), клиент провайдера, разбор ответа в `Suggestion` |
| T032 | сам | Правила формулировок зашиты в промпт целиком (тест сверяет с `docs/03-recording-rules.md` построчно) плюс второй слой — пометка подозрительных формулировок постфактум (`rules_check.py`) |
| T033 | сам | Транскрипция голосовых: перекодировка OGG/Opus → WAV через `ffmpeg` подпроцессом, вызов транскрипции, отказ — `TranscriptionFailed`, проверку не роняет |
| T034 | сам | Деградация без модели: `ModelUnavailable` не глотается, `manual_candidates()` — готовый перечень для кнопок ручного выбора, без сети и без ранжирования |
| T035 | сам | Замер точности на 31 боевом кадре, три модели, реальные вызовы API (не мок) — таблица выше. Не отдано исполнителю: выбор модели — не механическая работа, интерпретация чисел требует понимания контракта блока |
| T064 | сам | Разбор голого кадра по нажатию «Разобрать»: `classify("", photo=...)` не требует комментария, промпт меняет тон на «коротко, пунктами, без воды» — тесты на обе ветки текста запроса и на сам вызов |
| T113 | сам | Критерий однозначности быстрого пути и сам механизм (`fastpath.py`), колонки строк карты в `cues.py`. Исполнителю не отдано: критерий — решение, а не механика; слабый критерий здесь хуже отсутствия быстрого пути |
| T113 | исполнитель | Повторяемый замер доли однозначных срабатываний: `tools/fastpath_measure.py`, цель `make fastpath`, тесты. Отдано: контракт был описан целиком, работа механическая, ни миграций, ни политик, ни входа, ни денег, объём — больше полусотни строк однотипного кода |
| T143 | сам | Зона участвует в решении об однозначности: сужает коды внутри покрытой строки карты и только у пунктов с непересекающимися зонами; строки по зоне не отбрасываются — отбрасывание замерено и даёт неверное срабатывание. Слова колонок «грязь»/«поломка» переехали в карту к управляющей компании, встроенный словарь остался минимумом. Исполнителю не отдано: обе половины — про то, что считать однозначным, а не про механику; порчи (отбрасывание строк, разведение пересекающихся зон, вид пункта после зоны, замена словаря вместо дополнения, шапка таблицы в словаре, выбор первой колонки из двух) воспроизведены своими руками |
| T142 | сам | Порог различающего слова считается по различным кодам, к которым слово ведёт, а не по строкам карты и не по их доле: доля на черновике T118 делает различающими все 417 основ и поднимает «продукт» с десятью кодами — замерено. Исполнителю не отдано: задача выглядела как замена константы, а на деле требовала выбрать величину; три порчи (откат к счёту строк, снятие порога, порог в один код) воспроизведены своими руками |
| T166 | сам | Замер ложных срабатываний, корпуса и вывод: 0 срабатываний на 1027 текстах при 24 срабатываниях у признака «названо имя процесса». Главное решение — что считать корпусом: сырые слова аудитора отделены от заместителей, и пустота первого печатается словами, а не нулём. Порчи (сложение корпусов, немой пустой корпус, срабатывание по имени процесса) воспроизведены своими руками |
| T166 | исполнитель | Сама чистая функция `process_hint()` и её тесты. Отдано: контракт признака был описан целиком (связки, сравнение хвоста по основам, чтение имён из методики, оба запрета), работа механическая. На приёмке нашёл и починил две вещи: тесты про тире проходили и со сломанным правилом (хвост в них не был именем процесса — порча это показала), и порядок имён процессов зависел от засева хеша |
| T125 | сам | Замер чинится по существу: зона добывается тремя способами (как бот / только слова / эталон), с разбивкой «откуда бот брал зону» и отдельным счётом неверных срабатываний; шапка несёт дату и md5 карты слов. Исполнителю не отдано: править надо было не механику, а **что именно считается результатом**, — та же цена ошибки, что у самого критерия однозначности. Тесты написаны до правки и краснели на неисправленном инструменте; три порчи (подстановка эталона, протекание памяти между проверками, память сильнее слов) воспроизведены своими руками |
| T192 | сам | Правила разбора слов стали данными: `language.py` + `language_rules.json` (стоп-слова, окончания, отрицания, словарь колонок, заголовки разделов карты — на каждый язык), `cues.py` и `fastpath.py` читают их оттуда, заголовки разделов узнаются на любом объявленном языке. Исполнителю не отдано: работа состояла не в переносе списков, а в выборе — складывать правила языков или выбирать по `lang` (выбор по `lang` ломает русскую карту молча), и какие английские слова считать словарём колонки, где лишнее слово стоит записи в отчёте. Тесты написаны до правки и краснели на неисправленном коде; тест про отрицание сразу нашёл настоящий промах — в английских частицах не было «no» |
| T192 | исполнитель | Тесты на проверки границы файла правил: двенадцать случаев (нет файла, битый JSON, пустой объект, список вместо объекта, пустой `about`, слово в верхнем регистре, пустой список, не тот тип, пустой словарь колонок, половина разделов, заголовок без «## », опечатка в имени раздела). Отдано: список случаев описан целиком, работа однотипная, 200 строк. Принято без правок; порчей проверено, что тесты не декоративны — снятие проверки регистра красит их |
| T195 | сам | Защита от отрицания распространена со слов колонки на саму строку карты и сверяется с двух сторон; частица получила направление (данные, а не код), стала перешагивать служебные слова и перестала переходить знаки препинания. Исполнителю не отдано: работа состояла не в правке правила, а в **выборе** правила — четыре варианта отмерены на боевой и черновой картах, и три из четырёх либо ловят вчетверо меньше, либо убивают 30 строк карты из 32 незаметно. Тесты написаны до правки: пять красных на неисправленном коде, пять — сторожа против перестраховки, зелёные с обеих сторон. Семь порч воспроизведены своими руками (снятие каждой из двух половин сверки, потеря направления, переход знака препинания, отказ перешагивать служебные слова, обе проверки файла правил), восстановление сверено по md5. Попутно нашлось, что детектор утечки методики чувствителен к регистру и пропустил дословную строку карты в докстроке — заведено задачей #162 |
| T195 | исполнитель | Второй раздел замера `make fastpath`: корпус отрицаний строится из самой карты на ходу, считается доля пойманных по четырём образцам и отдельно — сколько строк теряется на безобидном отрицании. Отдано: алгоритм и ожидаемые числа были описаны целиком, работа механическая |
| T196 | сам | Связка признака процесса стала данными на каждый язык (`connectives` в правилах, символы «=» и «→» остались в коде — словами языка они не являются). Исполнителю не отдано: работа была не в пятнадцати строках, а в решении — предыдущий заход отказался, и надо было проверить его довод, а не повторить. Довод («английскую связку мерить не на чем») не подтвердился: колонка `question_en` в методике даёт корпус того же рода, каким меряется русская связка; замер расширен им, английская связка не сработала ни разу на 136 вопросах, 31 из которых содержит «is». Тесты написаны до правки, два краснели на неисправленном коде |
| T197 | сам | Остаток отрицания закрыт: основа, сказанная и отрицаемая в одной фразе разом, не считается ни сказанной, ни отрицаемой. Исполнителю не отдано: правка — две строки, а работа вся в том, ЧТО делать с противоречием, и в замере, что от этого не потерялись срабатывания (их и так единицы). Отказ, а не выбор стороны: цена срабатывания — запись в отчёте без подтверждения, цена отказа — вызов модели. Тесты написаны до правки: два красных на неисправленном коде (`test_recognize_fastpath_negation.py`) плюс сторож против перестраховки — повтор слова БЕЗ отрицания обязан срабатывать как раньше. Замер `make fastpath` до и после на обеих картах: боевая — пропусков 25 → 17 из 412, поймано 94% → 96%, срабатываний 1 и НЕВЕРНЫХ 0 в обоих прогонах; черновик T118 — 26 → 26 из 1092, 98%, 7 и 0. Списки пропусков сверены построчно: на боевой ушли ровно восемь известных строк и ни одной другой, на черновике не изменилась ни одна. Строк с утвердительным срабатыванием осталось столько же (32 и 171), потеряно на безобидном отрицании 0 из 32 и 0 из 171. Порча (правило снято, `_said` возвращён к прежнему виду) валит ровно три теста, восстановление копией файла, `PYTHONDONTWRITEBYTECODE=1`, сверено по md5 `fastpath c46b31c9…`. Попутно приведены к факту места, называвшие эту дыру законным пропуском: тест замера, две докстроки и объясняющий абзац самого инструмента. Общий прогон трёх задач одной волной: `make fmt lint types dead bounds` зелёный, полный набор с Postgres — **1941 passed** (без `tests/test_db_migrate_roles.py`, #128), `make regress` — 97.5%/A/5×D1 и 97%/A/6×D1 дословно. |
| T226 | сам + два исполнителя | Разбор пришит к изданию проверки: `chat_id: int | None` стал обязательным именованным параметром БЕЗ умолчания у `classify`, `fast_path`, `manual_candidates`, `process_hint`, `shortlist`, `picks_for`, `question_text` и у всех четырёх функций карты слов (`cues_path`, `load_cues`, `class_thresholds`, `column_words`). Умолчание не оставлено намеренно: оно и есть тот молчаливый откат на действующую методику, ради снятия которого задача заведена. «Проверки нет» названо вслух константой `NO_CHAT` в `config.py` — общей на продукт, а не своей копией у каждого замера; локальная копия `tools/fastpath_measure.py` (T225) снята и заменена импортом. **Сам:** решение о форме (обязательный параметр против умолчания; явный путь к карте сильнее чата, потому что MCP читает хранилище версий, а не проверку), новый тест `tests/test_recognize_edition_pinned.py` (11 случаев: проверка заводится, методика под ней переиздаётся — переименование зоны, подмена классов, снятие пункта, снятие строки карты, опустошение карты, дописанный раздел слов колонок — и дальше спрашивается разбор), правка `src/recognize/*`, `tools/*`, `src/mcp/photo_cues.py`, `src/bot/routers/record.py`. **Исполнители (двое, параллельно):** механическая правка 160 вызовов в 13 файлах тестов по готовому контракту — отдано ровно потому, что контракт был описан целиком и решений в работе не осталось. Приняты без правок; оба сами заметили ловушку с двумя разными `column_words` (правила языка против карты) и не перепутали. **Сверх перечня задачи** пришит и `process_hint`: он читает имена процессов той же `list_items()` и в продукте пока никем не зовётся (ждёт T165) — заводить у него умолчание, чтобы снимать его потом, значило бы заложить дефект осознанно. Проверено: семь порч (каждая снимает издание в одном месте — карта, перечень, классы, ручные кнопки, текст пункта, названия зон, покрытие строк) валят ровно свои тесты, от одного до четырёх, восстановление копией, md5 всех семи файлов сошлись. Полный набор 1870 → **1887 passed**, 227 skipped; `make fastpath` 7 из 17 при нуле неверных и раздел отрицания 171/1092/26/98% — дословно как до правки; `make regress` 97.5%/A/5×D1 и 97%/A/6×D1 |
| T212 | сам | Живому замеру нужны и `data/`, и `examples/` — метка стала второй. Без каталога эталонов тест падал (`fpm.main` печатал «боевых данных нет — это не поломка инструмента» и отдавал код 2 против `assert rc == 0`), а соседний тест в том же положении пропускался: одно и то же отсутствие данных давало в наборе два разных исхода. Ослабление до `rc in (0, 2)` отвергнуто — якорь стал бы зелёным на копии без эталонов, а это ровно та беда, из которой вырос баннер неполного прогона (#175). Проверено опытом на копии дерева без `examples/`: было 1 failed, 15 passed, 1 skipped; стало 15 passed, 2 skipped, оба пропуска посчитаны баннером |
| T224 | сам | Оснастка замера цены кадра прогнана вхолостую с подставным ключом (все вызовы — 401, платных обращений ноль) и починена по трём дефектам вывода: без `STATE_DIR` летела трассировка `ConfigError` вместо одной внятной строки; прогон, где все 34 вызова оборвались отказом, печатал «код верный 0 (0 %)» и «разница входных токенов +0. Это и есть цена кадра» — то же самое, что сказал бы настоящий замер, у которого кадр ничего не даёт, и отличался только предпоследним столбцом таблицы; шапка обещала, что ключ модели в отчёт и `--out` попасть не может, хотя провайдер повторяет его замаскированным в тексте 401, а текст цитируется как есть. Первое — отказ и код 2, как у `tools/process_hint_measure.py`; второе — предупреждение ДО таблиц, снятое «это и есть цена» и код возврата 1 на полностью провалившийся прогон; третье — обещание заменено фактом (резать текст своим выражением не стали: форма ключа это знание о провайдере, оно по D010 живёт в `client.py`). Шесть новых тестов, модель подменена на уровне `classify.ask_model`; четыре порчи валят ровно ожидаемые тесты, восстановление копией, md5 сошёлся. **Сам замер НЕ запускался** — он платный и ждёт решения владельца |
