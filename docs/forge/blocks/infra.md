# Блок: infra

## Назначение

Поднять продукт одной командой там, где он должен жить, и сделать так, чтобы переезд не был переписыванием.

## API-контракт

```bash
docker compose up -d                   # поднимает бота
docker compose --profile demo up -d    # он же плюс демо-стенд (T102)
tools/demo_refresh.sh                  # подтянуть main и пересеять демо
make regress                           # прогон движка на эталонных данных
make demo                              # демо-набор на английском
```

Переменные: `TELEGRAM_BOT_TOKEN`, `OPENAI_API_KEY`, `AUDIT_DATA_DIR`,
`ALLOWED_TELEGRAM_IDS`, `STATE_DIR`, `BOT_MODE` (`polling` | `webhook`), `TZ`.

Демо-стенд (T102, решение D059) берёт их из **того же** `.env` тем же
механизмом. Своё у него только то, что обязано отличаться:
`DEMO_TELEGRAM_BOT_TOKEN`, `DEMO_ALLOWED_TELEGRAM_IDS`, `DEMO_DATABASE_URL`,
`DEMO_STATE_DIR`. Единственный канонический список с описаниями —
`.env.example`; здесь только имена, чтобы два списка не разъехались.

## Зависимости

`bot`.

## Definition of Done блока

- [x] Образ `python:3.12-slim` с `ffmpeg`, системными библиотеками WeasyPrint и кириллическим шрифтом. Проверен не сборкой, а работой: сид собрал PDF **внутри контейнера**.
- [ ] `docker compose up -d` поднимает бота на домашнем сервере; префикс проекта свой, чужие контейнеры не затронуты. **Локально — да** (стенд поднят под своим именем `dodo_audit_service-infra2`, соседний проект `dodo-pnl` не затронут, всё снято `down -v`). **На домашнем сервере — не проверялось: это T073, туда ходит диспетчер.**
- [x] `.env.example` со всеми именами переменных закоммичен; настоящий `.env` в git не попадает. На полноту стоит проверка: каждая `${DEMO_*}` из файла стенда обязана быть описана.
- [x] Состояние проверок лежит в томе и переживает пересоздание контейнера. Показано на демо-стенде: сид записал состояние в контейнере `demo-seed-1`, прочитано оно **другим** контейнером из того же тома.
- [x] Каталог с методикой монтируется снаружи: `data/` в образ не запекается. Синтетический чек-лист демо тоже монтируется, а не копируется, — иначе правка данных в `main` требовала бы пересборки образа.
- [x] Часовой пояс задан явно — иначе даты в отчёте уедут относительно местного времени.
- [x] Демо-набор на английском: вымышленная точка, синтетический чек-лист, повторный запуск возвращает демо к чистому виду. Английский проверяется во всех артефактах: методика, состояние, имя файла, письмо, содержимое отчёта.
- [x] Смена площадки не требует правок кода: проверено поднятием того же compose локально.

## Статус

`ready-for-review` — по задачам T070, T071, T072, T074 (приняты 02.09), T100,
T102.

Открытым в блоке остаётся то, что требует самой площадки и отдельного
измерения: **T073** (поднять на домашнем сервере и проверить, что перезапуск
контейнера не теряет незавершённую проверку) и **T101** (измерить потолок
нагрузки числом, решение D058). Ни то, ни другое в эту волну не входило.

## Журнал работы

### Волна 2 (03.09.2026): T100, T102

**Находка до первой правки — демо-набора нет в git.** `git ls-files demo`
пуст, `git check-ignore -v demo/data/checklist.csv` показывает
`.gitignore:8:data/`. Правило `data/` в gitignore не привязано к корню и
совпадает с **любым** каталогом `data` на любой глубине, включая
`demo/data/`. Поэтому синтетический чек-лист демо (T074) не попал в индекс ни
одним коммитом — `git log --all -- demo` знает только `demo/state/`. В
основной копии этих файлов уже нет физически: там остался пустой
`demo/state/`. То есть `make demo` сегодня не работает ни у кого, а приёмка
T074 этого не увидела, потому что данные лежали в рабочей копии агента.

Следствие для плана: до T100 и T102 демо-набор надо восстановить и
закоммитить, иначе проверять нечего.

**Окружение прогона — ловушка.** Команда из брифа
(`python3 -m venv .venv && ./.venv/bin/pip install -q -e ".[dev]"`) на этой
машине даёт **сломанный venv молча**: системный `python3` здесь 3.9.6, а
`pip 21.2.4` из него отказывается ставить пакет с одним `pyproject.toml`
(«editable mode currently requires a setuptools-based build»). Установлено
ноль пакетов, `weasyprint` не импортируется, а `echo` после `tail` печатает
успех. Рабочая команда — с явным интерпретатором:
`/opt/homebrew/opt/python@3.12/bin/python3.12 -m venv .venv`.

#### T100 — демо целиком на английском (**сам**)

Сам: работа исследовательская — надо было пройти демо целиком и решить
развилку про `STATE_DIR`; механического куска на полсотни строк тут нет.
Исполнителю ушёл только набор данных (см. ниже).

**Развилка, решённая мной: сид НЕ читает `STATE_DIR`.** У демо своя
переменная `DEMO_STATE_DIR`, по умолчанию `demo/state`. Почему не `STATE_DIR`:
это путь работающего стенда, и на сервере он указывает на настоящие проверки.
Читай сид его — `make demo`, запущенный на стенде, положил бы демо-проверку
рядом с боевыми, а демо-отчёт лёг бы в папку с отчётами партнёрам. Симметрия
с `AUDIT_DATA_DIR`, который сид перекрывает и обязан перекрывать: демо не
наследует пути стенда, у него своя пара. При этом переменная именно
переменная, а не константа: демо-стенд в `docker-compose.yml` ставит
`DEMO_STATE_DIR` и `STATE_DIR` в один путь своего тома — там они совпадают, и
это видно в файле.

**Про `--lang en`.** Отдельным аргументом сборщику он не передаётся и не
должен: язык отчёта уже записан в шапке проверки (`report_lang="en"`), движок
читает его оттуда (`lang = --lang или meta.lang`, `engine/report.py:624`).
Вторая запись того же факта — ровно тот дубль, который конституция запрещает.
Проверено фактически: имя собранного файла — `Audit Demo Pizzeria #1 - Demo
Auditor - 03.09.2026.pdf`, в содержимом отчёта кириллицы нет.

**Что нашёл русского, проходя демо целиком:**
- `ui_lang` и `speech_lang` демо-проверки были `ru` (умолчание
  `start_inspection`). Бот берёт язык интерфейса из состояния проверки —
  значит, на демо-пути он говорил бы по-русски. Мой файл, починено.
- письмо партнёру сид не собирал вовсе — проверять было нечего. Собирается,
  кладётся рядом с отчётом.
- **чужое, не чинил:** до начала проверки язык интерфейса берётся из
  `DEFAULT_UI_LANG = "ru"` (`src/bot/texts.py:28`), и приветствие демо-бота
  будет русским. Переменной для языка интерфейса нет — нужна правка блока
  `bot`, описано в отчёте диспетчеру.

#### Набор данных `demo/data/` (**исполнитель**)

Контракт был описан целиком (колонки, коды, зоны, доли, ставки, правила
классов), работа однотипная — десять пунктов чек-листа, пять зон, критерии на
каждый пункт, — миграций, политик, входа и денег не касается, объём заметно
больше полусотни строк. Отдано `forge-executor`. Принято с двумя правками
моими: перевёл файл на LF (пришёл с CRLF) и сверил результат прогоном, а не
по отчёту.

#### T102 — демо не отстаёт от `main` (**сам**)

Сам: это выбор механизма и его обоснование, а не механическая работа.

**Развилка, решённая мной: профиль `demo` в том же `docker-compose.yml`,
плюс скрипт обновления на расписании площадки.** Что рассматривал и почему
отвергнуто:

- *Отдельный `docker-compose.demo.yml`* — второй файл расходится с первым
  молча, и это ровно та болезнь, которую задача лечит.
- *Реестр образов + watchtower* — требует публиковать образы наружу и
  поднимать ещё один сервис; наружу нам нельзя, а платить за реестр тем более.
- *GitHub Actions с деплоем по ssh* — нужен либо доступ извне на домашний
  сервер, либо self-hosted runner; и то, и другое за границами задачи.
- *Отдельный compose-проект для демо* — сильнее изолирует, но ломает главное
  требование «той же командой»: команда становится другой, и демо снова живёт
  своей жизнью.

Как устроено:

- `x-stand` — общая часть обоих стендов якорем, а не копией: сборка, образ,
  источник секретов. Демо физически не может собираться из другого образа.
- `demo-seed` (одноразовый) и `demo` (бот) под `profiles: ["demo"]`. Обычный
  `docker compose up -d` их не поднимает и демо-переменных не требует —
  проверено `docker compose config --services`.
- Изоляция: свой том `demo-state`, свой чек-лист `./demo/data:ro`, свой
  токен. Токен перекрывается на `${DEMO_TELEGRAM_BOT_TOKEN:-}` — пустое
  значение вместо наследования боевого из `env_file`. **Проверено запуском:**
  демо-бот без токена падает с внятным `BotConfigError`, а не молча
  перехватывает обновления настоящего бота.
- Доступы — из того же `.env` тем же механизмом (D059). Отличается только то,
  что обязано: токен, список ID, база. Всё это описано в `.env.example`, и на
  описание стоит проверка.
- `tools/demo_refresh.sh` — приводит копию к `origin/main` и пересобирает
  **поимённо** `demo-seed demo`: `up` без имён перезапустил бы боевого бота и
  оборвал идущие проверки. Вешается на расписание площадки (`schtasks` на
  Windows-сервере), сам туда не ходил — это T073.

**Что проверено фактически, а не по коду:**

- образ собрался; `docker compose --profile demo up demo-seed` внутри
  контейнера дал `Score: 92% grade B`, отчёт `Audit Demo Pizzeria #1 - Demo
  Auditor - 03.09.2026.pdf` и `letter.txt` на томе `demo-state`;
- письмо партнёру прочитано целиком из тома — английское, без единого
  русского слова;
- демо-бот без токена отказал громко;
- `demo_refresh.sh --check` отказал на грязном дереве с внятной причиной.

Порт из выделенного диапазона 8220-8229 не занят ни один: у бота нет
HTTP-поверхности (long polling, D004), публиковать нечего.

#### `make demo` (**сам**, попутно)

Цель печатала заглушку «поднимается блоком infra (задача T074)» спустя сутки
после того, как T074 был сделан. Теперь зовёт сид.

#### Найдено в чужих блоках — не чинил

- **`src/bot/texts.py:28`, `DEFAULT_UI_LANG = "ru"`.** До того, как проверка
  начата, язык интерфейса взять неоткуда, и бот здоровается по-русски. На
  демо-стенде это единственное оставшееся русское место: сама демо-проверка
  заводится с `ui_lang="en"`, но приветствие до неё — русское. Переменной для
  языка интерфейса нет, поэтому демо-профиль это выключить не может.
  Починка — блок `bot`: язык интерфейса по умолчанию должен приходить
  переменной (в духе «язык — параметр, никогда не константа»), и тогда
  демо-стенд ставит ей `en` рядом с остальными своими переменными.
- Отказы на старте (`BotConfigError` и родня) написаны по-русски. Это текст
  оператору в логе, а не пользователю демо, — дефектом не считаю, отмечаю,
  чтобы не всплыло как «нашли русский в демо».

#### Замечание по безопасности прогона

В рабочей копии лежит `.env` с **настоящим токеном боевого бота**, и
`docker compose config` его подставляет. Поэтому проверка стенда сравнивает
значения, но нигде их не печатает: ни в сообщении отказа, ни в выводе
прогона. При добавлении новых проверок стенда это правило надо держать —
`reports/junit.xml` уезжает в артефакты CI.

#### Полный прогон перед сдачей

```
593 passed in 42.75s          покрытие 97 % (было 569 проверок)
make check                    fmt / lint / types / dead / bounds — зелёные
make regress                  belgrade-1 → 97.5 %, A, 5×D1
                              belgrade-2 → 97 %,   A, 6×D1
```

### Волна 3 (05.09.2026): T150, T186

#### T150 — карта кадров демо (**тест и разбор сам, набор строк — исполнитель**)

**Сначала проверил фактом то, что числилось вылеченным.** Отсутствие карты
действительно больше не отказ (T157, D068): демо-разговор доходит до конца, и
русского сообщения с абсолютным путём аудитор не видит. Но лечение закрыло
только половину задачи. Прогон до правки, дословно:

```
'The model is unavailable — the inspection goes on, pick the item.'
'Checklist items, page 1 of 1:'
```

То есть на показе первый же комментарий упирался в модель, а демо-стенд живёт
без боевого ключа. **Быстрый путь (D063, D064) — единственное место, где запись
появляется без модели, и без карты его в демо нет вовсе.**

**Развилка, решённая мной: в карте демо нет разделов «Пороги классов» и «Слова,
которыми аудитор называет колонку».** Парсер (`src/recognize/cues.py`) узнаёт
оба раздела ТОЛЬКО по русским заголовкам, а демо обязано быть английским
целиком — совместить нельзя. Выбор был между «завести в демо русский заголовок»
и «обойтись без этих разделов»; второе ничего не стоит: пороги классов у демо
описаны в `demo/data/criteria.md`, а различение колонок держится на встроенном
минимуме, и оба состояния законны (`class_thresholds` и `column_words` на
отсутствующем разделе отвечают пустотой по D068). Правкой кода это чинить не
стал: она за границей блока и задачей не просилась. Само наблюдение — в отчёте
диспетчеру, это дефект многоязычности продукта, а не демо.

**Что сделал сам:** тесты (написаны до правки, красные — 5 из 5), разбор
поведения, выбор опорной строки карты и комментария, на котором быстрый путь
обязан сработать, проверка порчей, смоук запуском, две правки в файле
исполнителя.

**Что отдал `forge-executor`:** сам набор строк карты (31 подсказка на десять
кодов, пять разделов по зонам демо). Работа однотипная и с готовым контрактом —
приёмкой служили мои тесты. Принято с двумя моими правками: строки
`boxes stacked on the floor` и `shelf blocking the wall` держались на слове
`the`. Оно длиннее двух знаков и в список стоп-слов не входит (список русский),
поэтому основой считается наравне с существительным, и подсказка требовала
артикля от аудитора. Строки переписаны без артикля.

**Проверено порчей** (`PYTHONDONTWRITEBYTECODE=1`, восстановление копией, md5
до и после совпал — `1c2e60df04e1bb359f3640829e9153c5`):

| Порча | Что покраснело |
|---|---|
| карта убрана с диска | 4 теста из 5, разговор дошёл до «модель недоступна» — то есть деградация по D068 жива |
| опорная строка ведёт в другой пункт | быстрый путь и разговор целиком |
| из карты убран один код | покрытие чек-листа картой |

**Смоук запуском** (настоящие роутеры и движок, подменён только транспорт
телеграма; `OPENAI_API_KEY` снят из окружения — так живёт демо-стенд):
приветствие `Field audit bot. Tap “New inspection” to begin.` → мастер целиком
по-английски → кадр с комментарием `Fingerprints and dust on the entrance door
glass` → `✅ Recorded straight away, from your words — no confirmation needed.
#1 DEM02 · D1 · Storefront / entrance` → `/finish` → `Result: 99.0% — A` →
PDF `Audit Demo Pizzeria #1 - ... .pdf` (16 109 байт, сигнатура `%PDF-`) и
письмо партнёру по-английски. Сообщения «Analyzing…» в переписке нет ни разу —
модель не звалась. Сид демо (`tools/seed_demo.py`) на пополненном наборе
по-прежнему даёт якорь `92% grade B`.
