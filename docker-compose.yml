# Стенд бота выездных проверок (задача T071, блок infra).
#
# Имя проекта зашито здесь же (`name:`), а не оставлено флагу `-p`: имя внутри
# файла общее для каждой копии репозитория, и без своего префикса `up` на
# чужой площадке подхватил бы контейнеры соседа, а `down -v` унёс бы его том.
# `-p dodo_audit_service-infra` / `COMPOSE_PROJECT_NAME` всё ещё работают и
# перебивают это значение — но полагаться приходится не на память оператора.
name: dodo_audit_service-infra

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped

    # Секреты и параметры продукта (TELEGRAM_BOT_TOKEN, OPENAI_API_KEY,
    # ALLOWED_TELEGRAM_IDS, AUDITOR_NAMES, DATABASE_URL, BOT_MODE, TZ) —
    # из .env целиком, имена и назначение — в .env.example. AUDIT_DATA_DIR и
    # STATE_DIR приходят оттуда же для хоста (см. volumes ниже), но внутри
    # контейнера ниже намеренно переопределены на точки монтирования: .env
    # хранит host-путь для разработки без Docker, а не путь внутри образа.
    env_file:
      - .env
    environment:
      AUDIT_DATA_DIR: /app/data
      STATE_DIR: /app/state
      TZ: ${TZ:-UTC}

    volumes:
      # Методика — данные управляющей компании (docs/forge/plan.md, «Данные
      # предметной области лежат вне репозитория»): каталог снаружи, в образ
      # не запекается (см. Dockerfile). `:ro` — бот методику не правит,
      # `manage.py` не отсюда запускается.
      - ${AUDIT_DATA_DIR:-./data}:/app/data:ro
      # Состояние идущих проверок (`inspection.json` на чат, решение D007) —
      # именованный том, переживает пересоздание и переезд контейнера
      # (решение D004) без правки пути на диске конкретного хоста.
      - state:/app/state

    # У бота нет HTTP-порта — long polling (решение D004), Telegram сам
    # держит соединение наружу. Порты стенда (8200-8209) сюда никакие не
    # идут: публиковать нечего.

volumes:
  state:
